import type { ru } from "./ru.ts";

type DeepString<T> = {
  [K in keyof T]: T[K] extends string ? string : DeepString<T[K]>;
};

export const en: DeepString<typeof ru> = {
  app: {
    title: "Placitum",
  },
  nav: {
    fleet: "Status monitoring",
    settings: "Settings",
    datasets: "Data",
    lists: "Lists",
    ruleSets: "Traffic processing rules",
    rules: "Rule sets",
    ipData: "Address sets",
    ip: "IP filter",
    ipCountries: "Geo",
    ipAsns: "ASN",
    ipSets: "Composite",
    auth: "Second factor",
    captcha: "Captcha",
    json: "Specification gate",
    counter: "Counter",
    vlai: "Classifier",
    rewrite: "Response rewrite",
    cookie: "Cookies",
    actions: "Auto actions",
    authProfiles: "Profiles",
    authUsers: "Users",
    inspectors: "Inspectors",
    inspectorCatalog: "Catalog",
    incidents: "Security events",
    logs: "Logs",
    help: "Documentation",
    license: "License",
    structure: "Structure",
    config: "Configuration",
    general: "General",
    agent: "Agent",
    haproxy: "haproxy",
    http: "http",
    ports: "Listen addresses",
    upstreams: "Protected servers",
    servers: "Virtual servers",
    paths: "Paths",
    files: "Files",
    certificates: "Certificates",
    setup: "Setup wizard",
    expand: "Expand",
    collapse: "Collapse",
  },
  help: {
    title: "Documentation",
    blurb:
      "Administrator guide: how to set the protection up and what the panel is showing. The texts ship with the panel and open even when the API is down.",
    contents: "Contents",
    draft: "This section is not written yet. It is still in the plan — see the contents for what it will hold.",
    draftShort: "planned",
    ruOnly: "Help texts are Russian-only for now.",
    unknown: "No such section. Open the contents and pick one.",
  },
  license: {
    title: "License agreement",
    hint: "Accept the terms to work with the panel. The text is always available via the “License” link at the bottom of the main menu.",
    agree: "I have read the terms and accept them",
    accept: "Accept",
    version: "Version {version}",
    acceptedAt: "Terms accepted on {date} in this browser",
    notAccepted: "The terms have not been accepted in this browser yet",
  },
  connection: {
    checking: "checking…",
    online: "API online",
    offline: "API offline",
  },
  mode: {
    debug: "debug",
  },
  theme: {
    toLight: "Light theme",
    toDark: "Dark theme",
  },
  locale: {
    switchToEn: "English",
    switchToRu: "Русский",
  },
  fleet: {
    aria: "status",
    green: "all nodes up",
    yellow: "degraded or empty",
    red: "socket down or all degraded",
    nodes: "Nodes",
    live: "live /agent_health_socket",
    disconnected: "socket disconnected",
    empty:
      "Nobody is pulsing yet. Store, agents, inspectors and services write WAF_STATUS every 4 s.",
    workers: "{count} worker",
    workersHint: "click to expand",
    orphans: "Workers without an agent",
  },
  inspectorsPage: {
    title: "Inspectors",
    empty: "Nobody is pulsing yet. An inspector writes WAF_STATUS.inspector.<name>.<id> every 4 s.",
    replicas: "{up} / {count}",
    host: "Host",
    work: "Work",
    queue: "NATS group",
    queued: "Queued",
    queuedHint:
      "How many requests are waiting to be picked up. Not a capacity or a limit — the length only. vlai runs one inference at a time, so the queue can grow even when a single request is fast.",
    avgMs: "Latency",
    avgMsHint:
      "Mean time to process one request after it has been taken — not time spent waiting in the queue. Up to 100 ms is green, up to 500 ms yellow, above that red.",
    subject: "Subject",
    engineLabel: "Type",
    phaseLabel: "Phase",
    engine: {
      test: "test",
      modsec: "modsec",
      ip: "ip",
      vlai: "vlai",
    },
    phase: {
      request: "request",
      response: "response",
      frame: "frame",
      unknown: "—",
    },
    replicasLabel: "Replicas",
    desired: "desired",
    drift: "Delivery",
    driftState: {
      ok: "ok",
      stale: "previous generation",
      pending: "not applied yet",
      failed: "apply failed",
      foreign: "foreign configuration",
      silent: "silent",
      unmanaged: "not delivered",
      unknown: "—",
    },
  },
  convergence: {
    title: "Configuration delivery",
    blurb: "What is saved, what was sent, and what the members applied.",
    sendAll: "Send all",
    channel: {
      nginx: "nginx configuration",
      agent: "Agent settings",
      haproxy: "haproxy configuration",
      rules: "Rule profiles",
      ip: "IP filter profiles",
      auth: "Gate: sources and profiles",
      captcha: "Captcha profiles",
      json: "Specification profiles",
      counter: "Counter profiles",
      action: "Auto action profiles",
      vlai: "Classifier profiles",
      rewrite: "Response rewrite profiles",
    },
    state: {
      ok: "applied",
      empty: "empty",
      dirty: "not sent",
      no_effect: "no effect on the config",
      broken: "does not compile",
      never: "never sent",
      converging: "applying",
      failed: "apply failed",
      foreign: "foreign configuration",
      silent: "silent",
      nobody: "no members",
      unmanaged: "not delivered",
    },
    reason: {
      dirty: "Saved but not sent yet: members keep the previous generation.",
      noEffect:
        "Settings changed but the compiled configuration is identical: this edit goes nowhere. Either the compiler does not read the field, the directive is not supported by this module build, or the edited object is referenced by nothing.",
      never: "No generation on the bus: this channel has never been sent.",
      converging: "The generation has not reached everyone yet: some members still run the previous one.",
      silent:
        "The generation is published but nobody reports it: the channel members have been silent past the TTL.",
      failed:
        "The generation arrived but did not take: {nodes}. Their live configuration is unchanged.",
      foreign:
        "Members run a configuration the controller never published: {nodes}. A button will not fix this.",
    },
    lamp: {
      green: "configuration applied",
      yellow: "something to send",
      red: "configuration not applied",
    },
    sendFailed: "Send failed",
  },
  logsPage: {
    title: "Logs",
    blurb:
      "waf.log: access_log and error_log lines from the nodes, plus the log of every process on the contour.",
    time: "Time",
    writer: "written by",
    service: "service",
    severity: "level",
    text: "substring in text",
    filter: {
      all: "all",
    },
    pick: {
      on: "filter by {value}",
      off: "clear the filter",
    },
    empty: "No lines in this window.",
    none: "No lines match this filter.",
    unreachable: "Search is unavailable.",
    levels: {
      button: "Log levels",
      title: "Log levels",
      hint: "Contour services. A level applies right away, without a restart: the service switches its threshold and logs “log level applied”. “From environment” is the start value from the variable on the right — the level returns to it.",
      service: "service",
      level: "level",
      env: "variable",
      fromEnv: "from environment",
      others:
        "Inspectors — in the inspector catalog: the level travels with their channel generation. nginx — error_log in the node configuration; nginx core debug — WAF_NGINX_DEBUG=on on the node.",
      loadFailed: "Could not read levels: {error}",
      saveFailed: "Could not save levels: {error}",
    },
  },
  incidentsPage: {
    phase: "Phase",
    phaseValue: {
      request: "request",
      response: "response",
      frame: "frame",
      session: "session",
    },
    statusSplit:
      "Left is what the client got, right is what the application answered. They differ: we closed the application's response.",
    title: "Security events",
    blurb:
      "waf.audit spine for the last 24 hours: ClickHouse through search.",
    empty: "No rows in this window.",
    none: "No rows match this filter.",
    method: "method",
    host: "host",
    uri: "uri",
    route: "route",
    server: "server",
    request: "host/uri",
    status: "status",
    inspector: "inspector",
    user: "user",
    userHint:
      "A login, or \"source:login\" — the same string the column and the grouping show. A bare login finds namesakes across every sign-in source; a login that contains a colon is searched with an empty source: \":urn:user:1\"",
    marker: "marker",
    country: "country",
    countryHint:
      "Two-letter country code of the address (de). Resolved from the space catalog, not from the record: the filter works on rows written before the catalog was loaded",
    asn: "ASN",
    asnHint:
      "Autonomous system number of the address, without \"AS\" (9009). Same catalog as the country",
    time: "Time",
    timeMs: "show milliseconds",
    timeSec: "hide milliseconds",
    orderAsc: "show oldest first",
    orderDesc: "show newest first",
    verdict: "Verdict",
    score: "Score",
    filter: {
      all: "all",
      deny: "deny",
      allow: "allow",
      redirect: "redirect",
    },
    window: "24h",
    unreachable: "Search is unavailable.",
    ray: "ray",
    rayTail: "show the ray tail only",
    rayWhole: "show the whole ray",
    ip: "IP",
    cidr: "ip / cidr",
    range: {
      "15m": "15m",
      "1h": "1h",
      "6h": "6h",
      "24h": "24h",
      "7d": "7d",
      "30d": "30d",
      today: "today",
      yesterday: "yesterday",
      week: "this week",
      from: "from",
      to: "to",
    },
    seek: {
      apply: "filter by this value",
      clear: "clear this filter",
    },
    chip: {
      verdict: "verdict",
      state: "state",
      score: "score",
      role: "role",
      latency: "latency",
      ms: "ms",
      more: "+{count}",
    },
    search: {
      title: "search in request",
      kind: {
        header: "header",
        param: "param",
        body: "body",
      },
      placeholder: {
        header: "name or name=value",
        param: "name or name=value",
        body: "substring in body",
      },
    },
    columns: {
      label: "columns",
      count: "columns: {shown} of {total}",
      fixed: "always",
      forced: "filtered",
      all: "show all",
      name: {
        time: "time",
        ray: "ray",
        ip: "address",
        phase: "phase",
        method: "method",
        host: "host",
        uri: "uri",
        server: "server",
        route: "route",
        status: "status",
        verdict: "verdict",
        score: "score",
        inspector: "inspectors",
        user: "user",
        marker: "marker",
        country: "country",
        asn: "ASN",
      },
    },
    group: {
      title: "grouping",
      dim: {
        ip: "address",
        host: "host",
        uri: "uri",
        route: "route",
        server: "server",
        method: "method",
        status: "status",
        verdict: "verdict",
        phase: "phase",
        marker: "marker",
        user: "user",
        country: "country",
        asn: "ASN",
      },
      hits: "Requests",
      outcomes: "outcomes",
      last: "Last seen",
      none: "No groups match this filter.",
      filters: "filters",
      open: "to filters",
      pick: "filter by this value; the axis is dropped — nothing left for it to split",
      total: "groups: {count}",
    },
    resetFilters: "reset filters",
    card: {
      siblingPhase: "to the {phase} record",
      inspectors: "inspectors",
      request: "request",
      headers: "headers",
      args: "params",
      body: "body",
      by: "decided by",
      latency: "in module",
      result: "result",
      resultAllow: "clean",
      reasonLine: {
        allow: "passed",
        deny: "blocked by",
        denyModule: "blocked by the module via",
        redirect: "redirected by",
        redirectModule: "redirected by the module via",
        score: "score",
        local_list: "a list",
        local_rate: "a rate limit",
        fail_timeout: "a wave timeout",
        fail_absent: "a silent inspector",
        fail_bus: "a bus failure",
        fail_body: "an unavailable body",
        fail_inspector: "an inspector failure",
        fail_overload: "an overloaded inspector",
        ws_close: "a frame policy, connection closed with Close {close}",
      },
    codes: {
        score: "scoring",
        local_list: "blocked by list",
        local_rate: "internal {status}",
        inspector: "inspector {name}",
        fail_timeout: "wave timeout",
        fail_absent: "no inspector answered",
        fail_bus: "bus failure",
        fail_body: "body unavailable",
      },
      decisive: "decisive",
      profile: "profile",
      clean: "clean",
      orphan: "off the list",
      rewriteApplied: "response rewrite applied",
      rewriteWouldApply: "response rewrite not applied",
      connection: "whole connection",
      connectionActive: "connection: clear filter",
      frameDir: {
        c2s: "client → server",
        s2c: "server → client",
      },
      frameSeq: "frame #{seq}",
      frameFragment: "fragment",
      frameRewritten: "rewritten",
      previewSessionNone: "A session record has no preview: it sums up the connection, the content is on its frames.",
      previewSent: "As delivered: an inspector rewrote the body; the record shows what was sent, the original is in the archive.",
      session: {
        c2s: "client → server",
        s2c: "server → client",
        denied: "frames denied",
        rewritten: "frames rewritten",
        close: "close",
        duration: "duration",
      },
      actions: "Auto actions",
      actionAll: "everyone",
      actionPassive: "not delivered: sender is passive",
      actionUnheard: "no rule at the receiver",
      actionNotAsked: "the receiver was not asked on this phase",
      actionRoute: "route",
      actionModule: "executed by the module",
      actionModuleRejected: "not executed: sender is passive",
      actionCol: {
        from: "Sender",
        to: "Receiver",
        do: "Ask",
        apply: "About",
        code: "Reason",
        amount: "Amount",
        outcome: "Outcome",
      },
      outcome: {
        applied: "applied",
        no_rule: "no rule",
        no_weight: "no weight in the profile",
        no_counter: "no counter for this axis",
      },
      noInspectors: "No inspectors were involved.",
      sessions: "Sessions",
      markers: "Markers",
      sessionAnonymous: "no login",
      sessionCol: {
        identity: "Identity",
        kind: "Kind",
        by: "Named by",
        id: "Id",
        groups: "Groups",
        notes: "Notes",
      },
      sessionKind: {
        own: "own",
        jwt: "JWT",
        app: "application",
      },
      sessionUnverified: "signature not verified",
      sessionPassive: "sender is passive",
      noContent: "No content.",
      emptyContent: "Empty.",
      truncated: "module did not read it all",
      clipped: "first {size} shown",
      binary: "Not text — base64 shown.",
      previewNote:
        "A preview, not the archive: authorization, cookie, proxy-authorization and the route's own deny list are excluded.",
      previewSecrets: "· without authorization, cookie or the route's deny list",
      previewOff: "The preview is off on this route.",
      previewHeadersNone: "There were no headers.",
      previewHeadersFiltered:
        "Headers arrived but none made the preview — likely all denied by `deny=` on `waf_preview headers`, or the budget ran out on the first pair.",
      previewArgsNone: "There were no query parameters.",
      previewArgsFiltered:
        "The query string arrived but did not fit the preview — the budget ran out on the first pair.",
      previewBodyNone:
        "No body in the preview: either there was no body, or the route never names a body preview — it is off by default, and then the body is not read for the record.",
      previewPairTruncated: "value trimmed by the per-pair ceiling",
      previewDropped: "{count} pairs dropped: the name took more than half the per-pair ceiling.",
      inPreview: "in preview",
      ip: {
        reason: {
          denylist: "denylist",
          geo: "geo",
          inverse: "outside the list",
          unknown: "unknown profile",
        },
      },
      seekHint: "search for this across all records",
      noStore: "The original was never stored.",
      archived: "original in the archive",
      archivedUntil: "original in the archive until {date}",
      stored: "original in the exchange ({store})",
      released: "original already released by the exchange",
      view: "view",
      showOriginal: "view",
      hideOriginal: "collapse",
      fullData: "full data",
      sourcePreview: "preview",
      sourceArchive: "archive",
      archiveStatus: {
        none: "not stored",
        empty: "stored · empty",
        emptyUntil: "empty · until {date}",
        saved: "stored",
        savedUntil: "stored until {date}",
        released: "already released",
      },
      modsec: {
        foundIn: "in {place}",
        span: "{offset}+{length}",
        header: "header {name}",
        cookie: "cookie {name}",
        target: {
          uri: "uri",
          args: "args",
          body: "body",
          conn: "conn",
        },
        family: {
          init: "init",
          exceptions: "exceptions",
          method: "method",
          scanner: "scanner",
          protocol: "protocol",
          protocolAttack: "protocol",
          multipart: "multipart",
          lfi: "LFI",
          rfi: "RFI",
          rce: "RCE",
          php: "PHP",
          generic: "attack",
          xss: "XSS",
          sqli: "SQLi",
          session: "session",
          java: "Java",
          inbound: "threshold",
          leak: "leak",
          sqlError: "SQL error",
          javaError: "Java error",
          phpError: "PHP error",
          iisError: "IIS error",
          nodeError: "Node error",
          outbound: "outbound",
          correlation: "correlation",
          rule: "rule",
        },
      },
    },
    reason: {
      absent: "Was not stored.",
      discarded: "No address: the object was not kept in the exchange.",
      ttl_expired: "The object was deleted when its retention ran out.",
      expired: "Retention has run out.",
      store_disabled: "The archive for this kind is not open to search.",
      driver_unsupported: "Not in the archive — search cannot reach it.",
      store_error: "The exchange answered with an error.",
      store_unconfigured: "No exchange configured on this route.",
      oversize: "Over waf_body_limit — not stored.",
      streaming_not_supported: "Streaming bodies are not stored.",
      transform_failed: "Masking failed.",
      empty: "The object was required and turned out empty — nothing was written.",
      overload: "The archiving queue was full — the PUT was never tried.",
      archive_error: "Moving to the archive failed.",
    },
  },
  fleetPage: {
    title: "Status monitoring",
    blurb:
      "The whole cluster: stores, agents with workers, inspectors and services. Agent↔worker join is by node_id. Redis and S3 are shared stores, not nodes.",
    cpu: "CPU",
    memory: "Memory",
    load: "Load",
    cores: "cores",
    uptime: "Uptime",
    rps: "rps",
    rpsWindow: "RPS · 10 s",
    rpsFleet: "Fleet RPS · 10 s",
    rps2xx: "2xx",
    rps3xx: "3xx",
    rps4xx: "4xx",
    rps5xx: "5xx",
    rpsCodes: "Codes · 10 s",
    rpsTooltipIdle: "No requests in the last 10 s",
    routeNodes: "nodes in window: {n}",
    routeSilent: "No requests in the last 10 s",
    rpsTotal: "Total",
    rps200: "200",
    rps200Hint: "success",
    rps300: "300",
    rps300Hint: "redirects",
    rps400: "400",
    rps400Hint: "client",
    rps500: "500",
    rps500Hint: "server",
    ops: "op/s",
    secondsShort: "s",
    ms: "ms",
    flow: "Work",
    flowWindow: "{n} s window",
    flowOps: "Rate",
    flowIn: "Read",
    flowOut: "Write",
    flowErr: "Errors",
    flowLatency: "p50 / p95 / max",
    flowIdle: "No operations in the window",
    flowChannel: {
      audit: "Audit",
      archive: "Archive",
      inspect: "Inspect",
      consume: "Consume",
      insert: "Insert",
      log: "Logs",
      log_consume: "Consume logs",
      log_insert: "Insert logs",
      cmd: "Commands",
      api: "Requests",
      probe: "Probe",
    },
    sectionHost: "Host",
    sectionTraffic: "Traffic",
    sectionWork: "Work",
    sectionWorkers: "Workers",
    sectionCounters: "Counters",
    sectionMetrics: "Metrics",
    instance: "Instance",
    sectionInstances: "Instances",
    sectionAbout: "Details",
    loadHint: "1 / 5 / 15 min",
    hitRate: "Hit rate",
    process: "Process",
    sync: "Delivery",
    syncOk: "in sync",
    syncStale: "stale",
    version: "version",
    role: "role",
    policy: "eviction",
    error: "error",
    endpoint: "endpoint",
    region: "region",
    apply: "apply",
    hash: "hash",
    confHash: "live conf",
    status: "Status",
    seen: "Seen",
    noWorkers: "No workers yet — the module does not publish presence.",
    up: "up",
    degraded: "degraded",
    ok: "ok",
    warning: "warning",
    danger: "danger",
    store: "Redis",
    storeS3: "S3",
    storeSection: "Store",
    objects: "{count} objects",
    objectsLabel: "Objects",
    buckets: "Buckets",
    bucket: "Bucket",
    agent: "Agent",
    nodesSection: "Nodes",
    keys: "{count} keys",
    keysLabel: "Keys",
    clients: "Clients",
    service: "Service",
    servicesSection: "Services",
    inspectorsSection: "Inspectors",
    inserted: "Inserted",
    lastBatch: "Last batch",
    clickhouse: "ClickHouse",
    clickhouseDown: "ClickHouse down",
    countries: "Countries",
    asns: "ASN",
    skipped: "Skipped",
    fingerprint: "Fingerprint",
    serviceName: {
      logger: "Logger",
      geo: "Geocoder",
    },
    disk: "Disk",
    restarts: "{n} restarts / h",
    bus: "Bus",
    evicted: "Evicted · 1h",
    evictedHint: "data loss",
    lag: "Lag",
    lagHint: "JetStream pending",
    skewHint: "Producer clock differs from the controller",
    wafBar: "WAF",
    wafBarHint: "Verdicts over the window: allow / challenge / deny / fail-open",
    wafAllow: "Allow",
    wafDeny: "Deny",
    wafChallenge: "Challenge",
    wafDeadline: "Deadlines",
    wafFailOpen: "Fail-open",
    wafFailOpenHint: "uninspected",
    wafFailClosed: "Fail-closed",
    wafDenyShort: "deny",
    wafFailShort: "fail",
    state: {
      ok: "ok",
      er: "fault",
      dr: "drift",
    },
    errorsBlock: "Errors",
    storesSection: "Stores",
    bar: {
      fleet: "fleet",
      fleetHint: "Members up out of everyone the controller sees",
      errors: "errors",
      errorsHint: "On {n} members · jump to the first",
      errorsQuiet: "quiet",
    },
  },
  api: {
    name_taken: "This name is already taken.",
    address_taken: "This entry already exists.",
    invalid_address: "Not an address:",
    invalid_content_type: "Choose a content type.",
    invalid_mode: "A list needs mode active or internal.",
    ttl_internal: "ttl= is only valid on active lists.",
    entries_active: "Active-list addresses are not written into nginx.",
    default_taken: "default_server is already set on another server.",
    ssl_mismatch: "A server cannot mix TLS and non-TLS ports.",
    port_taken: "This port is already bound to the server.",
    kind_taken: "This certificate kind is already bound to the server.",
    unknown_certificate: "Certificate not found.",
    unknown_upstream: "This server pool is not in the space.",
    order_stale: "The path list has changed. Reload and try again.",
    invalid_order: "Invalid path list.",
    upstream_bound: "The server pool is used by paths. Unbind them first.",
    in_use: "The object is in use — remove the references first:",
    fast_path_gated:
      "The profile gates by groups, but the gate call on its routes sits under an “if” condition: the local layer answers on its own and never checks the groups. Drop the condition first:",
    default_required: "default cannot be deleted: the manifest does not build without it.",
    default_is_required: "default cannot be deleted: the manifest does not build without it.",
    default_name_locked: "The default profile cannot be renamed: a route without profile= looks it up by that name.",
    not_default: "Only the default profile can be restored.",
    baseline_files_gone: "None of the shipped rule files are left in the catalog:",
    builtin_locked: "A built-in object is locked: it is a shipped template. Get your own variant by copying.",
    builtin_name_locked: "A built-in object cannot be renamed: it is referenced by name.",
  },
  exception: {
    title: "What to do when there is no verdict",
    hint:
      "The wave ended without a usable verdict: the inspector stayed silent, replied with garbage, the bus did not answer, or the exchange did not hand over the object. The route decides the outcome, not the inspector.",
    edit: "Edit exceptions",
    none: "as above",
    pass: "let through",
    deny: "deny",
    response: "Deny page",
    responseHint:
      "A waf_deny_response entry: its status, type and page are what the client sees. Empty — a bare 503 with no catalog entry.",
    responseNone: "bare 503",
    inherit: {
      pass: "as above (default: let through)",
      deny: "as above (default: deny)",
    },
    class: {
      timeout: "No answer in time",
      absent: "Nobody to ask",
      bus: "Bus",
      body: "Object unavailable",
      inspector: "Inspector could not check",
      overload: "Inspector is overloaded",
    },
    classHint: {
      timeout: "The phase budget (waf_deadline) ran out, or an active inspector timed out. A malformed reply lands here too: for the wave it is indistinguishable from silence.",
      absent: "No subscriber on this phase's subject — the inspector is not running or the name is wrong. Known at once, no need to wait for the deadline.",
      bus: "The bus did not answer, the publish failed, or the wait slots ran out (waf_max_inflight).",
      body: "The exchange neither returned nor accepted the object: a store failure, exhausted body holds, a failed lift of a rewrite.",
      inspector: "The inspector answered “could not check”: unknown profile, a schema version mismatch, a neighbouring service it could not reach. It used to pick for the route itself — answering allow or deny.",
      overload: "The inspector shed the request at the door: its queue is full, or the budget expired before the work began. Nothing is broken — in a minute the same request would pass — which is why this class is separate from “could not check”.",
    },
    phase: {
      request: "Before the application: the client is waiting, anything can be the answer.",
      response: "The application's response is already in hand; a deny here replaces a ready answer.",
      frame: "WebSocket frames, both directions. A deny closes the connection.",
    },
  },
  common: {
    apply: "Apply",
    edit: "edit",
    hide: "hide",
    create: "Create",
    refresh: "Refresh",
    uploadData: "Upload data",
    cancel: "Cancel",
    close: "Close",
    add: "Add",
    save: "Save",
    send: "Send",
    upload: "Upload",
    download: "Download",
    reset: "Reset",
    locked: "locked",
    restore: "Restore",
    delete: "Delete",
    enabled: "Enabled",
    disabled: "Disabled",
    name: "Name",
    filter: "Filter",
    yes: "yes",
    no: "no",
    none: "—",
    default: "default",
    inherit: "inherit",
    inherited: "inherited",
    defaultOf: "{value} (default)",
    presets: "pick a value",
    on: "on",
    off: "off",
    more: "advanced",
    hint: "hint",
    helpMore: "More in the help",
    blockedAdd: "To add: {what}.",
    blockedSave: "To save: {what}.",
    back: "Back",
    next: "Next",
    skip: "Skip",
    ellipsis: "…",
  },
  modal: {
    errorTitle: "Not saved",
    dirtyTitle: "Close without saving?",
    dirtyText: "Changes in this window will be lost.",
    discard: "Close without saving",
  },
  table: {
    loadingTitle: "Loading",
    loading: "Data is still loading.",
    errorTitle: "Error",
    error: "Failed to load data.",
    retry: "Retry",
    emptyTitle: "No data",
    empty: "Nothing here yet.",
    noneTitle: "No matches",
    none: "No rows match this filter.",
    deleteAsk: "“{name}” will be gone from the space. This cannot be undone.",
    copyOff: "Copy is not available",
    deleteOff: "Delete is not available",
    expand: "Expand",
    collapse: "Collapse",
  },
  pager: {
    range: "{from}–{to} of {total}",
    page: "{page} / {pages}",
    pageSize: "per page",
    auto: "Auto",
    prev: "Previous",
    next: "Next",
    first: "First page",
    last: "Last page",
  },
  errors: {
    noSpace: "No space — API did not respond.",
  },
  compileErrors: {
    datasetsTooMany:
      "{count} lists are declared in nginx, but the module accepts at most {limit} — nginx -t would fail on every edge, so the configuration does not compile. Take some off nginx or delete them (datasets_too_many).",
  },
  copyModal: {
    button: "Copy",
    profileTitle: "Copy profile",
    sourceTitle: "Copy source",
    hint: "A new object with the same content. It lives its own life: later edits of the original do not follow.",
    name: "Copy name",
    nameHint: "Unique within the space. For a deny page it is also the waf_deny_response catalog record name.",
    submit: "Copy",
  },
  datasets: {
    title: "Data",
    blurb:
      "Space catalog: lists for `waf_local_dataset` and files that reach the edge whole — deny pages, forms, contract specifications.",
    description: "Description",
    active: "active",
    activeHint:
      "Keeper holds the entries: deltas travel on `waf.sets.<name>`, without an nginx reload. Module autobans land there too.",
    internal: "internal",
    internalHint: "Addresses in the form become nginx lines. Changing them reloads nginx.",
    limit: "limit",
    limitHint: "Entry cap of the shm slot. Same as `limit=` on `waf_local_dataset`.",
    listTtl: "list ttl",
    listTtlHint:
      "Active overlay: `ttl=5m`. An add without its own ttl uses this. Empty — live add without ttl is rejected.",
    mode: "Mode",
    static: "internal",
    dynamic: "active",
    linkedHint: "Composite sets that include this list",
    linkedExclude: "exclude",
    ttl: "TTL, sec",
    ttlHint: "0 — permanent. Otherwise the entry expires on its own.",
    ttlNone: "forever",
    kind: "Kind",
    kindHint: "List — addresses in shm. Content — a single file.",
    type: "Type",
    typeHint: "Type is chosen at creation and cannot be changed later.",
    hash: "hash=md5",
    hashHint:
      "The dataset stores md5 of its values instead of the values: sessions and tokens sit neither in shm nor in the database in clear text, and no entry is longer than 32 bytes. Checks, auto-bans, keeper and the panel hash the value themselves; enter raw values here. String lists only; inspector mirrors compare raw values and do not read such a set.",
    hashLocked: "Changes only on an empty dataset: clear the entries first.",
    contentType: "Content type",
    file: "File",
    records: "Size",
    value: "Value",
    tabTable: "Table",
    tabRaw: "Raw",
    emptyEntries: "This list has no entries yet.",
    emptyContent: "No file yet — upload one or paste text.",
    placeholderString: "value",
    placeholderNumeric: "42",
    placeholderIpv4: "203.0.113.0/24",
    placeholderIp: "2001:db8::/32",
    helperString: "A single free-form string.",
    helperNumeric: "An integer.",
    helperIpv4: "IPv4 or IPv4/mask. Bare address becomes /32.",
    helperIp: "IPv4 or IPv6 CIDR. Bare address becomes /32 or /128.",
    newTitle: "New data",
    editTitle: "Data",
    text: "Contents",
    addTitle: "Add entry",
    uploadTitle: "Upload data",
    uploadHint: "One value per line. Blank lines and # comments are skipped.",
    uploadCount: "Lines: {count}",
    deleteAria: "delete",
    closeAria: "close",
    empty: "No lists yet — create the first one.",
    emptyPages: "No files yet — create the first one.",
    builtinHint:
      "Shipped with the release and fully locked: not editable, not deletable, not renamable. Get your own variant with Copy in the table row.",
    builtinNameHint:
      "A built-in page is named after its waf_deny_response catalog entry — that is how try_files finds it. Locked.",
    builtinListHint:
      "Shipped with the release: cannot be deleted or renamed. Entries, limit and mode are editable.",
    builtinListNameHint:
      "The preset's name is locked — it shipped with the release and presets refer to it. Entries and mode are editable.",
    previewTitle: "Preview",
    previewHint:
      "The draft with variables substituted — what the client will see. Rendered by the panel: include and further SSI logic are out of scope.",
    previewScenario: "Scenario",
    previewJsonBad: "Not valid JSON after substitution — client code does not expect such a response.",
    previewCase: {
      network: "Network closed + retry",
      address: "Address closed",
      country: "Region closed",
      asn: "AS closed",
      retry: "Retry only (rate limit)",
      silent: "All params locked",
    },
    contentNameHint:
      "Also the file name on the node. A deny page must be named like its catalog record (blocked, suspicious); other files carry their own name",
    kinds: {
      list: "List",
      content: "Content",
    },
    types: {
      string: "String",
      numeric: "Numeric",
      ipv4: "IPv4",
      ip: "IPv4 + IPv6",
    },
  },
  lists: {
    description: "Description",
    newTitle: "New list",
    editTitle: "List",
    nameHint: "Like CRS: letters, digits, dot, dash, underscore",
    text: "Text",
    empty: "No lists yet — create the first one.",
  },
  rulesEditor: {
    open: "Editor",
    openHint:
      "Open in the ModSecurity editor: highlighting, rule builder, semantic checks",
    openNew: "Create the set first: the editor writes straight to the server",
    openEmpty: "The profile has no sets — add at least one",
    titleFile: "Rule set",
    titleProfile: "Profile rules",
    loading: "Reading the files of the set…",
    clean: "everything saved",
    changed: "unsaved files: {count}",
    orderChanged: "file order changed",
  },
  inspectorCatalog: {
    copyOff: "A catalog record is not copied: it belongs to the process, not the space.",
    deleteOff: "A catalog record is not deleted: it ships with the release.",
    editTitle: "Inspector",
    nameHint:
      "Process name — fixed by the process itself. Names for routes and second names with profiles are declared in settings, the Inspectors section.",
    subject: "Bus subject",
    phase: "Phases",
    phaseValue: {
      request: "request",
      response: "response",
      frame: "frames",
    },
    description: "Description",
    descriptionHint: "Free text for the catalog. Does not affect compile or the bus.",
    logLevel: "Log level",
    logLevelHint:
      "The nginx error_log dictionary without emerg. Reaches the process with the next send of its channel and applies without a restart; until the first generation the environment variable holds. The inspector itself only writes debug/info/warn/error: notice cuts the same as warn, crit and alert silence the log entirely.",
    instances: "Running",
    docsOpen: "Open the documentation",
    docsNone: "No documentation yet",
    logsLink: "Process logs",
    conf: "inspector.conf",
    confHint:
      "Process-local queue: queue_max, queue_full, queue_expand. Not printed into nginx.conf.",
    uploadHint: "Fill the text from a file",
    empty: "The catalog is empty: process records arrive with the delivery, they are not created by hand.",
  },
  certificates: {
    copyOff: "No copy: the key material lives in the secret store, upload a second pair instead.",
    sans: "SAN",
    subject: "Subject",
    notAfter: "Valid until",
    validFrom: "Valid from {date}",
    expired: "expired",
    expiresIn: "{days} days left",
    fingerprint: "Fingerprint",
    type: {
      label: "Type",
      server: "Server TLS",
      client_ca: "mTLS: client root",
      serverHint:
        "A certificate and private key pair for ssl_certificate. This is what nginx presents to the client.",
      client_caHint:
        "A trusted root for ssl_client_certificate: nginx verifies client certificates against it. The CA private key neither needs nor may be uploaded here.",
    },
    kind: {
      label: "Role",
      server: "ssl_certificate",
      client_ca: "ssl_client_certificate",
      trusted: "ssl_trusted_certificate",
    },
    crl: {
      column: "Revocation",
      none: "no CRL",
      revoked: "revoked: {count}",
      thisUpdate: "Issued: {date}",
      nextUpdate: "Valid until: {date}",
      upload: "upload revocation list",
      replace: "replace revocation list",
      remove: "remove revocation list",
      title: "Revocation list — {name}",
      file: "CRL",
      blurb:
        "The certificate revocation list (CRL) in PEM. Encrypted in the browser, same as a certificate. nginx uses it to reject clients with revoked certificates: ssl_crl sits next to ssl_client_certificate.",
      current: "Currently loaded: {count} revoked, valid until {date}.",
      removeTitle: "Remove revocation list",
      removeConfirm:
        "Remove the CRL from “{name}”? Revoked certificates will start passing verification again.",
    },
    errors: {
      fingerprint_mismatch:
        "The contour public key fingerprint does not match the one pinned at build time. Upload cancelled.",
      not_a_ca:
        "This is not a CA: the certificate has no BasicConstraints CA:TRUE. mTLS needs a root or intermediate certificate, not a leaf.",
      invalid_crl:
        "The file does not parse as a CRL. It looks like a certificate was chosen instead of a revocation list.",
      crl_issuer_mismatch:
        "This CRL was issued by a different issuer — it does not belong to this root and would verify nothing.",
      crl_needs_client_ca: "A revocation list can only be attached to an mTLS root.",
      key_mismatch: "The private key does not match this certificate.",
      key_required: "Server TLS requires a private key.",
      key_not_allowed:
        "An mTLS root must not carry a private key — only the public half belongs in the contour.",
      invalid_certificate: "The file does not parse as a PEM certificate.",
      undecryptable: "The envelope could not be opened with the contour key.",
      crypto_unavailable: "The crypto service is unavailable: nothing can parse the certificate.",
    },
    empty: "No server certificates yet — upload the first one.",
    emptyCa:
      "No mTLS roots yet. Upload the CA that client certificates should be verified against.",
    deleteAria: "delete",
    deleteTitle: "Delete certificate",
    deleteConfirm: "Delete “{name}”? Deletion is rejected if it is bound to a server.",
    newTitle: "Upload certificate",
    uploadBlurb:
      "The certificate and key are encrypted in the browser with the contour public key before reaching the controller. It only ever sees ciphertext.",
    certFile: "Certificate",
    caFile: "CA certificate",
    keyFile: "Private key",
    chainFile: "Chain",
    chainOptional: "optional",
    noFile: "no file chosen",
    upload: "Encrypt and upload",
    cryptoUnavailable: "Contour key unavailable: certificate upload is disabled.",
    pinMismatch:
      "The contour public key fingerprint does not match the one pinned at installation — upload disabled. The controller's key may have been swapped.",
    noPin:
      "No fingerprint pin in this installation (contour-pin.json). Verify manually: {fingerprint}",
  },
  profiles: {
    lockedDelete: "The default profile is not deleted: the space refers to it.",
    dataFiles: "Data files",
    dataFilesHint:
      "Sets from Data → Files that a rule reads with @pmFromFile, @pmf, @ipMatchFromFile. The name in the rule is the set name plus the type extension: sqli_words of type text is seen as sqli_words.txt. Order here means nothing: the file is found by name.",
    defaultModified:
      "The default profile has been changed: it no longer matches what shipped. The button restores it — your edits are lost.",
    defaultNameHint:
      "The default profile cannot be renamed: a route without profile= looks it up by that name",
    restoreMissing: "Shipped files missing from the catalog:",
    description: "Description",
    lists: "Lists",
    newTitle: "New profile",
    editTitle: "Profile",
    nameHint: "This name goes to waf_inspector_profile: letters, digits, dot, dash, underscore",
    sectionProfile: "Profile",
    sectionProfileHint: "Name and description: the name is what a route selects",
    sectionIncludeHint:
      "Rule sets and the order they are included in: rules are read top to bottom, drag to reorder",
    sectionChannel: "Actions channel",
    sectionChannelHint:
      "A pair: signals are what we accept from neighbours, rules are what we say and do once the phase is decided",
    includeOrder: "Include order",
    emptyOrder: "Empty for now — add lists below.",
    addList: "Add list",
    chooseSet: "Select a set",
    drag: "drag",
    removeList: "remove",
    needLists: "Create sets on the Sets page first.",
    empty: "No profiles yet — create the first one.",
    sent: "Version {rev}, {hash}",
  },
  ipCatalog: {
    type: "Type",
    description: "Description",
    records: "Prefixes",
    search: "Address",
    none: "No rows match this filter.",
  },
  ipCountries: {
    code: "Code",
    empty: "No geo sets in this space yet.",
    noAddresses: "No prefixes in this set.",
  },
  ipAsns: {
    asn: "ASN",
    empty: "No ASN sets in this space yet.",
    noAddresses: "No prefixes in this set.",
  },
  geoImport: {
    title: "Upload data",
    kindCountry: "country database",
    kindAsn: "ASN database",
    hintCountry:
      "A MaxMind DB file (.mmdb) with countries — GeoLite2-Country or compatible. The space catalog is reconciled with the file: new networks are added, missing ones are removed. The geo coder downloads a copy and switches to it, answering from the previous one until the new one is ready.",
    hintAsn:
      "A MaxMind DB file (.mmdb) with autonomous systems — GeoLite2-ASN or compatible. The space catalog is reconciled with the file: new networks are added, missing ones are removed. The geo coder downloads a copy and switches to it, answering from the previous one until the new one is ready.",
    pick: "Choose file",
    noFile: "No file chosen",
    submit: "Upload",
    statusImport: "Upload",
    statusFile: "Dataset",
    statusCoder: "Coder",
    phaseCatalog: "{type}: networks are moving from the file into the catalog",
    phasePublish: "Catalog written, the coder is being told",
    failed: "failed",
    fileTitle: "{type} · {size} · uploaded {uploaded} · sha256 {sha}",
    coderNone: "none",
    coderNoneTitle: "The geo coder is not on the air: no presence frames",
    coderUnpublished:
      "The coder does not know about the file yet: policy/geo did not reach KV, the controller retries",
    coderBehind:
      "Not every coder replica is on this file: downloading it or still on the previous dataset",
    coderOnFile: "Every coder replica answers from this dataset",
    done: "{type} built {build} uploaded: {networks} networks, {added} added, {removed} removed.",
    errors: {
      file_empty: "The file is empty.",
      file_too_large: "The file is larger than {max}.",
      file_unreadable: "The file did not arrive in full: the connection broke.",
      mmdb_invalid: "The file is not a readable MaxMind DB.",
      mmdb_unsupported: "This is neither a country nor an ASN database.",
      mmdb_kind_mismatch: "This is a {got}, but this page takes a {want}.",
      mmdb_empty: "The file has no networks of this kind.",
      import_running: "An upload of this kind is already running.",
      catalog_failed: "The catalog was not written: {detail}",
    },
  },
  geo: {
    unknown: "Geo coder has no data for this address",
    unreachable: "Geo coder did not answer",
  },
  actionProfiles: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    sentHint: "Version sent:",
    empty: "No profiles.",
    description: "Description",
    rulesCount: "Rules",
    newTitle: "New auto action profile",
    editTitle: "Auto action profile",
    nameHint: "[a-z_][a-z0-9_-]*. The name is what profile= on a route refers to",
    sectionProfile: "Profile",
    sectionProfileHint:
      "This inspector checks nothing and has no verdict: it looks at the route and tells the neighbours — with channel actions — and may add or remove points on the route. Which paths that covers is decided by the route the inspector is attached to via profile=.",
    sectionConditions: "Conditions",
    conditionsHint:
      "Named conditions the rules refer to: «if A», «unless A». A condition is a list of rows `value · comparison · dataset or text` joined with AND or OR; a row may also be another condition («holds» / «fails»), which gives any nesting. Values are the path, method, host, address and request fields from the message, headers, cookies and arguments from the object store; datasets are the active lists of the space only.",
    conditionsEmpty: "No conditions — the rules fire on every request of the profile.",
    condName: "Name",
    condNameHint: "For example from_ads. Rules pick the condition by this name in «When»",
    condNameBad: "Latin letters, digits and _ . -; starts with a letter, digit or _",
    condNameTaken: "A condition with this name already exists",
    condFixName: "fix the condition name",
    condNeedName: "name the condition",
    condPurpose:
      "A condition is a named check of the request. On its own it does nothing: a rule picks it in «When» — «if …» or «unless …».",
    condCol: "Condition",
    condAnd: "and",
    condOr: "or",
    condMode: "Holds when",
    condModeAll: "all rows match (AND)",
    condModeAny: "any row matches (OR)",
    condModeHint: "With a single row either will do",
    clauseKind: "Kind",
    clauseKindValue: "value",
    clauseKindCond: "condition",
    clauseOp: "Comparison",
    condRefPick: "pick a condition",
    condRefGone: "not in the profile",
    opIs: "holds",
    opIsNot: "fails",
    addCondition: "Add a condition",
    editCondition: "Edit the condition",
    condInUse: "Condition «{name}» is used by rules or other conditions ({n}) — detach it from them first.",
    condNoDataset: "not an active list",
    condNoLists: "no active lists",
    clauseNeedRow: "add a row with «+» in the table",
    clauseNeedValue: "row {n} — set the value",
    clauseNeedText: "row {n} — type the text",
    clauseNeedList: "row {n} — pick a list",
    clauseNoLists:
      "row {n} — the space has no active lists (active mode): compare with a text (= or ≠) or create such a list in «Data → Lists»",
    clauseNeedCond: "row {n} — pick a condition",
    condAlert:
      "A row compares a value from the request with a text (`=` / `≠`) or with an active list of the space (`in` / `not in`). Example: `$uri` `=` `/logout` — the logout request.\n\nNo value means the row does not match: without the argument `=` and `in` fail, while `≠` and `not in` hold. A row may also be another condition of the profile — that is how AND inside OR is built. Conditions that refer to each other in a cycle are not saved.",
    clauseOperand: "Dataset or text",
    clauseText: "text",
    clauseNewDialog: "New condition row",
    clauseKindHint: "Value compares a request variable with a text or a list; condition refers to another condition of the profile",
    clauseCondHint: "Another condition of this profile",
    clauseOpHint: "= and ≠ compare with a text, in and not in with an active list; a condition row holds or fails",
    clauseList: "List",
    clauseListHint: "An active list of the space",
    clauseTextLabel: "Text",
    clauseTextHint: "What the value is compared with",
    addClause: "Add a row",
    clausesEmpty: "No rows — the condition will not be saved.",
    when: "When",
    whenAlways: "always",
    whenIf: "if {name}",
    whenUnless: "unless {name}",
    whenHint: "Always, or when a condition of the profile holds or fails.",
    whenNoConditions: "The profile has no conditions — the ask goes on every request. Conditions are declared in the «Conditions» section.",
    sectionRules: "Rules",
    rulesHint:
      "Asks to neighbours, the route's record and points on the route — on every request of the profile or by condition. No terminal rules: every row fires, in its order.",
    rulesEmpty: "No rules: the inspector stays silent.",
    to: "To",
    toHint: "The receiving inspector, the route's record or points on the route. This inspector never sends an ask without an addressee.",
    toModule: "the route's record",
    verb: "What to do",
    verbHint: "The list depends on the receiver: each has its own input actions.",
    params: "Parameters",
    amountShort: { delta: "by {n}%", value: "counter {n}%" },
    code: "Code",
    codeHint: "[A-Z][A-Z0-9_]*. Empty — no code",
    listCodeHint: "[A-Z][A-Z0-9_]*. Reason on the dataset entry; empty — ACTION_LIST",
    addRule: "Add a rule",
    editRule: "Edit the rule",
    removeRule: "Remove the rule",
    unknownTargets: "Targets missing from the registry",
    noRegistry: "The action dictionary did not arrive: ask selectors are empty.",
  },
  cookieProfiles: {
    lockedDelete: "The default profile cannot be deleted: without it the manifest does not build.",
    sentHint: "Version sent:",
    empty: "No profiles.",
    description: "Description",
    rulesCount: "Rules",
    newTitle: "New cookie profile",
    editTitle: "Cookie profile",
    nameHint: "[a-z_][a-z0-9_-]*. The name is what profile= refers to in the inspector registry",
    sectionProfile: "Profile",
    sectionProfileHint: "Issues and drops a cookie; blocks nothing itself",
    sectionCookies: "Cookies",
    cookiesHint: "What the cookie is: lifetime, value, signature",
    cookiesEmpty: "No declarations — nothing to issue or drop.",
    addCookie: "Add cookie",
    editCookie: "Edit cookie",
    cookieNameHint: "[A-Za-z0-9_.-]. The cookie name at the client; the local layer sees it by the same name",
    cookieNameTaken: "A cookie with this name is already declared",
    cookieInUse: "Rules ({n}) refer to cookie \"{name}\" — detach them first.",
    cookieAlert:
      "The value is built as `tag~random.time.signature`. The tag is what the cookie is kept for: the referral source, the campaign, the test branch; it is read by humans and travels into the record marker, so anything outside [A-Za-z0-9_-] collapses into an underscore and is cut at the limit. The signature is on by default and holds up everything that is later decided by this cookie: without it the value is typed by hand in the browser console in a second. The signing key belongs to the process (WAF_COOKIE_SECRET) and is the same across replicas; with no key the inspector answers error instead of quietly issuing an unsigned cookie.\n\nRenewal (renew_after) lives in the signed half and has to be shorter than the cookie lifetime, or the browser drops it before we get to renew it.\n\nSecure, HttpOnly and SameSite are not set here: the route sets them (waf_cookie_defaults). The module never sets a domain — the cookie is bound to the server name.",
    path: "Path",
    pathHint: "Cookie path; dropping goes by the same path",
    maxAge: "Lifetime",
    maxAgeHint: "30d, 12h, 90m. Empty — a session cookie",
    session: "session",
    sign: "Signature",
    signHint: "Forgery is only visible on a signed cookie",
    signs: { hmac: "HMAC", none: "none" },
    renew: "Renew after",
    renewHint: "Age after which the cookie counts as stale; empty — never renew",
    renewShort: "renew {ttl}",
    value: "Value",
    valueFrom: "Tag source",
    valueFromHint: "$arg_utm_source, $http_referer, $cookie_<name>. Empty — no tag from the request",
    valueDefault: "Default",
    valueDefaultHint: "[A-Za-z0-9_-]. The tag when the source is empty",
    valueDefaultShort: "else {text}",
    valueRandom: "Random",
    valueRandomHint: "bytes, 0..32",
    valueRandomShort: "+{n} bytes",
    valueMaxLen: "Tag limit",
    valueMaxLenHint: "1..128",
    valueEmpty: "No source, no default, no random — there will be no value, and the profile will not save.",
    sectionConditions: "Conditions",
    conditionsHint: "Optional: checks for «When» in the rules",
    condInUse: "Rules or other conditions ({n}) refer to \"{name}\" — detach them first.",
    sectionRules: "Rules",
    rulesHint: "When to issue or drop the cookie and whom to tell",
    rulesEmpty: "No rules — the cookie is issued to no one.",
    addRule: "Add rule",
    editRule: "Edit rule",
    opIssue: "issue",
    opDrop: "drop",
    opAdd: "add",
    opRemove: "remove",
    cookie: "Cookie",
    cookieHint: "Which cookie: a declaration from the section above",
    cookieAny: "not chosen",
    cookieOnly: "{name} (the only one)",
    cookieOfRule: "the rule's cookie: {name}",
    noCookies: "No declarations — add a cookie in the Cookies section",
    phase: "Phase",
    phaseHint: "Where the rule runs; empty — both",
    phaseBoth: "both",
    phases: { request: "request", response: "response" },
    stateAbsentHint: "\"No cookie\" together with issuing is the first touch: only a client without one gets it",
    when: "When",
    whenNoConditions: "The profile has no conditions — the rule runs on every request. Conditions are declared in the Conditions section.",
    to: "To",
    toHint: "Self — issue or drop the cookie; or a receiving inspector, the route record, score, a dataset write",
    toModule: "route record",
    verb: "Action",
    verbHint: "The list depends on the receiver: each has its own incoming actions.",
    params: "Parameters",
    amountShort: { delta: "by {n}%", value: "counter {n}%" },
    writes: {
      addr: "address",
      net: "network",
      net_all: "all networks",
      asn: "autonomous system",
      cookie: "cookie value",
    },
    listOp: "Operation",
    listOpHint: "Add to the dataset or remove from it; removal takes no ttl",
    listCookie: "Whose cookie",
    listCookieHint: "Empty — the rule's cookie",
    code: "Reason",
    codeHint: "[A-Z][A-Z0-9_]*. Empty — no reason",
    listCodeHint: "[A-Z][A-Z0-9_]*. The reason recorded in the dataset; empty — COOKIE_LIST",
    phasesAt: { request: "on request", response: "on response" },
    sectionWhenHint: "What the client presented and on which request",
    sectionDoHint: "What to do with the cookie or whom to tell",
    triggers: {
      always: "always",
      absent: "no cookie",
      present: "cookie present",
      expired: "cookie present, due for renewal",
      invalid: "signature failed",
      tag: "cookie tag = …",
    },
    triggerTag: "tag = {tags}",
    triggerHint: "What the client presented: whether the cookie is there, whether it is ours, what its tag is",
    tags: "Tags",
    tagsHint: "The readable part of the value: google in google~1a2b… Any one will do; Enter or a comma adds a tag",
    tagsBad: "Tag «{tag}»: Latin letters, digits, _ and - only",
    andIf: "And if",
    andIfNone: "no condition",
    andIfHint: "A profile condition on the request: path, arguments, headers",
    toSelf: "self — cookie",
    selfHint: "Issue or drop the cookie chosen under «When»",
    needTags: "type at least one tag",
    needTagsFix: "fix tag «{tag}»",
    needCookieDecl: "declare a cookie in the Cookies section",
    needTarget: "choose the addressee",
    needCookie: "pick the cookie",
    needOverloadAt: "the overload threshold is {min}–{max} %",
    needOverloadAsk: "an overload row has no cookie — choose whom to tell",
    needPoints: "points are a whole number from 1 to {max}",
    needList: "pick a dataset",
    needTtl: "set the lifetime: 30d, 12h, 90m",
    needVerb: "choose what to do",
    needPercent: "percent is a whole number within the hint",
    needGroup: "name the group",
    needMarker: "check the marker",
    needRecord: "check the record fields",
    needCode: "the reason is capital Latin letters, digits and _",
    unknownTargets: "Targets missing from the registry",
    noRegistry: "The actions dictionary did not arrive: the request selectors are empty.",
  },
  vlaiProfiles: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    sentHint: "Version sent:",
    empty: "No profiles.",
    description: "Description",
    overload: "Overload",
    overloadHint:
      "The process queue is full. “Allow” — answer allow right away, fail-open; “wait” — stay queued until the deadline; “deny” — refuse right away, for routes where unchecked text costs more than a refusal. Queue depth stays a process property (inspector.conf in the catalog).",
    overloads: { wait: "wait until deadline", shed: "answer at once" },
    newTitle: "New classifier profile",
    editTitle: "Classifier profile",
    nameHint: "[a-z_][a-z0-9_-]*. The name is what profile= on a route refers to",
    sectionProfile: "Profile",
    sectionProfileHint:
      "The model reads the description in the body and answers a 0..100 score. The deny threshold stays with the route (waf_score_deny); the profile decides the mode, the full-queue behaviour and the correspondence with neighbours.",
    priorHint:
      "Whose asks to apply: threshold — a coefficient on the score we hand over, skip — do not check the request (skips inference). Both weaken, so the sender is always named.",
    outcomesHint:
      "What to do ourselves: on a score against a threshold — and on overload, when the request was shed at the door because the queue was full. An ask to a neighbour or the client address written into a live set; prefixes and systems are written by those with geo.",
    unknownTargets: "Targets missing from the registry",
    noRegistry: "The action dictionary did not arrive: ask selectors are empty.",
  },
  rewrite: {
    lockedDelete: "The default profile cannot be deleted: the manifest needs it.",
    sentHint: "Generation sent:",
    empty: "No profiles.",
    description: "Description",
    denyResponse: "Deny response",
    denyResponseHint:
      "A catalog entry name: the page is printed by the module, so the name must exist there. Only meaningful with \u00abrefuse\u00bb.",
    newTitle: "New response rewrite profile",
    editTitle: "Response rewrite profile",
    nameHint: "[a-z_][a-z0-9_-]*. This name is what profile= refers to",
    sectionProfile: "Profile",
    sectionProfileHint:
      "The inspector edits the upstream response before it reaches the client: the process rewrites the body into the exchange, and the module lifts it at the response-phase seam. Patterns run in Go (RE2) only; nothing from the profile reaches nginx.conf.",
    groups: "Modifier groups",
    groupsHint:
      "A group is the unit of switching: its own conditions and its own operations. An enabled group always applies, a disabled one never does — until a neighbour's request (mutate) flips its state.",
    groupsBlockHint: "Operations and conditions live behind the gear",
    groupsCol: "Groups",
    groupsEmpty: "No groups — this profile changes nothing",
    groupName: "Name",
    groupNameHint: "[a-z_][a-z0-9_-]*. Prior rules refer to this name",
    groupDefault: "Default",
    groupDefaultHint:
      "The state the profile brings to the response. Only a neighbour's request (mutate) flips it, and only one a prior rule below accepts",
    defaultOn: "enabled",
    defaultOff: "disabled",
    condCol: "Conditions",
    condHint: "Status code and content type; empty means any",
    opsCol: "Operations",
    anyStatus: "any status",
    anyType: "any type",
    bodyShort: "body",
    headersShort: "headers",
    groupDialog: "Modifier group",
    groupNewDialog: "New modifier group",
    groupOn: "Applies to",
    groupOnHint:
      "Response — the application response body; frames — the payload of a WebSocket frame in either direction, frame by frame. A frame group has its own conditions and no headers",
    groupOns: { response: "response", frame: "WebSocket frames" },
    frames: "frames",
    direction: "Side",
    directionHint: "c2s — from the client, s2c — from the application. Empty — both",
    anyDirection: "both sides",
    directions: { c2s: "client", s2c: "application" },
    opcode: "Opcode",
    opcodeHint: "Empty — text frames only: a pattern over a binary frame is enabled explicitly",
    opcodes: { text: "text", binary: "binary", continuation: "continuation" },
    status: "Status codes",
    statusHint: "Type a code and press Enter, or pick a preset. Empty — any status",
    contentType: "Content type",
    contentTypeHint:
      "application/json, text/html; \u00ab+json\u00bb matches the suffix (application/problem+json). Empty means any type",
    bodyOps: "Body",
    bodyOpsHint: "RE2 patterns; operations apply in the order listed",
    bodyEmpty: "No operations",
    bodyOpNewDialog: "New body operation",
    op: "Operation",
    opHint:
      "Replace puts the template in place of a match; remove cuts the match out; insert before or after adds text next to the match and keeps it",
    ops: {
      replace: "replace",
      remove: "remove",
      insert_before: "insert before",
      insert_after: "insert after",
    },
    pattern: "Pattern",
    patternHint:
      "RE2 (the Go standard library): no backreferences or lookaround, but linear time on any input",
    replacement: "Replacement / text",
    replacementHint:
      "For replace a template with $1..$9, for inserts the literal text. Remove has no such field",
    maxMatches: "Matches",
    maxMatchesHint: "Cap on matches per request; empty means the loader default (256)",
    headerOps: "Response headers",
    headerOpsHint:
      "Length, encoding and Host stay with the module — a profile never touches them",
    headersEmpty: "No operations",
    headerName: "Header",
    headerValue: "Value",
    headerSet: "set",
    headerUnset: "unset",
    headerOpNewDialog: "New header operation",
    headerOpHint:
      "Set replaces the header value or adds the header; unset drops the header from the response",
    priorHint:
      "Whose requests to honour: mutate switches the group the sender named (group and mode travel in its ask), skip drops the rewrite entirely. Both weaken the masking, so the sender is always named.",
    noRegistry: "The action registry did not arrive: request pickers are empty.",
  },
  channel: {
    signals: "Early-wave signals",
    rules: "Rules",
    unknownSenders:
      "Rules reference senders missing from the registry -- their asks will not arrive",
  },
  prior: {
    hint: "A neighbour's action applies only through a rule here; without one the ask shows up in the audit and nowhere else. Both verbs can weaken, so the sender is always named",
    from: "Sender",
    fromAny: "any",
    fromAnyLong: "any sender",
    fromHint:
      "The inspector's declaration name from the registry -- what it writes into from on the wire. \"Any\" accepts from everyone, but hardening actions only",
    accept: "Accepted verbs",
    acceptHint:
      "Verbs the rule applies from this sender. Its other asks stay in the audit",
    codes: "Reasons",
    codesAny: "any",
    codesHint:
      "Sender reasons the rule fires on; empty -- any. Suggestions list every reason declared by sender profiles along with who declared it, and the search matches both. The list is incomplete: file-based profiles are invisible to the controller, so your own reason is typed by hand",
    apply: "About what",
    applyAny: "any axis",
    applyHint:
      "Whom the ask is about -- this request, the address, the session. Empty -- any axis the chosen verbs allow",
    counter: "Bucket",
    counterPick: "pick one",
    counterHint:
      "Where accepted notes go: a fill: note bucket of this profile. The name does not travel on the wire; the rule names it",
    addSignal: "Add signal",
    editSignal: "Edit signal",
    dialogHint: "From whom, which asks and on which reasons to apply",
    broadcastWeakens:
      "A rule without a named sender may only accept hardening actions: weakening from anyone is how a single compromised inspector undoes everyone else's findings. Name the sender",
    empty: "No rules -- the profile listens to nobody, and that is the usual state",
  },
  outcomes: {
    hint: {
      request:
        "What to do once the request phase is decided: ask a neighbour or write a subject into a live dataset. The condition is the decision itself, not its premises",
      response:
        "The same for the response phase. An ask does travel: prior spans phases, and later response waves see it",
      rules:
        "What to do once the request is decided: ask a neighbour or write a subject into a live dataset. The threshold is compared against the score we hand over",
    },
    sectionWhen: "Trigger",
    sectionWhenHint: "what pulls this row",
    sectionDo: "What to do",
    sectionDoHint: "to whom and what, once the trigger fires",
    outcomeWhen: "When",
    outcomeWhenHint: "This inspector's own verdict",
    outcomeCompare: "Comparison",
    compare: {
      above: "score at least",
      below: "score below",
      eq: "score equals",
      levelAbove: "filled at least",
      levelBelow: "filled below",
    },
    outcomeBucket: "Counter",
    outcomeBucketHint: "Own bucket: the row fires on its level",
    outcomeBucketAxis: "Axis",
    outcomeLevelAt: "Threshold, %",
    outcomeLevelAtHint: "0-100 — percent of the bucket, same scale as judge rules",
    outcomeAt: "Threshold",
    outcomeAtHint: "Compared against the score we hand over",
    outcomeTo: "To",
    toModule: "the route's record",
    toScore: "Points on the route",
    toRoute: "the route",
    outcomeToHint: "A receiving inspector, or a dataset write",
    outcomeToDenyHint: "No asks on deny: it ends the phase, and the ask has nowhere to go",
    outcomeToDenyRouteHint:
      "On deny an ask cannot reach a neighbour: a dataset write and the route's log and archive remain — the module executes those",
    outcomeWhat: "What",
    outcomeWrite: "Write to dataset",
    outcomeWriteLabel: "Subject",
    outcomeWriteHint: "The inspector takes subnets and the system from geo and writes them in one batch",
    writes: {
      addr: "address",
      net: "subnet: the narrowest announce",
      net_all: "every covering announce, wider foreign ones included",
      asn: "the whole system: all of its announces",
    },
    outcomeParams: "Parameters",
    outcomeList: "Dataset",
    outcomeListHint: "Active ones only: a passive dataset has no subject",
    listEmpty: "No active datasets",
    outcomeTtl: "TTL",
    outcomeTtlHint: "1h, 15m, 7d",
    outcomeCode: "Reason",
    outcomeCodeHint: "Optional; empty means the decision code",
    outcomesEmpty: "No initiators -- the inspector decides and stays quiet",
    overloadAt: "Queue threshold, %",
    overloadAtHint:
      "How full the inspector's queue must be for the row to fire, 25–100 %. Empty means 100. Below the threshold the row stays silent. From the threshold up to the edge the inspector checks as usual and the request gets a real verdict — the row's action rides along with it: the point of a threshold is to shed before the queue overflows. 100 is the edge only: the request has already been shed, there is no verdict, the module carries out only its own verbs, a list write is still done by the inspector, and what happens to the request itself is decided by the route's “inspector is overloaded” exception",
    outcomeWhere: "Where",
    outcomeWhereHint: "The row's phase: by the verdict on the request or on the frame",
    outcomeRules: "Rules",
    outcomeRulesHint: "Ids and ranges: 942100, 942000-942999. The fields narrow each other; at least one is required",
    outcomeRulesBad: "An id or a range: 942100, 942000-942999",
    outcomeTags: "Tags",
    outcomeTagsHint: "Exactly as the engine reports them: attack-sqli, paranoia-level/1. The event card shows them",
    addOutcome: "Add initiator",
    editOutcome: "Outcome initiator",
    toDataset: "Write to dataset",
    toAll: "Everyone",
    toAny: "everyone",
    ons: {
      deny: "Deny",
      allow: "Allow",
      score: "Score",
      level: "Counter level",
      overload: "Inspector overloaded",
      rule: "Rule matched",
    },
  },
  counter: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    empty: "No counter profiles yet",
    newTitle: "New counter profile",
    editTitle: "Counter profile",
    description: "Description",
    nameHint: "Profile name: referenced by profile= in the inspector registry",
    sentHint: "Sent, fingerprint:",
    phases: "Phases",
    request: "Judge",
    response: "Measure",
    judgeCount: "Judge rules",
    measureCount: "Metrics",
    sectionProfile: "Profile",
    sectionProfileHint: "Name and description: the rest is phase rules. The call mode lives on the route",
    sectionProfiles: "Profiles",
    sectionCounters: "Counters",
    countersEmpty: "No counters yet — declare one: profiles have nothing to reference otherwise",
    copyOff: "A counter is not copied: the name is the tally key, and a copy would count the same thing apart",
    deleteAsk: "The declaration of counter “{name}” will be gone. Filled buckets cool down on their own.",
    usedByHint: "Profiles whose rules name this counter",
    profileOff: "off",
    sites: {
      judge: "judge",
      measure: "measure",
      outcome: "outcome",
      prior: "accept",
    },
    inUse: "Counter is in use — clear the references first:",
    lastCounter: "The last counter is not removed: with no declarations the inspector counts nothing and the channel stalls",
    newCounter: "New counter",
    editCounter: "Counter",
    counterNameHint: "The name keys the count in Redis: profiles reference it, renaming resets the accumulated",
    unit: "Unit",
    unitHint: "Label for the panel and audit: obj, kb. Does not affect arithmetic",
    fill: "Filled by",
    fillHint:
      "A bucket has one input: either the inspector's own response measurements or neighbours' signals (note). Judging works the same for both",
    fills: {
      measure: "measurements",
      note: "neighbour signals",
    },
    axes: "Axes",
    axesLabel: "bucket capacity and cooling rate",
    axesHint: "A bucket per axis × subject pair: capacities differ — a network tolerates more than an address",
    axisCol: "Axis",
    axis: {
      ip: "Address",
      asn_net: "Network (ASN)",
      asn_router: "System (ASN)",
      sess: "Session",
      user: "User",
      conn: "Connection",
    },
    max: "Capacity",
    maxHint: "In counter units; profile thresholds are written as its percentage",
    loss: "Loss, %/s",
    axesEmpty:
      "No axis is on: this counter has nothing to count. Turn on the one that gets judged — address, session, user",
    axisTierMissing:
      "No capacity on these axes: {axes}. The bucket has nothing to measure threshold percentages against, and neither the controller nor the inspector accepts such a declaration — set a capacity or turn the axis off",
    lossHint: "Percent of capacity per second, fractions allowed. A quiet subject cools in 100/loss seconds: 1 → ~1.5 min, 0.1 → ~17 min, 0.01 → ~2.8 h — the slower, the longer it remembers",
    subjects: "Key sources",
    sessCookie: "Session cookie",
    sessCookieHint: "The sess axis keys off its (hashed) value. Defaults to the captcha clearance cookie; no signature check",
    userFrom: "User axis source",
    userFromHint:
      "What the bucket keys off: a client cookie or header is a stable key but not an identity; an identity is what the gate said about the client. Empty disables the axis",
    userFromKinds: {
      none: "not set",
      cookie: "client cookie",
      header: "client header",
      session_user: "identity: login",
      session_sid: "identity: session",
    },
    userFromName: "Cookie or header name",
    userFromNameHint: "The account key is the hash of its value, not the value itself",
    userSourceMissing:
      "The user axis is on, but no key source is set — neither its own nor the shared one. Neither the controller nor the inspector accepts such a declaration: the bucket exists with no key for it",
    identityHint:
      "The login and the session are named by the gate (auth): it must run on this route and before the counter — a lower wave number on the request phase. The client cannot forge them, and cookie rotation does not change the key",
    ownSubjects: "Own key sources",
    ownSubjectsHint: "empty — the shared source",
    ownSessCookie: "Session axis cookie",
    ownSessCookieHint:
      "The counter keys off this cookie: its (hashed) value is the account key. Empty — the shared source; cookie names live in the captcha and gate profiles",
    ownUserFrom: "User axis source",
    ownUserFromHint: "The same for this counter; empty — the shared source from “Key sources”",
    priorHint:
      "A neighbour's action applies only via a rule here. skip lifts the judging, not the accounting: 'don't check' is not 'don't remember'. " +
      "note fills the named bucket with percent of its capacity: plus adds, minus drains, −100 clears; the charge lands on the response phase, judging reads it from the subject's next request",
    sectionRequest: "Request phase: judge",
    sectionRequestHint:
      "Bucket levels against thresholds: the strictest wins — deny over score, higher score over lower",
    sectionResponse: "Response phase: measure",
    sectionResponseHint:
      "Rules turn a response into bucket charges; the phase always answers allow — a meter does not judge",
    sectionPhases: "Phases",
    sectionPhasesHint:
      "Where the counter works and which deny responses it uses. Metering, judging and outcomes below are one table each across phases: a row carries its own Where column",
    sectionMeasure: "Metering",
    sectionMeasureHint:
      "Rules turn a response or a frame into bucket charges; the metering verdict is always allow — a meter does not judge. Where — the application response or WebSocket frames (both sides)",
    sectionJudge: "Judging and outcomes",
    sectionJudgeHint:
      "Bucket levels against thresholds: the stricter one wins — deny over score, a higher score over a lower. On a frame the judge reads the levels in the same trip as the meter: the frame that fills a bucket to its threshold is denied itself",
    phaseRequest: "request",
    phaseResponse: "response",
    phaseFrame: "frames",
    frameDenyResponse: "Deny response for frames",
    where: "Where",
    whereHint: "The row's phase: response and frame rules share one table, the document splits them into sections itself",
    whereMeasureHint: "The application response (selectors — status, type, method) or WebSocket frames (side and opcode). Foreign selectors are dropped when a row moves",
    whereJudgeHint: "Request or WebSocket frames. The connection axis exists only on frames",
    sectionFrame: "WebSocket frames: meter and judge",
    sectionFrameHint:
      "Every frame in both directions: measure rules charge buckets, judge rules read the levels in the same round trip — the frame that fills a bucket to the threshold is denied itself. A deny closes the connection with a Close frame from a type=websocket entry",
    frameEnabledHint: "A disabled phase answers allow with a reason: frames are neither counted nor judged",
    frameDenyResponseHint: "A type=websocket catalog entry: the Close frame code and reason. Default — ws_policy",
    frameOutcomesHint: "On the frame verdict: ask a neighbour or write the subject into a live set",
    anyFrame: "any frame",
    condDirection: "Side",
    condDirectionHint: "c2s — from the client, s2c — from the application. Empty — both",
    condOpcode: "Opcode",
    condOpcodeHint: "text, binary, continuation. Empty — any",
    directions: { c2s: "client", s2c: "application" },
    opcodes: { text: "text", binary: "binary", continuation: "continuation" },
    phaseEnabled: "Phase enabled",
    judgeEnabledHint: "A disabled judge answers allow with a reason: accounting continues",
    measureEnabledHint: "A disabled measure charges nothing: buckets cool down on their own",
    judge: "Judge rules",
    judgeHint: "A row is a rule: counter, axis, fill threshold and the action. No subject — the rule stays silent",
    judgeEmpty: "No rules — the phase answers allow, and that is a normal state",
    judgeStair: "Ladder",
    judgeStairHint: "Levels of this pair by ascending threshold -- the order a subject walks them. The stricter one wins: deny over score",
    judgeDialog: "Level ladder",
    judgeNewDialog: "New level ladder",
    judgeLevels: "Levels",
    judgeLevelsHint: "The threshold is a percentage of the bucket: natural units differ per counter, a percentage reads the same everywhere",
    judgeLevelsEmpty: "No levels -- a pair without them is not saved",
    levelNewDialog: "New level",
    judgeCounterHint: "The bucket whose level is judged. Buckets are declared on the «Counters» tab",
    judgeAxisHint: "The subject whose count is compared with the threshold. Only axes declared on this bucket are offered",
    counter: "Counter",
    at: "At, %",
    atHint: "Bucket fill percentage: 0 fires on an empty bucket — the probe lives off this",
    action: "Action",
    actionHint: "Score adds to the phase total and the route threshold decides; deny answers with the profile deny record at once",
    actions: { score: "Score", deny: "Deny" },
    score: "Score",
    scoreHint: "1..100. Contribution to the phase sum; the route decides the threshold (waf_score_deny)",
    code: "Code",
    codeHint: "Machine reason for the audit and neighbours; empty — COUNTER_LEVEL",
    denyResponse: "Deny response",
    denyResponseHint: "Deny catalog entry name: nginx serves the code and the page. 429 is more honest than 403 — the client can wait it out",
    outcomesHint: "On the judge's decision: an ask to a neighbour or a subject written into a live set",
    measure: "Metrics",
    measureHint:
      "A row is a rule: which response, measured how, into which counter. All matching rules fire; condition and multiplier are behind the summary",
    measureEmpty: "No metrics — the phase counts nothing, and that is a normal state",
    measureDialog: "Condition and multiplier",
    measureNewDialog: "New metric",
    measureCounterHint: "The bucket this rule charges. Buckets are declared on the «Counters» tab",
    source: "Source",
    sourceHint: "What goes into the bucket: one, the number of pattern matches, or the body size in KB or bytes",
    sources: {
      const: "One per response",
      regex_count: "Pattern matches",
      size_kb: "Body size, KB",
      bytes: "Size, bytes",
    },
    regex: "Pattern",
    regexHint: "Counted over the body preview: truncation is a deliberate undercount, the flag rides into the audit",
    cond: "Condition",
    condHint: "Which responses to count: statuses, types, methods. Empty — any. Per-path behaviour is a different profile on a different route",
    anyResponse: "any response",
    condStatus: "Status codes",
    condStatusHint: "Type a code and press Enter, or pick a preset. Empty — any",
    condTypes: "Content types",
    condTypesHint: "'+json' is a suffix (application/problem+json). Empty — any",
    condMethods: "Methods",
    condMethodsHint: "Type a method and press Enter, or pick a preset. Empty — any",
    per: "Multiplier",
    perHint: "Applied to the source value; empty — one. Negative refunds: 'a good response returns credit'",
    measureAxes: "Charge axes",
    measureAxesHint: "Empty — all axes declared on the counter",
  },
  json: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    empty: "No specification profiles yet",
    newTitle: "New specification profile",
    editTitle: "Specification profile",
    description: "Description",
    nameHint: "Profile name: named by profile= in the inspector registry",
    sentHint: "Published, hash:",
    kind: "Contract source",
    kindHint: "OpenAPI describes whole calls; a plain schema describes the body only",
    kinds: { openapi: "OpenAPI", jsonschema: "JSON Schema" },
    source: "Specification",
    sourceHint: "A file from the data section: an OpenAPI document or a body schema",
    sourceEmpty: "The files section is empty",
    basePath: "Strip prefix",
    basePathHint: "Route prefix missing from the spec paths: /api. Prefixes from the spec servers[] are stripped anyway",
    phases: "Phases",
    request: "Request",
    response: "Response",
    phaseEnabled: "Phase is checked",
    checkBody: "Body",
    checkQuery: "Query params",
    checkPathParams: "Path params",
    checkHeaders: "Required headers",
    checkStatus: "Response status",
    checkContentType: "Body type",
    sectionSchema: "Schema",
    priorHint:
      "A neighbour's action applies only through a rule here; without one the ask shows up in the audit and nothing else. Both actions can weaken, so the sender is always named — no \"everyone\" here",
    priorFrom: "Sender",
    priorAccept: "What to accept",
    priorCodes: "Reasons",
    priorCodesAny: "any",
    priorEmpty: "No rules — the profile listens to no one, and that is a normal state",
    priorUnknownSenders:
      "Rules name senders absent from the registry — their asks will never arrive",
    sectionRequest: "Request phase",
    sectionResponse: "Response phase",
    sectionBindings: "Bindings",
    sectionLimits: "Limits and audit",
    actions: { deny: "Deny", score: "Score", allow: "Allow" },
    onlyTypes: "Parse these types only",
    onlyTypesHint: "Comma separated; \"+json\" is a suffix (application/problem+json)",
    score: "Score",
    scoreHint: "0..100. This outcome contribution to the phase sum; the threshold belongs to the route (waf_score_deny)",
    denyResponse: "Deny response",
    denyResponseHint: "Catalogue entry name: nginx serves the status and the page",
    bindingsHint: "Which schema applies to a call. First match wins; with no rules one schema covers the whole phase body",
    bindingMethods: "Methods",
    bindingPath: "Path",
    bindingMatch: "Match",
    bindingSchema: "Schema",
    bindingNewDialog: "New binding",
    bindingMethodsHint: "Type a method and press Enter, or pick a preset. Empty — any method",
    bindingPathHint: "Request path, starting with /",
    bindingMatchHint: "Exact — the whole path matches; prefix — the path starts with this string",
    bindingSchemaHint: "A schema file from the files section: it checks the body of these calls",
    matches: { exact: "Exact", prefix: "Prefix" },
    maxBody: "Body up to, bytes",
    maxBodyHint: "Larger bodies are not parsed: same outcome as truncated",
    maxDepth: "Nesting depth",
    maxDepthHint: "Guard against a document that kills the parser",
    maxErrors: "Findings in audit",
    cache: "Schema cache",
    auditValues: "Field values in audit",
    auditValuesHint: "Never leave in clear text",
    auditValuesOff: "Omit",
    auditValuesHash: "Hashed",
    auditPaths: "Finding paths",
    sectionProfile: "Profile",
    sectionProfileHint: "Name and description; the call mode lives on the route",
    sectionSchemaHint: "What describes the contract and what to strip off the path",
    sectionRequestHint: "What is checked in the request and what each outcome does",
    sectionResponseHint: "The same for the application response: its own policies",
    sectionLimitsHint: "What the inspector parses and what it writes to the audit",
    sourceNone: "— not selected",
    phaseEnabledHint: "A disabled phase answers allow without parsing anything",
    checkBodyHint: "Body against the operation schema",
    checkQueryHint: "Query string parameters. Requires waf_capture request args",
    checkPathParamsHint: "Path parameters: /orders/{id}",
    checkHeadersHint: "Required operation headers. Off by default: specs often require what the edge sets",
    checkStatusHint: "The response status is declared by the operation",
    checkContentTypeHint: "The body type is declared for this status",
    policy: "Outcome policy",
    policyHint: "A row is a check outcome. The score applies when the action is Score",
    outcome: "Outcome",
    action: "Action",
    actionHint: "Deny takes its page from the Deny response row. Score adds up with others and meets the route threshold",
    outcomes: {
      invalid: "Schema mismatch",
      unparsable: "Body is not JSON",
      truncated: "Body truncated",
      unknown_operation: "Call is not in the contract",
      content_type: "Body type is not described",
      unavailable: "The exchange did not return the body",
      status: "Response status not described",
      opcode: "Binary frame instead of text",
    },
    codes: {
      invalid: "JSON_SCHEMA_MISMATCH, JSON_TOO_DEEP",
      unparsable: "JSON_UNPARSABLE",
      truncated: "JSON_BODY_TRUNCATED, JSON_BODY_TOO_LARGE",
      unknown_operation: "JSON_UNKNOWN_OPERATION",
      content_type: "JSON_CONTENT_TYPE",
      unavailable: "JSON_BODY_UNAVAILABLE, JSON_ARGS_UNAVAILABLE",
      status: "JSON_STATUS_UNDECLARED",
      opcode: "JSON_FRAME_OPCODE",
    },
    maxErrorsHint: "How many violations reach the audit event; the rest are counted in one line",
    cacheHint: "Compiled schemas kept in process memory. Schemas compile when the profile loads, not per request",
    auditPathsHint: "Path to the violation inside the document: /items/3/price",
    bindingsEmpty: "No rules — one schema covers the whole phase body",
    anyMethod: "any",
    frame: "Frames",
    sectionFrame: "WebSocket frames",
    sectionFrameHint:
      "Socket messages after the upgrade. The schema is picked by direction and by message type — a field in the body; each direction has its own policies. Plain schemas only: OpenAPI has no socket messages",
    frameEnabled: "Frames are checked",
    frameEnabledHint:
      "Off — socket messages get allow without parsing. The route calls the inspector on its “Frame inspectors” line",
    frameBindings: "Message bindings",
    frameBindingsHint:
      "Which message gets which schema. Path is the handshake URI; direction and subprotocol come from the frame; the discriminator is the body field naming the type. First match wins; a message with no match is not in the contract",
    frameBindingsEmpty: "No rules — every message is “not in the contract”",
    frameBindingNewDialog: "New message binding",
    frameBindingPathHint: "WebSocket handshake path, starting with /",
    bindingDirection: "Side",
    bindingDirectionHint: "c2s — from the client, s2c — from the application, any — both",
    bindingSubprotocol: "Subprotocol",
    bindingSubprotocolHint: "Sec-WebSocket-Protocol from the application reply. Empty — any",
    bindingPointer: "Type field",
    bindingPointerHint:
      "JSON pointer to the message type field: /type. Empty — one schema for the whole direction",
    bindingValue: "Value",
    bindingValueHint: "The type field value this schema applies to",
    anySubprotocol: "any",
    directions: { c2s: "from client", s2c: "from app", any: "any" },
    direction: { c2s: "From the client (c2s)", s2c: "From the application (s2c)" },
    directionHint: {
      c2s: "Client messages are held until the verdict: a deny is a Close frame before the application sees the message",
      s2c: "Application output. The module does not parse this side yet: the policies are stored but do not run",
    },
    frameCheckBodyHint: "Frame payload against the message schema. Needs waf_capture frame:c2s body",
    frameDenyResponseHint:
      "Catalog entry of type=websocket: the Close code and reason. An entry of another type falls back to 1008",
    frameOutcomesHint: {
      c2s: "What to do on the client-frame decision: an ask to a same-wave neighbour or an address write into a set",
      s2c: "The same for the application output",
    },
  },
  captcha: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    primary: "Primary",
    fallback: "Fallback",
    noFallback: "no fallback",
    empty: "No captcha profiles yet",
    newTitle: "New captcha profile",
    editTitle: "Captcha profile",
    description: "Description",
    nameHint: "Profile name: referenced by profile= in the inspector registry",
    server: "Server",
    serverHint: "The widget address is chosen among this server's locations",
    path: "Widget address",
    pathHint:
      "A location backed by the captcha service: the redirect leads here, and the challenge and the check come from here whichever way the widget is shown. The captcha inspector may sit on it — it recognises its own address and lets it through",
    pathEmpty: "The server has no addressable locations",
    pathNone: "Add /waf/captcha proxied to the captcha service — the widget has nowhere to live",
    page: "Page",
    pageHint: "A file from the data section; empty — the built-in page from the image",
    pageEmpty: "No html files in the Files section",
    title: "Page title",
    note: "Note under the title",
    when: "When to require",
    whens: { always: "Always", buckets: "By buckets", never: "Never" },
    whenHint:
      "Always — everyone without a session; by buckets — when a bucket reaches its threshold; never — the session is checked but nobody is redirected",
    prior: "Signals from earlier waves",
    priorHint:
      "What we accept from the neighbours. Without a rule an ask does not apply at all. " +
      "Rules are unordered: any matching one fires. " +
      "Empty codes mean any; where a charge lands is named by the ask itself",
    priorFrom: "Sender",
    priorAccept: "What to accept",
    priorAcceptAll: "all actions",
    priorCodes: "Codes",
    priorCodesAny: "any",
    priorEmpty: "No rules — the profile listens to nobody",
    priorUnknownSenders: "Rules reference senders missing from the registry — their asks will never arrive",
    sectionProfile: "Profile",
    sectionProfileHint:
      "Name, mode and address: server, widget address, page and how it is shown. The address must match a server location",
    sectionGateHint:
      "Who gets the widget: the rest (POST, fetch, API) get the deny entry; API paths get their own profile without show methods",
    sectionClearanceHint:
      "Session cookie lifetime, network and browser binding, page fingerprint. A session spans the whole server",
    sectionExtraHint:
      "Page texts and the widget HTTP self-protection — process properties, not policy",
    pageBuiltin: "built-in page from the image",
    bucketKind: "Bucket",
    bucketMaxHint:
      "Dimensionless units; neighbours' percentages count against it. 0 disables the bucket",
    bucketLossHint:
      "The bucket always leaks; a quiet subject cools to zero in 100/loss seconds",
    bucketCaptchaAtHint:
      "% full — every request of the subject without a session goes to the widget; 0 counts but does not push",
    bucketBanAtHint:
      "% full — the \"bucket: ban threshold\" event for the rules; 0 disables the threshold",
    sectionBuckets: "Buckets",
    bucketsHint:
      "Shared subject score across all instances: neighbours fill it with counter asks, percent of the bucket capacity. A bucket leaks at its loss rate; no capacity — disabled",
    bucketCaptchaAt: "Captcha, %",
    bucketBanAt: "Ban, %",
    bucketBanNeedsList: "Banning needs a banned set in the Lists section",
    buckets: {
      ip: "Address",
      sess: "Session (past the widget)",
      asnNet: "ASN subnet",
      asnRouter: "Whole ASN",
    },
    bucketMax: "Capacity",
    bucketLoss: "Loss, %/s",
    sectionExtra: "Extras",
    sectionRules: "Rules",
    rulesHint:
      "What to do on events: a failed attempt, a passed check, a bucket at a threshold, a client with a valid clearance or without one. No hard-coded attempt loop or auto-ban — these rows decide. The clearance is the only good news the captcha tells its neighbours: drop the counter bucket, discount modsec, skip vlai. Its absence is the reason to switch the neighbours down the chain on: the classifier, the output mask, points",
    rulesEmpty: "No rules: failures heat nobody, a full bucket bans nobody",
    ruleWhen: "When",
    ruleWhenHint: "Fail and pass happen in the HTTP widget; bucket thresholds and the clearance, present or not, — on the wave, where asks and the route record are available",
    ons: {
      fail: "failed an attempt",
      pass: "passed the check",
      bucket_captcha: "bucket: captcha threshold",
      bucket_ban: "bucket: ban threshold",
      cleared: "client with a valid clearance",
      uncleared: "client without a clearance",
      overload: "inspector overloaded: the queue is at the threshold or above",
    },
    ruleNext: "What next",
    ruleNextHint:
      "What the captcha decided about the client on this request: let it through or showed the widget — by redirect, in the body or as a deny with a ticket. Under observation — what it would decide",
    nexts: {
      any: "any decision",
      allow: "let through",
      challenge: "showed the widget",
    },
    ruleBucket: "Which bucket",
    ruleBucketHint: "Empty — any that reached this threshold",
    bucketAny: "any",
    ruleTo: "To",
    ruleToHint: "A neighbour inspector, the route record, points, an own set or an own bucket",
    ruleToHttpHint: "An ask cannot travel: fail and pass are not on the wave. A set or a bucket",
    ruleToWidgetHint:
      "A neighbour down the chain, the route record, points, an own set or an own bucket. An ask travels only if the request goes on: the captcha let the client through or stands in vote — the widget ends the phase",
    ruleToChallengeHint:
      "The captcha showed the widget: an ask reaches a neighbour only if it stands in vote — in active the phase ends. The route record, points, an own set and an own bucket always work",
    toBucket: "self: change a bucket",
    toBucketShort: "into a bucket",
    toDataset: "nobody: write into a set",
    toDatasetShort: "into a set",
    toAll: "everyone",
    ruleCharge: "Bucket",
    chargeHint: "1..100 — share of capacity; five fails at 20% fill it up",
    ruleList: "Which set",
    ruleListHint: "A live set: the module cuts by it downstream",
    ruleWrite: "What to write",
    ruleWriteHint:
      "The /32 address, subnet, AS number; the clearance id — only where the cookie is known: on pass and for a client with a valid clearance",
    writes: {
      addr: "address",
      net: "subnet: the narrowest announce",
      net_all: "every covering announce, wider foreign ones included",
      asn: "the whole system: all of its announces",
      cid: "clearance id",
    },
    ruleTtl: "ttl, s",
    ruleTtlHint: "Entry lifetime; expired — the ban lifts",
    ruleVerb: "What to do",
    ruleParams: "Parameters",
    ruleCode: "Code",
    ruleCodeHint: "[A-Z][A-Z0-9_]*. Empty — the default code",
    amountPercent: "{n}%",
    addRule: "Add a rule",
    editRule: "Edit the rule",
    deliver: "How to show the widget",
    deliverHint:
      "A redirect sends the browser to the widget address (30x; behind a TLS terminator or on a non-standard port the address may drift). As the response body — the inspector fetches the page from the captcha service and answers on the same URI with the deny entry's status; the client never leaves, no catalog entries or locations needed",
    delivers: { redirect: "redirect", body: "response body" },
    redirectMethods: "Methods shown the widget",
    redirectMethodsHint:
      "Only these requests get the widget page — by redirect or as the body. The rest get the deny entry below: a request body would not survive the widget",
    htmlOnly: "HTML only",
    htmlOnlyHint:
      "The widget goes only to a request asking for a page: Accept holds text/html, application/xhtml+xml or */*. A GET from fetch/XHR after JSON — and a request with no Accept at all — get the deny entry below instead: widget markup where JSON was expected would break the client anyway. Off — every method listed on the left gets the widget",
    denyResponse: "Deny entry",
    denyResponseHint:
      "For those not shown the widget (POST, fetch, API): nginx serves the status and the body per waf_deny_response. captcha_required is machine-readable — an SPA shows the widget itself. The same status is used for the widget as the response body",
    clearanceCookie: "Clearance cookie",
    clearanceCookieHint: "Sealed “challenge passed” token. The name lives in the profile only",
    idCookie: "Id cookie",
    idCookieHint:
      "Short clearance id — the same value that goes into the live clearance dataset. The name is wired elsewhere too: the counter's sess axis reads the cookie by name, so renaming means editing that as well",
    challengeCookie: "Challenge cookie",
    challengeCookieHint: "The widget ticket: nginx and the counter never read it, rename freely",
    providers: "Providers",
    providersHint: "Order is display order: first on the page, the rest as fallback",
    provider: {
      image: "Image (built-in, no JS)",
      turnstile: "Cloudflare Turnstile",
      recaptcha: "Google reCAPTCHA",
      hcaptcha: "hCaptcha",
      smartcaptcha: "Yandex SmartCaptcha",
    },
    providerSlot: "Slot",
    providerSlotHint:
      "The primary widget is shown first; the fallback comes when the primary failed to load, there is no JS, or the provider is down",
    providerWidget: "Widget",
    providerSetup: "What is set",
    providerSetupHint:
      "Widget settings in one phrase: keys, version, what happens when the provider fails. Edited behind the gear",
    providerDialog: "Widget settings",
    imageLength: "Characters",
    imageLengthHint: "How many characters the image shows",
    audioDir: "Sample directory on the node",
    audioDirHint: "<char>.wav, 16-bit PCM mono; empty — no audio",
    imageAudio: "Audio variant",
    imageAudioHint: "A “listen” link on the page: for those who cannot see the image",
    sitekey: "Site key",
    secretEnv: "Secret: environment variable",
    secretStore: "Secret: store object (uuid)",
    secretHint: "One of the two; the store is an encrypted contour envelope",
    secretValue: "Provider secret",
    secretValueHint: "Encrypted in the browser with the contour key; only the object uuid lands in the profile",
    secretValuePlaceholder: "type and seal",
    sealSecret: "Seal",
    remoteip: "Send the client IP to the provider",
    remoteipHint: "The client address leaves to a third party",
    onError: "Provider unavailable",
    onErrorHint: "What to do when the provider does not answer in time",
    onErrors: { fallback: "next provider", allow: "let through", deny: "deny" },
    recaptchaVersion: "Version",
    recaptchaVersionHint: "v2 — “I am not a robot” by click, v3 — a score without a click",
    minScore: "Minimum score",
    minScoreHint: "A v3 answer below the score counts as a failure",
    sumChars: "characters: {n}",
    sumAudio: "audio: {dir}",
    sumAudioNoDir: "audio, no directory",
    sumNoAudio: "no audio",
    sumKeysOk: "keys set",
    sumKeysNone: "keys not set",
    sumNoSitekey: "no site key",
    sumNoSecret: "no secret",
    sumScore: "v3, threshold {score}",
    sumOnError: "on failure — {what}",
    sumRemoteip: "with client IP",
    sumNone: "nothing to set",
    clearanceTtl: "Session lifetime, s",
    binds: { subnet: "Subnet", ip: "Address (IP)", ua: "Browser (UA)" },
    bind: "Cookie binding",
    bindHint:
      "Sealed into the cookie: stolen from another network or browser it will not pass. Changing networks means the widget again. The address is stricter than the subnet, but a mobile client gets the widget on every address change",
    issuePerSubnet: "Issues per subnet",
    verifyPerSubnet: "Verifications per subnet",
    limitsHint: "Format 30/m; empty — no limit",
    listEmpty: "No active string lists",
    sectionList: "Live clearances",
    sectionListHint:
      "The captcha's source of truth: issuing adds an entry, the inspector checks it on every request, deleting the entry kills the clearance",
    clearanceList: "Live clearance dataset",
    clearanceListHint:
      "An active string list: the clearance id goes into it. Empty means the clearance rides on its signature alone and cannot be killed from outside",
    clearanceListNone: "None",
    clearanceGrace: "Entry arrival window, s",
    clearanceGraceHint:
      "The entry travels through the sequencer; a clearance younger than the window rides on its signature alone. 0 — the default of 2, 300 max",
    fingerprint: "Collect a fingerprint on the page",
    canvas: "Canvas hash",
    sentHint: "Version sent:",
    sectionChannel: "Actions channel",
    sectionChannelHint:
      "A pair: signals — what we accept from neighbours, rules — what we say and do ourselves on widget events",
    sectionGate: "Gate",
    sectionProviders: "Providers",
    sectionClearance: "Session",
  },
  auth: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    provider: "Password is checked by",
    providerHint: "One per source. Password plus TOTP is two sources and two gates on one route: a password gate and a code gate after it",
    after: "after {from}",
    sectionMode: "Where the session comes from",
    sectionModeHint:
      "The first choice a source makes: who issues the session. Everything else — the form, the cookie, the check — follows from it",
    mode: "Method",
    method: "Sign-in method",
    modes: {
      own: "Sign-in through the WAF",
      jwt: "Foreign JWT",
      app: "Application session",
    },
    modeHint: {
      own: "The contour shows the form itself, checks the password and issues the session cookie",
      jwt: "A foreign issuer minted the session as a token: the gate reads it and verifies the signature when it has a key",
      app: "The application issued the session as its own cookie: the gate trusts it once it has watched the login",
    },
    sectionToken: "Token",
    sectionTokenHint: "Where the token sits on the request: a cookie or a header with a scheme",
    sectionVerify: "Verification",
    sectionVerifyHint:
      "What proves the token is genuine: algorithm and key, issuer, audience, validity",
    sectionClaims: "Token fields",
    sectionClaimsHint:
      "Which claims to read: the issuer names them, here we say where to look. Dotted path — data.email",
    sectionAppCookie: "Application cookie",
    sectionAppCookieHint:
      "Which cookie the application issues to whoever signed in: the gate recognises the session by it",
    sectionLearn: "How the login is observed",
    sectionLearnHint:
      "The application's login address and the signs of an accepted login. The gate stands there on the response phase: it sees the login in the request and the cookie in the response",
    sectionAppLogout: "Logout",
    sectionAppLogoutHint:
      "The application's logout address: a successful response there drops the trust in the cookie. Empty — the entry lives until it expires",
    sectionTrusted: "Trusted sessions",
    sectionTrustedHint:
      "The set the observed sessions live in: an entry means the cookie is trusted, removing it ends the session",
    trustedSessions: "Trusted sessions set",
    trustedSessionsHint:
      "A live string set: the gate puts the cookie hash there along with the login. Without a set the method does not work",
    trustedTtl: "Trust lifetime, s",
    trustedTtlHint:
      "How long the cookie stays trusted after the observed login. 0 — the session lifetime",
    jwtCookieHint: "Name of the cookie the application keeps the token in",
    jwtAudienceHint: "Empty — do not check aud",
    jwtClaimGroupsHint: "Empty — groups are not read from the token",
    jwtClaimExpiryHint: "Token expiry; an expired one is not let through",
    jwtNoneNote:
      "Without a key the signature is not verified: the client wrote the token fields. This is attribution for the journal, not protection — such a session is marked in the records",
    learnCaptureNote:
      "The login route must capture the request body and the response headers, and the gate must stand on it on the response phase too. Without that there is no login to observe",
    appLogoutMethod: "Logout method",
    sourceSummaryExternal: "{provider}, no login page: no session means a refusal",
    sourceSummaryPage: "{provider}, login page {uri}",
    redirectMethodsExternal: "Methods shown the login page",
    redirectMethodsExternalHint:
      "These are sent to the application's login page, the rest are refused. Empty — everyone is refused",
    gateNoLoginPage:
      "The source has no login page: without a session every method is refused, there is nowhere to send them",
    identityFrom: "Identity from source",
    identityFromHint: "The first source of the chain: the code is checked for whoever signed in through it",
    identityFromEmpty: "No identity-establishing sources",
    codeUsers: "User set with TOTP secrets",
    codeUsersHint: "The same string list as for the local provider",
    empty: "No gate profiles yet",
    sourcesEmpty: "No login sources yet: the gate starts with one",
    newTitle: "New gate profile",
    editTitle: "Gate profile",
    newSourceTitle: "New login source",
    editSourceTitle: "Login source",
    description: "Description",
    nameHint: "Profile name: profile= in the inspector registry points at it",
    sectionProfile: "Profile",
    sectionProfileHint: "Name and description: the name is what a route selects",
    sectionSources: "Login sources",
    sectionProfiles: "Profiles",
    sectionSource: "Login source",
    sectionSourceHint:
      "Name and description. Profiles refer here with the source key, and profiles of one source accept each other's sessions",
    sourceNameHint: "Source name: profiles refer to it with the source key",
    source: "Login source",
    sourceHint:
      "Whose sessions the profile accepts and whose form serves it. Profiles of one source cooperate, of different ones are independent. Required in every mode but Off",
    sourceEmpty: "No login sources yet — create one on the neighbouring tab",
    sourceSummary: "{provider}, form {uri}",
    factor: {
      local: "Local list",
      code: "One-time code",
      ldap: "LDAP directory",
      ntlm: "NTLM domain",
      jwt: "Foreign JWT",
      app: "Application session",
    },
    externalNote:
      "No form or ticket for this method: the session is not issued by the contour. The login address below is the application's login page for navigation without a session; empty means 401 for everyone",
    sectionAppLogin: "Application login page",
    sectionAppLoginHint:
      "Where to send navigation without a session: a server location with the application's own login page. Empty — 401 for everyone, no redirect",
    appLoginPage: "Login page",
    jwtWhere: "Where the token lives",
    jwtWhereHint: "Exactly one place: a cookie or a header",
    jwtInCookie: "in a cookie",
    jwtInHeader: "in a header",
    jwtCookie: "Token cookie",
    jwtHeader: "Token header",
    jwtPrefix: "Scheme prefix",
    jwtPrefixHint: "Stripped case-insensitively: Bearer for Authorization; empty takes the value as is",
    jwtAlg: "Signature",
    jwtAlgHint:
      "The source names the algorithm, not the token header. none parses without verification: the client wrote the claims, and the journal marks such a session",
    jwtSecretEnv: "Environment variable with the secret",
    jwtSecretEnvHint: "The HMAC secret lives in the inspector's environment, never in the document",
    jwtKey: "Public key",
    jwtKeyHint: "PEM: PUBLIC KEY, RSA PUBLIC KEY or a certificate. Shipped as jwt.key next to the source",
    jwtIssuer: "Issuer",
    jwtIssuerHint: "Empty — do not check iss",
    jwtAudience: "Audience",
    jwtLeewayS: "Clock leeway, s",
    jwtLeewayHint: "Tolerated clock skew when checking exp and nbf",
    jwtClaimUser: "Login claim",
    jwtClaimsHint: "Token field names; defaults are sub, sid, iat, exp",
    jwtClaimSession: "Session id claim",
    jwtClaimSessionHint: "Absent — jti, then the token fingerprint",
    jwtClaimGroups: "Groups claim",
    jwtClaimExpiry: "Expiry claim",
    appCookie: "Application cookie",
    appCookieHint: "The application's session cookie. The list and the journal keep its hash, not the value",
    appLoginUri: "Application login address",
    appLoginUriHint:
      "Where the application accepts the login and password. The gate stands there on the response phase: the route must capture the request body and the response headers",
    appLoginMethod: "Login method",
    appLogoutUri: "Logout address",
    appLogoutUriHint: "Empty — logout is not observed, the entry lives until it expires",
    appUserFrom: "Login source",
    appUserFromHint: "From the login request: a form field, a JSON field, a query parameter or a header",
    appUserFroms: {
      body_form: "form field",
      body_json: "JSON body field",
      args: "query parameter",
      header: "header",
    },
    appUserField: "Field name",
    appUserFieldHint: "For JSON — a dotted path: user.email",
    appSuccessStatus: "Success statuses",
    appSuccessStatusHint: "Response statuses that mean the login was accepted",
    appCookieNew: "New cookie only",
    appCookieNewHint:
      "Learn only when the cookie in the response differs from the cookie in the request: an application that sets a cookie on a failed login would otherwise label an attacker's session with someone else's login",
    appJsonPath: "Response JSON field",
    appJsonPathHint: "A dotted path in the response body; empty — not checked. Needs a response body capture on the route",
    appJsonEquals: "Expected value",
    appListRequired: "This method needs a live sessions set: it is the set of trusted application cookies",
    factorHint: {
      jwt: "A foreign issuer minted the token: the gate reads it by a claims schema and verifies the signature when it has a key",
      app: "The application's cookie, trusted after the gate has watched the login: the login in the form and the response carrying that cookie",
      local: "Login and password from a controller user set",
      code: "TOTP from an authenticator app, or a shared access code",
      ldap: "The organisation directory checks the password with a bind",
      ntlm: "The domain controller checks the password, NTLM bind",
    },
    sectionLogin: "Login form",
    sectionLoginHint: "Where the form lives: the server, its waf-off location, the texts and your own markup",
    sectionSessionHint:
      "The sign-in cookie: ttl, renewal, binding to the network and the browser. The session is shared by every profile of the source",
    sectionListHint:
      "An active dataset through which a session is ended from outside: entry deleted, session gone",
    sectionUpstreamHint: "What the gate tells the application about the visitor: headers and the identity cookie",
    sectionExtra: "Extra",
    sectionExtraHint: "Protecting the form itself from brute force — a property of the process, not a policy",
    sectionGateHint:
      "Who gets the form: the rest get 401 with the deny entry",
    sectionGateExternalHint:
      "What a client without a session gets: the application login page, or a refusal",
    sectionAccess: "Access",
    sectionAccessHint:
      "Who gets in after signing in. Empty — the login itself is enough; with groups a stranger gets 403 without a second login",
    sectionChannel: "Action channel",
    sectionChannelHint:
      "Asks from neighbours: cut the session or let through without a login. Without a rule an ask shows up in the audit only",
    loginPageBuiltin: "built-in page from the image",
    listSessionsNone: "None",
    deliver: "How to show the form",
    deliverHint:
      "A redirect sends the browser to the source's form address (30x; behind a TLS terminator or on a non-standard port the address may drift). As the response body — the inspector fetches the form from the login service and answers on the same URI with the deny entry's status; the client never leaves, no catalog entries or locations needed",
    delivers: { redirect: "redirect", body: "response body" },
    binds: { subnet: "Subnet", ua: "Browser (UA)" },
    ldapBinds: { upn: "UPN suffix", dn_template: "DN template", search: "Directory search" },
    startTls: "STARTTLS",
    startTlsHint: "Upgrade ldap:// to TLS before the bind; ldaps:// is encrypted as is",
    timeoutS: "Timeout, s",
    upstreamUser: "Login header",
    upstreamGroups: "Groups header",
    upstreamMethod: "Sign-in method header",
    upstreamHeaderHint:
      "Name of the header the application receives; a client-forged one is stripped by the gate. Empty means none",
    lockoutAttempts: "Wrong passwords before lockout",
    lockoutAttemptsHint: "That many failures within the window lock the login for the term below. 0 — no lockout",
    lockoutWindow: "Counting window, s",
    lockoutLock: "Lockout term, s",
    server: "Server",
    serverHint: "A source belongs to a server: the form path is picked from its locations",
    loginUri: "Form path",
    loginUriHint:
      "A location of that server backed by the login service; listed in waf_redirect_allow. The gate may sit on it — it recognises its own address and lets it through",
    loginUriNone: "The server has no addressable locations — the login form has nowhere to live",
    loginTitle: "Title",
    loginNote: "Note",
    loginPage: "Form page",
    loginPageHint:
      "Your own markup from the Files section. Empty means the built-in one. A POST form and a csrf field holding {{.Nonce}} are required",
    sectionGate: "Gate",
    redirectMethods: "Methods shown the form",
    redirectMethodsHint:
      "Only these requests get the form — by redirect or as the body. The rest get 401 with the deny entry: a request body would not survive the form",
    htmlOnly: "HTML only",
    htmlOnlyHint:
      "The form goes only to a request asking for a page: Accept holds text/html, application/xhtml+xml or */*. A GET from fetch/XHR after JSON — and a request with no Accept at all — get 401 with the deny entry instead: form markup where JSON was expected would break the client anyway. Off — every method listed on the left gets the form",
    denyResponse: "Deny entry",
    denyResponseHint:
      "For those not shown the form (POST, fetch, API): nginx serves the status and the body per waf_deny_response. The same status is used for the form as the response body",
    gateGroups: "Allowed groups",
    gateGroupsHint:
      "Empty — the login itself is enough. Otherwise the visitor must belong to at least one: a stranger to the groups does not pass, yet no second login is needed either — the source session is shared. Groups travel in the session as taken at login: removing someone takes effect on their next login, or at once by revoking the session",
    forbiddenResponse: "Deny entry for a foreign group",
    forbiddenResponseHint:
      "Separate from the entry above: that one says «log in» (401), this one — «logged in, but not here» (403). Such a request is never sent to the form",
    sectionSession: "Session",
    sessionCookie: "Session cookie",
    sessionCookieHint:
      "The cookie is unique per source: the session space is glued by the source reference of profiles, not by matching names. Empty — waf_sid_<source name>",
    sessionTtl: "Session ttl, s",
    renewAfter: "Renew after, s",
    renewAfterHint: "0 disables renewal; the user logs in again when the ttl runs out",
    bind: "Binding",
    bindHint:
      "Sealed into the cookie: stolen from another network or browser it will not pass. Changing networks means the widget again",
    priorHint:
      "A neighbour's action applies only through a rule here; without one the ask shows up in the audit and nothing else. Rules are an allow-list: any match fires, there is no order and nothing to drag",
    reauthAfter: "Sign-in freshness, s",
    reauthAfterHint:
      "A younger session ignores the cut-session ask: someone who just signed in is not sent to the form in a loop. 0 — the ask always applies",
    sectionRules: "Rules",
    rulesSectionHint:
      "What the gate tells its neighbours and the route on its own: no captcha and no vlai for the signed-in, an anonymous client on a closed zone into a counter bucket or a set, a wrong group marked in the journal",
    rulesHint:
      "What to do on wave events: signed in, anonymous, broken session, wrong group. An ask to a neighbour travels only with «signed in» — deny and redirect end the phase; the other events keep the journal, archive, marker, points and a set write",
    rulesEmpty: "No rules: the gate only listens",
    ruleWhen: "When",
    ruleWhenHint:
      "The ladder event: about the client, not the verdict — under observation an anonymous client stays anonymous",
    ons: {
      authenticated: "signed in: the session holds and access is granted",
      anonymous: "anonymous: no cookie at all",
      invalid: "session invalid: expired, revoked, another network or source",
      forbidden: "signed in at the wrong door: not in the access groups",
      overload: "inspector overloaded: the queue is at the threshold or above",
    },
    ruleToHint: "A neighbour inspector, the route record, points or a live set",
    ruleToDenyHint:
      "An ask cannot travel: deny and redirect end the phase. The route record, points or a set",
    toAll: "everyone",
    addRule: "Add rule",
    editRule: "Rule",
    sectionList: "Live sessions",
    listSessions: "Live session dataset",
    listSessionsHint:
      "The gate's source of truth: sign-in adds an entry, the inspector checks every request against it, deleting the entry ends the session. Empty means sessions cannot be ended from outside",
    listSessionsEmpty: "No active string lists in this space",
    listCookie: "List cookie",
    listCookieHint:
      "A separate short cookie: the dataset holds the session id, not a token hundreds of bytes long. Must differ from the session and ticket cookies",
    listTtl: "Entry ttl, s",
    listTtlHint: "0 means the session ttl",
    listGrace: "Entry arrival window, s",
    listGraceHint:
      "The entry travels through the sequencer; a session younger than the window rides on its signature alone, otherwise sign-in would lose the race with its own entry. 0 — the default of 2, 300 max",
    sectionUpstream: "To the application",
    upstreamCookie: "Identity cookie",
    upstreamCookieHint: "Read by the application itself without gate headers. Empty means none",
    localUsers: "User list",
    localUsersEmpty: "No string lists in this space",
    usersEmpty: "The list is empty",
    localUsersHint: "A plain string list of the data section: login:bcrypt[:groups]",
    usersOpen: "Edit the users of this list",
    usersPick: "Pick a list first",
    userAddTitle: "New user",
    codeKind: "Code kind",
    codeKindHint: "TOTP takes the secret from the user; a shared code has no identity",
    codeTotp: "TOTP",
    codeStatic: "Shared code",
    codeCodes: "Code hashes",
    codeCodesHint: "bcrypt only: htpasswd -nBC 12",
    ldapUrl: "Directory url",
    ldapUrlHint: "ldaps://host:636 or ldap:// with start_tls",
    ldapBind: "How the DN is found",
    ldapBindHint: "search is required to check groups and read the display name",
    ldapUpn: "UPN suffix",
    ldapDnTemplate: "DN template",
    ldapBase: "Search base",
    ldapFilter: "Filter",
    ldapFilterHint: "%s is where the login goes",
    ldapBindDn: "Service account DN",
    ldapPasswordEnv: "Password variable",
    ldapPasswordHint: "Environment variable name. The password is not stored in the profile",
    ldapGroups: "Allowed groups",
    ldapGroupsHint: "Empty means membership is not checked",
    ntlmUrl: "Domain controller url",
    ntlmDomain: "Domain",
    ntlmDomainHint: "NTLM bind, not the WWW-Authenticate HTTP scheme",
    users: "Users",
    userCount: "Entries",
    inUse: "In use",
    inUseYes: "by a profile",
    addUser: "Add",
    login: "Login",
    displayName: "Display name",
    password: "Password",
    passwordHint: "At least eight characters. Hashed with bcrypt and not stored",
    groups: "Groups",
    groupsHint: "Comma separated",
    sentHint: "Version sent,",
  },
  ipSetsPage: {
    description: "Description",
    lists: "Lists",
    live: "Live",
    liveChip: "live",
    liveHint:
      "This set includes an active list: its contents reach the inspector over the bus, not with the generation. The pack carries a reference, not a copy.",
    newTitle: "New set",
    editTitle: "Set",
    nameHint: "The name is what profile rules refer to",
    inverse: "Inverse",
    inverseHint: "Match everything that is not in the set after subtracting ext",
    countries: "Geo",
    countriesHint: "Countries from geo sets. Both v4 and v6 apply.",
    asns: "ASN",
    asnsHint: "Autonomous systems from ASN sets. Both v4 and v6 apply.",
    addList: "List",
    listHint: "Address sets (ipv4/ip). Live ones travel as a bus subject.",
    exclude: "ext",
    excludeHint:
      "Subtracted from the set above. Inverse applies after: Russia + inverse + office in ext — everything except Russia, plus the office.",
    needSources: "Add ipv4/ip datasets, geo sets, or ASN sets first.",
    empty: "No composite sets yet — create the first one.",
  },
  ipProfiles: {
    lockedDelete: "The default profile is not deleted: the manifest does not build without it.",
    ttlShort: "ttl {n}s",
    description: "Description",
    rules: "Rules",
    newTitle: "New profile",
    editTitle: "Profile",
    nameHint: "This name goes to waf_inspector_profile",
    setHint: "A composite set from Sets (composite)",
    editRule: "Edit",
    whiteEmpty: "Empty: nobody passes without a check.",
    blackEmpty: "Empty: nobody to deny, the otherwise block answers.",
    rulesEmpty: "Empty: nothing is told to the neighbours.",
    add: {
      white: "Add to the allowlist",
      black: "Add to the denylist",
      rules: "Add a rule",
    },
    edit: {
      white: "Allowlist row",
      black: "Denylist row",
      rules: "Rule",
    },
    amountShort: {
      delta: "by {n}%",
      value: "counter {n}%",
    },
    toInspectors: "Inspectors",
    toLists: "Datasets",
    noRegistry: "The action vocabulary failed to load: the verb list is empty.",
    sectionProfile: "Profile",
    sectionProfileHint: "Name and description: the name is what a route selects",
    sectionLists: "Address lists",
    sectionListsHint:
      "What the inspector decides with: allowlists, denylists and the answer for whoever matched none",
    sectionDatasets: "Lists for conditions",
    sectionDatasetsHint:
      "What to pack and ship to the inspector: only declared lists reach the node, and only they are offered as a rule's condition",
    sectionChannel: "Actions channel",
    sectionChannelHint:
      "What the profile tells its neighbours and writes into datasets. There is no accept table here: the address inspector runs on wave zero and nobody sends to it",
    whiteTitle: "Allowlists",
    whiteHint:
      "A match returns allow at once. Checked first and independent of row order: an exception must beat a ban.",
    blackTitle: "Denylists",
    blackHint:
      "There may be several, each with its own response. The first match decides, so the order is set by dragging. The inspector has no score: it answers allow or deny.",
    fallbackTitle: "Otherwise",
    fallbackHint:
      "No list matched. The inspector always has to answer, and it has two answers -- this is its default one.",
    fallbackAction: "What to answer",
    fallbackActions: {
      allow: "let through",
      deny: "deny",
    },
    trigger: "When",
    triggerHint:
      "Where the address turned up: in a particular list, outside it, or in the profile's own lists. \"In no list\" is the fallback branch — it covers everyone who matched nothing. The last entry is not about the address at all but about the inspector itself: its queue has reached the threshold. The action is the same for all of them: an ask to a neighbour or writing the address into a live dataset.",
    triggers: {
      white: "address in the allowlists",
      black: "address in the denylists",
      none: "address in no list",
      list: "address in the list",
      list_not: "address not in the list",
      overload: "inspector is overloaded",
    },
    notSetShort: "not {name}",
    list: "List",
    listHint:
      "A raw list declared by this profile. Composite sets are not asked here: the row gives no verdict, so an expression over the raw material buys it nothing",
    listEmpty: "This profile declared no lists",
    listKind: "Contents",
    listLive: "over the bus",
    listStatic: "in the pack",
    listInUse: "A rule points at this list — remove the rule first",
    datasetsHint:
      "They carry no logic: they are simply shipped. Active ones sync over the bus from keeper, the rest travel as bodies in the pack.",
    datasetsEmpty: "Nothing declared yet — rules have nothing to pick from.",
    addList: "Add a list",
    chooseList: "Choose a list",
    askCodeHint:
      "The ask's reason: the receiver filters on it when deciding whether to apply. Empty — no reason. [A-Z][A-Z0-9_]*",
    outcomeCodeHint: "What explains the dataset entry. Empty — the decision code. [A-Z][A-Z0-9_]*",
    toDenyRiskHint:
      "The receiving inspector. An ask does not arrive where the decision is deny: it ends the phase. A marker and writing the address into a dataset always work",
    toDatasetShort: "into a dataset",
    rulesTitle: "Rules",
    rulesHint:
      "These decide nothing: they tell the neighbours, and they work in both outcomes -- on the allowlist and on the denylist. The order is set by dragging.",
    set: "Set",
    params: "Parameters",
    removeRule: "Remove rule",
    drag: "drag",
    needSets: "Create a composite set under Sets (composite) first.",
    needLive: "There is no active list to write into.",
    code: "Reason",
    codeHint: "[A-Z][A-Z0-9_]*. Empty means the default code (IP_LIST or IP_GEO).",
    response: "Response",
    responseHint: "The deny catalog entry the client is answered with",
    responseDefault: "default",
    to: "To",
    toHint:
      "The receiving inspector. There is no \"everyone\" here: an ask without a name reaches all of them, and who listens is the sender's call.",
    toAll: "everyone",
    verb: "What to do",
    apply: "About whom",
    applyHint:
      "This request, the address, the whole provider network, or the session. What to make of it is the receiver's call.",
    verbHint: "Only what the chosen receiver listens to.",
    listDataset: "Which dataset",
    toDataset: "no one: write the address to a dataset",
    ttl: "ttl, sec",
    ttlHint: "0 means forever. Otherwise the address expires on its own.",
    empty: "No profiles yet — create the first one.",
    actions: {
      allow: "allow",
      deny: "deny",
      request: "request",
      list: "list",
    },
  },
  actions: {
    verbs: {
      challenge: {
        label: "Send to a challenge",
        hint: "Asks to run the client through a captcha. Whether and which one is the receiver's call.",
      },
      threshold: {
        label: "Count stricter or softer",
        hint: "A coefficient on what the receiver measures for this request: minus is a discount (allowlist), plus makes everything dearer (blocklist). Neither its thresholds nor the route's move -- the price of the client's behaviour does.",
      },
      skip: {
        label: "Let through unchecked",
        hint: "Asks the receiver to let this request pass unchecked. Offered to the counter only: for it this means 'count, but do not judge' — the tally goes on, there is no verdict. For everyone else the lever is mode 'off': the same meaning without the bus round trip and without a receiving rule.",
      },
      reauth: {
        label: "Cut the session",
        hint: "The session looks hijacked: asks to require signing in again.",
      },
      note: {
        label: "Change a counter",
        hint: "The receiver's counter on the chosen axis. The amount is strictly percent of its scale: plus fills (+100% — to the top), minus drains (−100% — reset, never below zero). Which scale to change is decided by the receiving rule; for the counter inspector with several buckets you may name one — the Bucket field — and a named bucket passes only through the rule that grants it.",
      },
      mutate: {
        label: "Toggle modifiers",
        hint: "Asks rewrite to switch a response modifier group on this request or frame: which one and which way is yours to name; whether it is accepted, the receiver's rule decides by sender and reason.",
      },
      active: {
        label: "Mode 'active'",
        hint: "Executed by the module: the addressee's call is set to mode 'active' — it is asked and its verdict decides. The axis is the term: 'this request' lasts to the end of the transaction (on frames — this frame), 'connection' — to the end of the frame connection. No grant is needed: any inspector asked on the route may ask; the addressee must be called on it.",
      },
      passive: {
        label: "Mode 'passive'",
        hint: "Executed by the module: the addressee's call is set to mode 'passive' — it is asked, its verdict goes to the log and the shadow score but does not decide. The axis is the term (request or frame connection). No grant is needed: any inspector asked on the route may ask.",
      },
      vote: {
        label: "Mode 'vote'",
        hint: "Executed by the module: the addressee's call is set to mode 'vote' — only points are taken from it: a score lands in the sum, a deny counts as 100, and it decides nothing on its own. A denial by the sum is up to the route threshold. The axis is the term (request or frame connection). No grant is needed: any inspector asked on the route may ask.",
      },
      off: {
        label: "Mode 'off'",
        hint: "Executed by the module: the addressee's call is set to mode 'off' — it is not asked on the remaining waves. A quiet relaxation like skip, without the bus round trip. The axis is the term (request or frame connection). No grant is needed: any inspector asked on the route may ask.",
      },
      audit: {
        label: "Write to the log",
        hint: "Executed by the module: whether this request is written to the audit log. 'Write' overrides the route's waf_audit_sample; 'skip' is like a sample miss: denials, inspector silence and archived objects are written anyway. Objects are the 'To record' axis as in the path's object dialog: each with a budget ('whole' — up to the datagram ceiling) and a source (snapshot or unmasked original). Which record — the request's or the response's — is chosen by the 'Record' field, on frames — this frame; both can be set from any phase: on a route with a later phase the request record waits for the outcome. No grant is needed: any inspector asked on the route may ask for this.",
      },
      archive: {
        label: "Write to the archive",
        hint: "Executed by the module: whether the request's objects are left to the agent for the archive. 'Keep' overrides the route's when= and works even without waf_archive: the switched-on objects, else the route's set, else everything captured; each object has its own size and source, the TTL covers all. 'Drop' — the objects go with the verdict. Which record — the request's or the response's — is chosen by the 'Record' field, on frames — this frame; both can be set from any phase. No grant is needed: any inspector asked on the route may ask for this.",
      },
      mark: {
        label: "Add a marker",
        hint: "Executed by the module: a label on this request's record (on frames, this frame's; what is said at the handshake stands on every frame of the connection). You name the string — events are searched and grouped by it in the log. No grant is needed: a marker overrides and hides nothing, it is only added to the record. The same marker from two inspectors is one marker; who asked and why is visible in the record's actions.",
      },
      score: {
        label: "Points on the route",
        hint: "Executed by the module: a signed number added to the phase sum of this request (on frames, this frame), next to the neighbours' verdict points. Add and the sum grows; remove and it shrinks, but never below zero: only what was accumulated can be removed. One inspector's contribution stays within a hundred either way, and neighbours see it as its score in prior. What to do with the sum is up to the route threshold (waf_score_deny). From a passive sender the ask is dropped: it affects traffic in no way.",
      },
      list: {
        label: "Add to a dataset",
        hint: "The address goes to an active set and outlives the request.",
      },
    },
    group: {
      record: "The route's record",
      mode: "Inspector mode",
      ask: "Request to the inspector",
    },
    audit: {
      record: "Record",
      recordHint:
        "Which record of the transaction to override: the request's or the response's. Each has its own; both take two rows. There is no response record without a response phase on the path",
      recordOf: { request: "request", response: "response" },
      recordResponse: "response record",
      set: "What to do",
      alertAuditOn:
        "The record happens regardless of what the route says: waf_audit_sample does not cancel the ask, and the objects enabled below land in the record even where the route's Record axis does not take them.",
      alertAuditOff:
        "The same as a sample miss, not a ban: a denial, an inspector's silence and a request that left objects to the archive are written anyway — otherwise the ask would hide evidence.",
      alertArchiveOn:
        "The objects stay with the agent regardless of what the route says: the route's when= does not filter the ask, and the archive happens even where there is no waf_archive at all. The ask names its own outcome below.",
      alertArchiveOff:
        "The objects leave with the verdict: nothing is left to the agent, and the exchange is cleaned right away.",
      alertSourceCapture:
        "The capture's Mask and Do not capture apply, and the route's own lists go on top. The size may be wider than the capture: the module hands what is missing to the agent with the record.",
      objectShort: "budget and source — the ask's own",
      objectAsRoute: "as on the route",
      write: "write to the log",
      skip: "skip",
      keep: "keep in the archive",
      drop: "drop",
      ttl: "Archive TTL",
      ttlHint: "E.g. 30d. Empty — as configured on the route",
      when: "Archive on which outcomes",
      whenHint:
        "The ask's own filter, not the route's: it does not read the route's when=. \"Any outcome\" — keep it whatever happens",
      objectsAuditHint:
        "Objects as in the path's object dialog, the 'To record' axis: a switched-on object goes into the record itself with this budget and from this source; switched off — as on the route",
      objectsArchiveHint:
        "Objects as in the path's object dialog, the 'To archive' axis: switched-on objects stay for the agent with this size and from this source; none switched on — the route's set, or everything captured without one",
      budgetHint: "How many bytes of the object go into the record; empty — the whole object, up to the datagram ceiling. The sum over objects is clamped to it",
      sizeHint: "How many bytes of the object go to the archive; empty — the whole object",
      sourceHint:
        "As captured — the capture slice and masks, the route's own lists on top. Original — before the capture masks: only the route's own lists apply, and without them the object goes with no masks at all. A body nobody read is read for this.",
      objectOff: "exclude",
    },
    mark: {
      marker: "Marker",
      markerHint:
        "Any string: events are searched and grouped by it. Up to {max} bytes, no leading or trailing spaces",
    },
    param: {
      delta: {
        label: "By how much, %",
        hint: "Minus is a discount (softer), plus makes it dearer (stricter): −100% counts to zero, +100% doubles, +900% is tenfold.",
      },
      value: {
        label: "By how much, %",
        hint: "1–100 of the receiver's counter scale.",
      },
    },
    axes: {
      request: "this request",
      ip: "address",
      asn: "whole provider network (ASN)",
      session: "session",
      conn: "connection (to its end, frames only)",
      response: "response record",
    },
    counter: {
      label: "About whom",
      bucket: "Bucket",
      bucketHint:
        "Counter only: which of its buckets to change. Empty — the receiving rule names the bucket; a named one passes only through the rule that grants it",
      direction: "Add or remove",
      add: "add: a share of the threshold",
      cut: "remove: a share of the accumulated",
      percent: "By how much, %",
      percentHint:
        "1–100. Adding 100% puts the counter right at the threshold; removing 100% resets it — it never goes below zero",
    },
    mutate: {
      group: "Group",
      groupHint:
        "Name of a modifier group at the receiver: whether it exists, the receiver knows. Whether you may touch it, its prior rule decides",
      set: "Mode",
      on: "switch on",
      off: "switch off",
    },
    phase: {
      label: "Call phase",
      hint:
        "An inspector standing on two phases of the route is called twice. Empty — both calls get the mode; a phase — only the call of that phase",
      all: "on every phase",
      request: "request",
      response: "response",
      frame: "frames",
    },
    score: {
      direction: "Add or remove",
      add: "add to the sum",
      cut: "remove from the sum",
      points: "Points",
      pointsHint: "1–100. Only what was accumulated can be removed: the sum never goes below zero",
      summary: "{n} points",
    },
    direction: {
      label: "Stricter or softer",
      stricter: "stricter: everything dearer",
      softer: "softer: a discount",
      percent: "By how much, %",
      percentHint:
        "Stricter: 100 doubles, 900 is tenfold. Softer: at most 100 -- a 100% discount counts to zero",
    },
  },
  agent: {
    sent: "Version {rev}, {hash}",
    scopeHint:
      "These settings are the same for every node of the contour. Everything that differs between nodes -- bus address, key, paths, nginx management -- stays on the node itself: that is how the agent reaches the bus, so it cannot arrive over the bus.",
    drift: "Saved but not sent yet: nodes keep the previous settings.",
    section: {
      archive: "Archive",
      archiveHint:
        "Where the agent moves objects after a verdict: endpoint, region and buckets. An empty endpoint means archiving is off and `waf_archive` turns into archive_error in the record.",
      pace: "Upload pace",
      paceHint:
        "How many workers carry objects, how long the queue is and how PUTs accumulate. A full queue sends the record with an overload locator: the payload is lost, not the event.",
      nodes: "Nodes",
      nodesHint:
        "Which revision each node applied. Stale means the pulse has not brought the new one yet, or applying it failed.",
    },
    group: {
      s3: "Storage",
      s3Hint:
        "Three buckets -- different sensitivity, different retention. A node rejects an endpoint with no bucket named.",
      pool: "Pool",
      poolHint: "An empty field is the agent default.",
      batch: "Baskets",
      batchHint:
        "S3 has no PutObjects, so a batch here means collecting objects of one kind and firing N plain PUTs. Sections do not wait for each other: headers leave while the body is still filling up.",
    },
    field: {
      endpoint: "endpoint",
      region: "region",
      bucket: "bucket",
      workers: "workers",
      queue: "queue",
      timeout: "timeout",
      batch: "batch",
    },
    help: {
      endpoint:
        "Address of the S3-compatible storage. Empty means archiving is off. Access credentials belong to the node (WAF_RETAIN_S3_CREDENTIALS_FILE): the controller names the storage but does not open it.",
      region: "Signing region. Defaults to us-east-1.",
      bucket_headers: "Bucket for request headers.",
      bucket_args: "Bucket for the query string.",
      bucket_body: "Bucket for the request body.",
      workers: "How many objects travel to the archive at once. Defaults to 4.",
      queue: "Length of the transfer queue. Full means records leave with overload.",
      timeout: "Ceiling for one storage operation. Defaults to 5000.",
      batch:
        "Both cells empty -- the PUT leaves at once. size= fires at N objects, timeout fires by time since the first object in the basket; with both set, whichever comes first.",
    },
    nodes: {
      node: "Node",
      rev: "Revision",
      hash: "Hash",
      state: "State",
      ok: "applied",
      stale: "stale",
      failed: "not applied",
      none: "never arrived",
      empty: "No live agents.",
    },
  },
  haproxy: {
    sent: "Version {rev}, {hash}",
    scopeHint:
      "The balancer in front of the edges. The controller renders the whole haproxy.cfg from these settings; the node agent validates it with `haproxy -c` and reloads the master without dropping connections. Until the first send the node runs the config baked into the image.",
    section: {
      proxy: "Proxy",
      proxyHint:
        "Process limits and default timeouts: how many connections to hold, when to cut stalled clients and how long a tunnel lives after an upgrade.",
      routing: "Routing",
      routingHint:
        "Entry point, distribution and cluster membership: which port to listen on, how to balance and which path proves an edge is healthy.",
      servers: "Servers",
      serversHint:
        "The backend server lines: name, address and port. An empty list prints the shipped edge-01..03. haproxy merges two servers with one name, so names are unique.",
      nodes: "Nodes",
      nodesHint:
        "Which revision each balancer applied. Stale means the presence frame has not brought the new one yet, or `haproxy -c` rejected it.",
    },
    group: {
      process: "Process",
      processHint: "The global section. An empty field keeps the shipped default.",
      timeouts: "Timeouts",
      timeoutsHint:
        "The defaults section: applies to the entry point and to edge connections. Tunnel covers websocket and socket.io after the upgrade; it outlives timeout server.",
      frontend: "Entry point",
      frontendHint: "The port the balancer listens on.",
      backend: "Edge cluster",
      backendHint:
        "Where traffic goes: the balancing algorithm and the health check. A failed edge leaves rotation until the first successful answer.",
      stats: "Statistics",
      statsHint:
        "The stats page: cluster distribution and edge health without entering the container.",
    },
    field: {
      maxconn: "maxconn",
      bufsize: "tune.bufsize",
      connect: "timeout connect",
      client: "timeout client",
      server: "timeout server",
      keepalive: "timeout http-keep-alive",
      tunnel: "timeout tunnel",
      port: "bind",
      balance: "balance",
      checkPath: "httpchk",
      checkStatus: "http-check expect status",
      checkInter: "check inter",
      statsEnabled: "stats enable",
      statsPort: "stats bind",
      dockerDns: "resolvers docker",
    },
    help: {
      maxconn: "Ceiling of concurrent connections. Defaults to 4096.",
      bufsize:
        "Request buffer size. The stand drives query strings up to 256k -- the haproxy default of 16k would cut them into 400s.",
      connect: "How long to wait for a TCP connection to an edge. Defaults to 2s.",
      client: "How long to wait for the client. Defaults to 30s.",
      server: "How long to wait for the edge to answer. Defaults to 30s.",
      keepalive: "How long to hold an idle keep-alive. Defaults to 5s.",
      tunnel:
        "Tunnel lifetime after an upgrade: websocket outlives timeout server. Defaults to 1h.",
      port: "Entry port. Defaults to 8080.",
      balance:
        "roundrobin cycles, leastconn picks the least busy, source hashes the client address.",
      checkPath: "Health check path on the edge. Defaults to /healthz.",
      checkStatus: "Which status counts as healthy. Defaults to 200.",
      checkInter: "Check period. Defaults to 2s.",
      statsEnabled: "Show the statistics page.",
      statsPort: "Statistics page port. Defaults to 8404.",
      dockerDns:
        "The Docker DNS resolver: without it a recreated edge keeps its old IP and stays DOWN forever. Turn off outside compose.",
      serverName: "Name of the server line; the stats page labels the edge with it.",
      serverHost: "Edge address: a compose service name or a DNS name.",
      serverPort: "Edge port. Empty means the entry port.",
    },
    servers: {
      name: "Name",
      host: "Address",
      port: "Port",
      add: "+ add server",
      defaults: "The list is empty -- the shipped edge-01..03 are printed.",
      addTitle: "New server",
      nameTaken: "A server with this name already exists: haproxy merges two lines with one name.",
    },
    nodes: {
      node: "Node",
      rev: "Revision",
      hash: "Hash",
      state: "State",
      ok: "applied",
      stale: "stale",
      failed: "not applied",
      none: "never arrived",
      empty: "No live haproxy agents.",
    },
  },
  config: {
    varValue: "Value",
    vars: {
      field: "Field",
      fieldHelp:
        "Key in the audit record's `vars`. The journal search filters by it, so the name is fixed.",
      source: "nginx variable",
      sourceHelp:
        "Computed once per request, before the local layer. No value — an empty string; the request does not fail.",
      about: "What it is",
      always: "always",
      alwaysHelp:
        "The module's standard set: computed by itself, always in the audit record. An inspector gets what its declaration ticks (Inspectors → gear → Request fields).",
      hint: {
        user_agent: "Client User-Agent",
        referer: "Referer: where the client came from",
        origin: "Origin of a cross-site request",
        xff: "X-Forwarded-For: address chain behind a load balancer",
        accept_language: "Client Accept-Language",
        content_type: "Request Content-Type",
        accept: "Accept: what the client asks to be served",
        protocol: "HTTP version: HTTP/1.1, HTTP/2.0",
        request_id: "nginx request id, to join with upstream logs",
        connection_requests: "Request number within a keepalive connection",
        ssl_cipher: "TLS connection cipher",
        ssl_client_dn: "Client certificate subject",
      },
    },
    rawSeed: "Insert the current config",
    rawEmpty: "The raw config is empty. You can start from what the tree prints now.",
    title: "Configuration",
    uuid: "UUID",
    raw: "raw",
    rawNginx: "Configuration",
    empty: "No space — pick a scope in the menu.",
    loading: "Loading…",
    sent: "Version {rev}, {hash}",
    deleteAria: "delete",
    chipsHint: "Enter to add",
    chipsPick: "pick on the right",
    addHeaderAlways: "always",
    header: {
      add: "Add header",
      addTitle: "New header — add_header",
      addHint:
        "What the block appends to responses. Inheritance is nginx's: any add_header below replaces the whole list.",
      emitted: "Line in the file",
      nothing: "Name and value are empty — nothing to append.",
      dup: "This header is already in the list — the client would get it twice.",
      empty: "The list is empty — the block appends nothing to responses.",
    },
    proxyHeader: {
      nameHint: "Name of the header sent to the protected server",
      addTitle: "New header — proxy_set_header",
      addHint:
        "What the proxy passes to the protected server. A row overrides the preset by name: Host here replaces the preset's Host.",
      nothing: "Name and value are empty — nothing to pass.",
      empty: "The list is empty — only the preset goes to the protected server.",
      dup: "This header is already in the list.",
    },
    mode: {
      config: "Builder",
      raw: "Own config",
      preview: "Preview",
      rawKeptServer:
        "Hostnames, ports, and certificates stay in the panel — they are printed outside the block. Everything inside server {} is written here by hand.",
      rawKeptLocation:
        "match and the path stay in the panel — the location header is built from them. Everything inside the block is written here by hand.",
    },
    tab: {
      config: "Config",
      waf: "WAF",
      nginx: "nginx",
      preview: "preview",
    },
    rawWarnTitle: "Enable raw?",
    rawWarn: "Nested objects, servers, and paths will be lost.",
    rawConfirm: "Enable",
    section: {
      catalogs: "Contour catalogs",
      catalogsHint:
        "Lists, deny responses, the exchange and log formats. Servers and paths reference these by name.",
      main: "File skeleton",
      mainHint:
        "load_module, worker_processes, events {} — everything above http {}. nginx does not start without it.",
      wafHttp: "Bus and memory",
      wafHttpHint:
        "Shared worker memory, message limits, and the NATS connection. Process-wide, not inherited by servers. Local checks will not load without a zone.",
      waf: "Request inspection",
      wafHint:
        "Default WAF behavior: enablement, timeouts, inspection failures, body, score, and cookies. Empty means the module default. A server or path can override a key.",
      nginx: "nginx",
      nginxHint: "http {} core: connections, client, address, compression, TLS, headers, and logs.",
      conn: "Connections",
      connHint: "TCP, keepalive, and closing stuck clients. Not events {}.",
      client: "Client and buffers",
      clientHint:
        "Body size, header buffers, timeouts, and name hash tables. A hash that is too small keeps nginx from starting.",
      net: "Client address",
      netHint:
        "DNS resolver and real_ip. Behind a balancer, limits see its address until trusted proxies are set.",
      gzip: "Compression",
      gzipHint: "gzip responses. Types are a MIME list; Enter adds one.",
      tls: "TLS",
      tlsHint: "SSL protocols, ciphers, and sessions. Certificates are set on the server.",
      files: "Files and output",
      filesHint:
        "Root, index, and what the block serves itself: alias, try_files, path role.",
      access: "Access",
      accessHint: "nginx allow/deny — a coarse address filter before any inspection.",
      proxy: "Proxy",
      proxyHint:
        "Timeouts, buffering, and protected-server headers. Printed only where proxy_pass stands.",
      logs: "Logs",
      logsHint:
        "Where the block writes access_log and error_log. The default sink is the agent socket — that is how lines reach the Logs section. No key — inherited; key set — replaces.",
      headers: "Response headers",
      headersHint: "Block add_header. Inherited as in nginx: a child replaces the whole set.",
      nginxParams: "Advanced settings",
      nginxParamsOpen: "Configure",
      nginxParamsSet: "keys set: {n}",
      serverNginxHint:
        "WAF request decisions and the server {} overlay. Empty inherits from http {}. Listeners and certificates are configured separately.",
      locationNginxHint: "WAF request decisions and the location {} overlay. Empty inherits from the server, then http {}.",
      protect: "Protection",
      protectHint:
        "What an operator changes day to day. Directive names live in the helper. A profile value is marked “Secure edge”.",
      inspectors: "Inspectors",
      inspectorsHint:
        "waf_inspector declarations in http {}. subject and phase come from the catalog. Who is called on a route is set in server/path protection, not here.",
      advanced: "Advanced",
      advancedHint:
        "Bus, buffers, keepalive, proxy, logs, and raw nginx. Empty means the module default, not the profile.",
    },
    group: {
      mainModule: "Process and module",
      mainModuleHint:
        "The file skeleton above http {}. The agent writes the compiler output into nginx.conf whole and checks it with nginx -t -c.",
      mainEvents: "events {}",
      mainEventsHint: "Without the events {} block the master does not start.",
      mainIncludes: "include",
      mainIncludesHint: "Files included above http {}.",
      shm: "Memory and slots",
      shmHint:
        "Without a zone the local layer (rate, dataset) will not load. Reload does not change zone size.",
      limits: "Message limits",
      limitsHint: "How much a worker accepts from an inspector and how many bodies it holds.",
      bus: "Bus",
      busHint: "NATS addresses are resolved at config load, not on the hot path.",
      vars: "Variables",
      varsHint:
        "`waf_var`: a field in the audit record and the inspector message. The module's standard set always travels, ticks add to it; which inspector gets what is set on its declaration. The journal search filters by field name. Not for `waf_local_check` — that reads nginx variables directly.",
      core: "Core",
      coreHint: "Enable WAF and early data. Budget and failures live on the phase card.",
      phaseRequest: "Request phase",
      phaseRequestHint:
        "The live phase. Missing key inherits the parent. Inspector `none` is not capture: capture has its own toggle.",
      phaseResponse: "Response phase",
      phaseResponseHint:
        "The phase runs: the response is held until the verdict and a deny serves a catalog page. Missing key means the request value.",
      phaseFrame: "Frame phase",
      phaseFrameHint:
        "WebSocket frames from the client after the upgrade: budget and threshold. A frame is held until the verdict (gate); a deny is a Close from a type=websocket entry. Missing key means the request value.",
      capture: "Capture & deliver",
      captureHint:
        "What to capture for inspectors, what of it to show in the audit record, what to archive, and what to deliver to the receiver — as received or with inspector edits. Four columns along the data path. A column is dimmed while inherited; any edit makes it your own.",
      policies: "Inspection failures",
      policiesHint:
        "What to do if an inspector is silent, the bus is down, or the body is missing.",
      body: "Request and response body",
      bodyHint: "Body limit and what happens to what exceeds it.",
      preview: "Request preview in the record",
      previewHint:
        "What of the request travels inside the audit record and is searchable in" +
        " ClickHouse. The section budget is the last limit: above it stand the" +
        " absolute WAF read limits.",
      score: "Score and deny responses",
      scoreHint: "A score threshold closes the phase. Redirects not in the list are rejected.",
      cookie: "Cookies and debug",
      cookieHint: "Attributes are forced on top of whatever the inspector sent.",
      tcp: "TCP",
      keepalive: "Keep-alive",
      lingering: "Connection close",
      lingeringHint: "Slowloris and half-closed clients are an edge concern, not a cache one.",
      clientBody: "Body and buffers",
      clientTimeouts: "Client timeouts",
      clientHeaders: "Headers and behavior",
      hash: "Hash tables",
      hashHint: "Otherwise nginx will not start with long or numerous server_name values.",
      resolver: "Resolver",
      realIp: "Real IP",
      realIpHint: "Trusted proxies. Without this, limits see the balancer address.",
      gzip: "Compression",
      tls: "TLS",
      proxy: "Proxy",
      access: "Access",
      logs: "Logs",
      logsHint:
        "`access_log` and `error_log` inherit like the rest of nginx core: no key — the parent's, a key — replaces it.",
      addHeaders: "add_header",
      addHeadersHint: "Global headers (HSTS). Inheritance is nginx-style: a child replaces.",
      raw: "Raw http {}",
    },
    option: {
      idle: "unset — the phase does not run",
      pass: "pass — allow",
      block: "block — deny",
      allow: "allow — pass",
      wave: "wave — continue to inspect",
      objects: "objects",
      truncate: "truncate — cut",
      fast: "fast — deny on first deny",
      deterministic: "deterministic — wait for the whole wave",
      always: "always",
      standard: "standard",
      websocket: "websocket",
      none: "none",
      inherit: "inherit",
      all: "all",
      list: "list",
      override: "override",
      active: "active",
      passive: "passive",
      ignore: "ignore",
      prefix: "prefix",
      exact: "exact",
      regex: "regex",
      regex_i: "regex_i",
      named: "named",
      proxy: "proxy",
      static: "static",
      return: "return",
      healthz: "healthz",
      challenge: "challenge",
      deny_page: "deny_page",
      optional: "optional",
      custom: "custom",
      Strict: "Strict",
      Lax: "Lax",
      None: "None",
      gate: "gate — hold until the verdict",
      monitor: "monitor — release immediately",
    },
    op: {
      scorePage: "Deny page",
    },
    inspectorGraph: {
      title: "Declaration graph",
      hint: "Row order is declaration order: headers, cookies, and deterministic deny stack this way. after builds waves. An empty cell is the module default.",
      graphGroup: "Graph",
      optionGroup: "Directives",
      name: "name",
      nameHint: "Catalog name. Below it: subject and phase, which the compiler prints.",
      timeout: "timeout, ms",
      timeoutHint: "Empty inherits the route waf_deadline.",
      needs: "needs",
      needsHint:
        "Empty means headers,args. none means no exchange. The list replaces the default entirely.",
      body: "body",
      bodyHint: "Body level. Empty means none, or full if needs includes body.",
      after: "after",
      afterHint: "Who must finish first. Not allowed: self, advisory, another phase.",
      add: "Add inspector",
      choose: "Select an inspector",
      noCatalog: "The catalog is empty — create a process on the Inspectors page first.",
      empty:
        "No graph yet. Pick an inspector from the catalog below. Without a graph nginx.conf has no waf_inspector.",
      unknown: "Not in the catalog: {names}. The config will not compile without a catalog node.",
      unknownName: "not in catalog",
      badAfter: "after cannot point at advisory or another phase: {details}.",
      drag: "drag",
      moveUp: "Higher in declaration order",
      moveDown: "Lower in declaration order",
      more: "Role, weight, breaker",
      moreHint: "Only set fields are printed. Empty means the module default.",
      role: "role",
      roleHint: "Empty means mandatory. advisory is not awaited and cannot appear in after.",
      placement: "placement",
      placementHint: "Empty means remote. local is the same host (exchange, transform_after).",
      weight: "weight",
      weightHint:
        "Score multiplier, 0–10. Empty means 1. On advisory it only affects the audit estimate.",
      sample: "sample",
      sampleHint: "Request share, 0–1. Empty means 1. On mandatory it leaves uneven coverage.",
      audit: "audit",
      auditHint: "Empty means waf.audit.inspector.<name>. off disables publishing.",
      allowHeaders: "allow headers",
      allowHeadersHint: "Headers this inspector may set. Example: X-WAF-Score.",
      allowCookies: "allow cookies",
      allowCookiesHint: "Cookies this inspector may set. Example: waf_chal.",
      breaker: "breaker",
      breakerHint: "Empty means on.",
      breakerThreshold: "threshold",
      breakerThresholdHint: "Timeout share 0–1. Empty means 0.5.",
      breakerWindow: "window, ms",
      breakerWindowHint: "Observation window. Empty means 10000.",
      breakerProbe: "probe, ms",
      breakerProbeHint: "Probe interval while open. Empty means 5000.",
    },
    field: {
      loadModules: "load_module",
      workerProcesses: "worker_processes",
      workerRlimitNofile: "worker_rlimit_nofile",
      user: "user",
      pid: "pid",
      mainErrorLog: "error_log",
      mainErrorLogShip: "copy to Logs",
      workerConnections: "worker_connections",
      multiAccept: "multi_accept",
      eventsUse: "use",
      mainIncludes: "include",
      shmName: "waf_shm_zone name",
      shmSize: "waf_shm_zone size",
      agentSocket: "waf_agent_socket",
      maxInflight: "waf_max_inflight",
      replyMax: "waf_reply_max",
      headerValueMax: "waf_header_value_max",
      bodyMaxHolds: "waf_body_max_holds",
      busFlushIntervalMs: "waf_bus_flush_interval",
      busAddress: "bus address",
      redisExchange: "exchange Redis",
      redisInternal: "internal Redis",
      busName: "bus name",
      busConnectTimeoutMs: "bus connect_timeout",
      busReconnectWaitMs: "bus reconnect_wait",
      busPingIntervalMs: "bus ping_interval",
      busPendingMax: "bus pending_max",
      busPayloadMax: "bus payload_max",
      vars: "waf_var",
      addHeaders: "add_header",
      enabled: "waf",
      deadlineMs: "waf_deadline request",
      exception: "waf_exception",
      responseDeadlineMs: "waf_deadline response",
      denyMode: "waf_deny_mode request",
      captureObjects: "waf_capture request objects",
      capture: "waf_capture",
      bodyLimit: "waf_body_limit",
      bodyLimitPolicy: "waf_body_limit policy",
      archive: "waf_archive",
      preview: "waf_preview",
      scoreDeny: "waf_score_deny request",
      scoreDenyResponse: "score_deny response=",
      responseScoreDeny: "waf_score_deny response",
      frameDeadlineMs: "waf_deadline frame",
      frameScoreDeny: "waf_score_deny frame",
      requireUpgrade: "waf_require_upgrade",
      requireUpgradeResponse: "waf_require_upgrade response=",
      wsStripExtensions: "waf_ws_strip_extensions",
      frameAudit: "waf_audit_frames",
      frameAuditSample: "waf_audit_frames sample=",
      frameReassemble: "waf_frame_reassemble",
      frameControlRate: "waf_frame_control_rate",
      frameCacheTtl: "waf_frame_cache ttl=",
      frameCacheStream: "waf_frame_cache: side",
      denyResponseDefault: "waf_deny_response_default",
      actionMax: "waf_action_max",
      actionsMax: "waf_actions_max",
      redirectAllow: "waf_redirect_allow",
      debugHeader: "waf_debug_header",
      cookieSecure: "cookie secure",
      cookieHttpOnly: "cookie http_only",
      cookieSameSite: "cookie same_site",
      headerValue: "Value",
      sendfile: "sendfile",
      tcpNopush: "tcp_nopush",
      tcpNodelay: "tcp_nodelay",
      keepaliveTimeoutS: "keepalive_timeout",
      keepaliveRequests: "keepalive_requests",
      keepaliveTimeS: "keepalive_time",
      resetTimedoutConnection: "reset_timedout_connection",
      lingeringClose: "lingering_close",
      lingeringTimeMs: "lingering_time",
      lingeringTimeoutMs: "lingering_timeout",
      clientMaxBodySize: "client_max_body_size",
      clientHeaderBufferSize: "client_header_buffer_size",
      clientBodyBufferSize: "client_body_buffer_size",
      clientBodyTempPath: "client_body_temp_path",
      largeHeaderCount: "large_client_header_buffers count",
      largeHeaderSize: "large_client_header_buffers size",
      clientHeaderTimeoutMs: "client_header_timeout",
      clientBodyTimeoutMs: "client_body_timeout",
      sendTimeoutMs: "send_timeout",
      defaultType: "default_type",
      underscoresInHeaders: "underscores_in_headers",
      ignoreInvalidHeaders: "ignore_invalid_headers",
      mergeSlashes: "merge_slashes",
      serverTokens: "server_tokens",
      serverNamesHashBucketSize: "server_names_hash_bucket_size",
      serverNamesHashMaxSize: "server_names_hash_max_size",
      typesHashBucketSize: "types_hash_bucket_size",
      typesHashMaxSize: "types_hash_max_size",
      resolver: "resolver",
      resolverTimeoutMs: "resolver_timeout",
      realIpFrom: "real_ip_from",
      realIpHeader: "real_ip_header",
      realIpRecursive: "real_ip_recursive",
      gzip: "gzip",
      gzipCompLevel: "gzip_comp_level",
      gzipMinLength: "gzip_min_length",
      gzipVary: "gzip_vary",
      gzipTypes: "gzip_types",
      sslProtocols: "ssl_protocols",
      sslCiphers: "ssl_ciphers",
      sslPreferServerCiphers: "ssl_prefer_server_ciphers",
      sslSessionCache: "ssl_session_cache",
      sslSessionTimeout: "ssl_session_timeout",
      proxyHttpVersion: "proxy_http_version",
      proxyHeaders: "proxy_headers",
      errorLog: "error_log",
      accessLog: "access_log",
      root: "root",
      charset: "charset",
      httpsRedirect: "https_redirect",
      sslVerifyClient: "ssl_verify_client",
      sslVerifyDepth: "ssl_verify_depth",
      alias: "alias",
      index: "index",
      tryFiles: "try_files",
      allow: "allow",
      deny: "deny",
      internal: "internal",
      ssi: "ssi",
      ssiTypes: "ssi_types",
      role: "role",
      proxyConnectTimeoutMs: "proxy_connect_timeout",
      proxyReadTimeoutMs: "proxy_read_timeout",
      proxySendTimeoutMs: "proxy_send_timeout",
      proxyBuffering: "proxy_buffering",
      proxyRequestBuffering: "proxy_request_buffering",
      proxySetHeader: "proxy_set_header",
    },
    help: {
      loadModules:
        "Dynamic modules of the process. Without `modules/ngx_http_waf_module.so` there is no module in nginx and every `waf_*` directive fails the load.",
      workerProcesses: "Worker count. `auto` follows the core count.",
      workerRlimitNofile:
        "Descriptor cap per worker. Size it from `worker_connections` and server pools.",
      user: "Worker user.",
      pid: "Master pid file.",
      mainErrorLog:
        "`error_log` of the process itself, above `http {}`. Path and level on one line.",
      mainErrorLogShip:
        "A second `error_log` line at the same level — into the agent socket, and from there into the Logs section. That is everything the module writes outside a request (bus, dataset subscriptions, breaker) and what the master writes itself (reload, a crashed worker). No key — the copy is on; “off” keeps only the file. Without `error_log` there is no copy: a lone socket line would cancel nginx’s built-in file.",
      workerConnections: "Connections per worker. Client and protected server are two.",
      multiAccept: "Accept all ready connections at once instead of one per cycle.",
      eventsUse: "Connection method: `epoll` on Linux. Empty lets nginx choose.",
      mainIncludes:
        "`include` above `http {}`, where the node file lands if it declares anything outside http.",
      shmName: "Shared-memory zone name. One per config; the local layer will not load without it.",
      agentSocket:
        "Unix verdict socket to the agent, usually `/run/waf/verdict.sock`. No default: with `waf` on or a non-empty `waf_archive` the config will not compile without it.",
      shmSize:
        "Shared-memory zone size. Without it the local layer (rate, dataset) will not load.\n\n**Default** `8m`\n**Reload** does not change the size — a **restart** is required.",
      maxInflight: "How many requests a worker waits on at once. Default 4096.",
      replyMax: "Max inspector reply. Larger replies are dropped whole.\n\n**Default** `8k`",
      headerValueMax: "Limit for one header value in an override.\n\n**Default** `4k`",
      bodyMaxHolds: "How many bodies a worker holds at once. Default 1000. Stage 6.",
      busFlushIntervalMs: "Publish batching delay. 0 means flush immediately, lowest latency.",
      busAddress:
        "The NATS address the controller runs with (CONTROLLER_NATS_URL). Not editable: the same NATS is used by the agent and the inspectors, and editing one of the three would silently split them. Credentials come from there too.",
      redisExchange:
        "The combat body exchange (CONTROLLER_REDIS_URL): printed into waf_store; the module puts headers, query and body there, inspectors read them and the agent archives them. Not editable: the agent and the inspectors know the same address. Password hidden.",
      redisInternal:
        "The contour's internal Redis (CONTROLLER_REDIS_INTERNAL_URL): generation blobs, captcha and counter buckets, gate rosters. The module does not use it. Inspectors and edges keep the same address in one form: the internal key of the redis block in inspector.conf and agent.conf. Password hidden.",
      busName: "Connection name in NATS monitoring. Default waf-<hostname>.",
      busConnectTimeoutMs: "Connect timeout. Default 1000 ms.",
      busReconnectWaitMs: "Pause between reconnect attempts. Default 100 ms.",
      busPingIntervalMs: "Bus keepalive. Default 10000 ms.",
      busPendingMax: "Outbound buffer limit per worker.\n\n**Default** `8m`",
      busPayloadMax:
        "Cap for one inspect packet. Smaller than the worst capture — `nginx -t`.\n\n**Default** `1m`",
      vars: "`waf_var <name> <value>`: the value is a complex nginx expression (`$http_x_ja3`), computed once per request, before the local layer. If it fails, it is an empty string; the request does not fail. Names of the standard set are taken — it travels by itself.",
      addHeaders:
        "Global response headers (HSTS). Inherited like nginx: any `add_header` below replaces the whole list. `always` also writes on error responses, not just 2xx.",
      enabled: "Enable WAF for the whole space. Empty means the module default (off).",
      deadlineMs: "Budget for the whole request phase, not one wave. Default 50 ms.",
      exception:
        "What to do when there is no verdict. The line is the directive tail:" +
        " the phase first, an optional class second (`timeout`, `absent`," +
        " `bus`, `body`, `inspector`), then `pass` or `deny`; a `deny` may name" +
        " a page (`response=<catalog entry>`), otherwise the client gets a bare" +
        " 503. A line without a class sets all six; a class named twice on one" +
        " level is rejected by the build — narrow it a level below." +
        "\n\n" +
        "**Default** `timeout`, `absent` and `body` deny, `bus` passes",
      captureObjects:
        "What goes into Redis for inspectors: headers, args, body. mask=/deny= lists are separate lines.",
      responseDeadlineMs: "Budget for the response phase. Empty means the request deadline.",
      responseHold:
        "gate — the client gets no bytes until the verdict: a deny still becomes a page. monitor — the response leaves immediately, waves follow, deny cuts the stream. Default gate.",
      frameDeadlineMs: "Budget per frame, both sides. Empty means the request deadline.",
      frameScoreDeny:
        "Score threshold on a frame; the score resets on every frame. `response=` — a type=websocket entry with the Close code. Empty means the request threshold.",
      requireUpgrade:
        "A plain GET without `Upgrade: websocket` on this location is denied by the local layer, never reaching the bus. Empty — on: a websocket location has no response phase, so an unchecked response cannot be proxied.",
      requireUpgradeResponse:
        "Deny response entry for a request without the upgrade. Empty — `upgrade_required` (426).",
      wsStripExtensions:
        "Extensions removed from the client offer on the handshake: a negotiated extension means frames with rsv bits the module does not parse. Empty — `permessage-deflate`; an empty list — strip nothing; `all` — everything.",
      frameAudit:
        "Which frames become security event records, and with them the frame slice and archive. `deny` — only a frame with a deny, a rewrite or a score; `all` — every frame of both sides, a side without inspectors is journaled; `off` — neither frames nor the session summary. Empty — `deny`. The session summary is a separate record written when the connection closes, on a path without frame inspectors too. The same is editable in the “Capture & deliver” table.",
      frameAuditSample:
        "Only with `all`: record every n-th frame. Empty or 1 — every frame.",
      frameReassemble:
        "Fragment reassembly: a message split into fin=false frames is glued in the buffer and judged whole — a signature cannot hide between frames. The receiver gets it as a single frame, so leave it off for applications that rely on fragment boundaries. The ceiling is `waf_body_limit frame`. Empty — off.",
      frameControlRate:
        "Ping and pong above the rate are not forwarded (a bucket per direction, burst is one second of the rate); the connection stays up, Close is never limited. `<n>r/s`, `<n>r/m` or `off`. Empty — `10r/s`.",
      frameCacheTtl:
        "Verdict cache by payload hash: a repeat of the same frame on this location skips the bus until the entry expires. Only a clean allow from a real inspection is remembered; denies and rewrites never; the counter opts out by itself. Enable where inspectors judge by content and frames repeat (heartbeats, broadcasts). At most an hour. Empty — no cache.",
      frameCacheStream:
        "Which side to cache: both, client frames only or application frames only. Not printed without a ttl.",
      denyMode: "fast closes on the first required deny; deterministic waits for the whole wave.",
      capture:
        "Capture is separate from inspectors. inherit — parent." +
        " none — `waf_capture request none` (inspectors may stay)." +
        " objects — what goes into Redis.",
      bodyLimit: "Maximum body size the module accepts for inspection.\n\n**Default** `1m`",
      bodyLimitPolicy:
        "What happens to a body over the limit: `block`, `trim` (inspect the" +
        " prefix, the locator says `truncated`) or `pass`. Second word of" +
        " `waf_body_limit` — printed only next to a size of its own." +
        "\n\n**Default** `block`",
      archive:
        "What outlives the request. One line per object, tail included:" +
        " `request headers=64k args=64k ttl=30d`, `request body=8k ttl=180d" +
        " when=deny`, or `request none`. No `=` means the whole object, `=none`" +
        " turns that kind off. Lines add up by object, so the body can get its" +
        " own term and size without rewriting the headers. Without a ttl the" +
        " object is kept forever; the size is trimmed by the agent, not the" +
        " module. `when=` is allow or deny; omit it to archive every outcome." +
        " No exchange is named here — the contour has exactly one (`waf_store`).",
      preview:
        "Request slice carried inside the audit record. The size on the object" +
        " is required: `headers=64k/2k args=8k body=10k` or `none`. Name lists" +
        " are separate lines: `headers deny=x-api-key`, `args allow=id`. Deny" +
        " wins. No `=` is a nginx -t error. `/` caps one pair (body has none)." +
        " A named preview is a demand to extract the object, which is why every" +
        " object defaults to `off`.",
      scoreDeny: "Score threshold that closes the phase with a deny. 0 is off.",
      scoreDenyResponse: "Deny response name when the threshold fires. Must exist in the catalog.",
      responseScoreDeny:
        "Score threshold for the response phase. Empty means the request threshold.",
      denyResponseDefault: "Deny response if the inspector did not name one. Default blocked.",
      actionMax: "Ceiling for one action on the wire. Longer ones are dropped; the verdict stands.",
      actionsMax: "How many neighbour requests live on one request, counted across all phases. 0 turns the channel off here.",
      redirectAllow: "Allowed redirect patterns (/path or scheme://host). Empty forbids redirects.",
      debugHeader: "Diagnostic header: rid, latency, score, deny reason.",
      cookieSecure: "Force Secure on cookies from the inspector. Default on.",
      cookieHttpOnly: "Force HttpOnly. Default on.",
      cookieSameSite: "Force SameSite. Default Lax.",
      sendfile: "Serve files with sendfile(2), without a userspace copy.",
      tcpNopush: "Hold packets before send. Useful together with sendfile.",
      tcpNodelay: "Disable Nagle — lower latency on small responses.",
      keepaliveTimeoutS: "How long to keep an idle keep-alive, seconds.",
      keepaliveRequests: "Max requests on one keep-alive connection.",
      keepaliveTimeS: "Max lifetime of a keep-alive connection, seconds.",
      resetTimedoutConnection: "RST instead of FIN on timeout — against Slowloris.",
      lingeringClose: "How to close the socket if the client is still writing: on, off, always.",
      lingeringTimeMs: "How long to wait for the client tail after close.",
      lingeringTimeoutMs: "Pause between tail reads. Smaller frees the slot sooner.",
      clientMaxBodySize: "Max body at the nginx layer, before WAF.\n\n**Default** `1m`",
      clientHeaderBufferSize: "Initial request header buffer.\n\n**Default** `1k`",
      clientBodyBufferSize:
        "In-memory body buffer before spilling to a temp file.\n\n**Default** `16k`",
      clientBodyTempPath:
        "Directory for bodies that outgrew the buffer. Mount it on tmpfs — bodies never reach the disk and the module reads them from memory. The directory must exist on the node and be writable by the nginx worker.\n\n**Default** `client_body_temp` from the nginx build",
      largeHeaderCount: "Number of large buffers for long headers / cookies.",
      largeHeaderSize:
        "Size of each large buffer for long headers and cookies.\n\n**Default** `8k`",
      clientHeaderTimeoutMs: "How long to wait for client headers, else 408.",
      clientBodyTimeoutMs: "How long to wait for the client body between reads.",
      sendTimeoutMs: "Timeout writing the response to the client.",
      defaultType: "Default MIME if types does not match.",
      underscoresInHeaders: "Accept headers with underscores. Otherwise they are dropped.",
      ignoreInvalidHeaders: "Ignore malformed headers instead of 400.",
      mergeSlashes: "Collapse // in the URI. Affects location matching and normalization.",
      serverTokens: "nginx version in errors and the Server header. Usually off at the edge.",
      serverNamesHashBucketSize:
        "Hash bucket for `server_name`. Too small — nginx will not start.\n\n**Default** `64`",
      serverNamesHashMaxSize: "Upper size of the `server_name` hash.\n\n**Default** `512`",
      typesHashBucketSize: "Hash bucket for the MIME types table.\n\n**Default** `64`",
      typesHashMaxSize: "Upper size of the MIME types hash.\n\n**Default** `512`",
      resolver: "DNS for variables and protected servers. At the edge these are recursor addresses.",
      resolverTimeoutMs: "Resolver timeout.",
      realIpFrom: "Trusted proxies to take the client address from.",
      realIpHeader: "Where to read the address, usually X-Forwarded-For.",
      realIpRecursive: "Walk the XFF chain to the first address not in real_ip_from.",
      gzip: "Compress responses. On HTTPS watch vary.",
      gzipCompLevel: "Level 1–9. Higher means less traffic, more CPU.",
      gzipMinLength: "Do not compress responses shorter than this, bytes.",
      gzipVary: "Add Vary: Accept-Encoding — needed by caches and CDNs.",
      gzipTypes: "MIME types to compress. text/html is always compressed.",
      sslProtocols: "Allowed TLS versions. Usually TLSv1.2 and TLSv1.3.",
      sslCiphers: "OpenSSL cipher string. Empty means the nginx default.",
      sslPreferServerCiphers: "The server picks the cipher, not the client.",
      sslSessionCache: "Session cache, e.g. shared:SSL:10m.",
      sslSessionTimeout: "Session lifetime in the cache, e.g. 1d.",
      proxyHttpVersion: "HTTP version to the protected server. 1.1 is needed for keepalive and websocket.",
      proxyHeaders:
        "Protected-server header preset. standard — Host, X-Forwarded-For and" +
        " X-Forwarded-Proto: without them the server sees the pool name in Host." +
        " websocket — plus Upgrade and Connection, and version 1.1 is printed" +
        " along with it. custom — only the list below. none and an empty choice" +
        " print nothing: the set is inherited from the parent. Mind the nginx" +
        " rule: a level with headers of its own replaces the parent set entirely" +
        " rather than adding to it.",
      errorLog:
        "`error_log` for this block: sink and level. A key here replaces the parent's entirely, as in nginx.\n\nThe agent socket (`log_sock`) is the road that gets lines into the Logs section; the tag becomes the “service” column there. A file is visible only on the node itself.",
      accessLog:
        "`access_log` for this block. Several lines are allowed — nginx permits it, and at the edge that is two sinks: the agent socket and a file next to it.\n\nThe agent socket (`log_sock`) is the road that gets lines into the Logs section; the tag becomes the “service” column there.\n\nClearing it means `access_log off;` — what silences the log on `= /healthz` — not an absent key.",
      addHeaderAlways: "Write the header on error responses too, not only 2xx.",
      raw: "The template gets this text instead of the generated http {}.",
      root: "File root for this server. Locations may replace it with alias.",
      charset: "Response charset. Empty inherits from http {}.",
      httpsRedirect: "HTTPS redirect only — not a full WAF server on :80.",
      sslVerifyClient: "mTLS: on, off, or optional. The CA certificate lives on the server.",
      sslVerifyDepth:
        "Chain length from the client certificate to the root. nginx defaults to 1, which only covers clients signed by the root itself; an intermediate CA needs 2.",
      alias: "Map the URI onto a directory. Do not mix with root on the same path.",
      index: "Index files for a directory.",
      tryFiles: "Look up a file, then fall back. Last argument is a URI or a status.",
      allow: "Allowed addresses. Empty inherits.",
      deny: "Denied addresses. all closes the path.",
      internal: "Reachable only through internal nginx redirects.",
      ssi: "Server-side includes. The deny page uses them to fill in $waf_ray and $waf_deny_name.",
      ssiTypes: "Response types scanned for ssi includes. Defaults to text/html only.",
      role: "Service role: UX will not offer inspectors on captcha/healthz.",
      proxyConnectTimeoutMs: "Timeout connecting to the protected server.",
      proxyReadTimeoutMs: "Timeout reading the protected server response.",
      proxySendTimeoutMs: "Timeout sending the request body to the protected server.",
      proxyBuffering: "Buffer the protected server response.",
      proxyRequestBuffering: "Buffer the request body before the protected server.",
      proxySetHeader:
        "Your own headers to the protected server. They edit the preset by name" +
        " rather than adding to it: a Host row replaces the preset's Host. With" +
        " custom this list is the whole set.",
    },
  },
  ports: {
    copyOff: "No copy: address and port are unique within the space.",
    boundDelete: "The port is bound to servers: remove the bindings first.",
    title: "Listen addresses",
    blurb:
      "Listen-address catalog of the space. A port does not accept traffic by itself — bind it to a server. ssl, http2 and proxy_protocol belong to the socket: they are shared by every server_name on that address. default_server is set on the binding.",
    listen: "listen",
    address: "Address",
    addressHint: "0.0.0.0, ::, 127.0.0.1, or a node address.",
    port: "Port",
    portHint: "1–65535. In nginx this is listen address:port.",
    nameHint: "Catalog label, not server_name.",
    ssl: "ssl",
    sslHint: "listen … ssl. The certificate lives on the server (SNI), not on the port.",
    http2: "http2",
    http2Hint: "listen … http2. Usually together with ssl.",
    proxyProtocol: "proxy_protocol",
    proxyProtocolHint: "Client address from HAProxy / the balancer.",
    servers: "Servers",
    empty: "No ports yet — create 80 and 443.",
    newTitle: "New port",
    editTitle: "Port",
    deleteAria: "delete port",
    deleteTitle: "Delete port",
    deleteConfirm: "Delete “{name}”? Unbind it from servers first.",
    boundNote: "Bound to {count} servers. Changing ssl/http2 updates every listen.",
    sectionListen: "Address",
    sectionListenHint: "listen address:port — the socket nginx opens.",
    sectionSocket: "Socket properties",
    sectionSocketHint: "Tail of the listen line. Shared by every server_name on this address.",
  },
  upstreams: {
    boundDelete: "The pool is used by routes: detach it from them first.",
    title: "Protected servers",
    blurb:
      "Protected-server pools (`upstream {}`) of this space. They do not belong to a server: a path points at a pool, and one pool can serve several virtual hosts.",
    method: "Method",
    methodHint: "round_robin is the nginx default. hash needs a key.",
    hashKey: "hash key",
    hashKeyHint: "The hash directive, e.g. $request_uri.",
    keepalive: "keepalive",
    keepaliveHint: "Cached connections in the pool. Empty disables keepalive.",
    keepaliveRequests: "keepalive_requests",
    keepaliveRequestsHint: "Requests per cached connection.",
    keepaliveTimeoutMs: "keepalive_timeout",
    keepaliveTimeoutHint: "Idle timeout of a pooled connection.",
    peers: "Pool servers",
    peersHint: "server host:port lines in the upstream block. Order matches the file.",
    server: "Server",
    host: "Host",
    port: "Port",
    weight: "Weight",
    maxFails: "max_fails",
    failTimeoutMs: "fail_timeout",
    backup: "backup",
    down: "down",
    flags: "Flags",
    paths: "Paths",
    empty: "No pools yet — create the application pool.",
    newTitle: "New server pool",
    editTitle: "Server pool",
    nameHint: "Name of the upstream {} block. proxy_pass points at it.",
    deleteAria: "delete server pool",
    deletePeerAria: "delete pool server",
    deleteTitle: "Delete server pool",
    deleteConfirm: "Delete “{name}”? Unbind it from paths first.",
    boundNote: "{count} server paths point at this pool.",
    sectionWire: "Scheme and name",
    sectionWireHint:
      "How a path talks to the pool servers: over http or https, and under which name. The port alone decides nothing — 443 without TLS is still plain HTTP.",
    tls: "TLS to server",
    tlsHint:
      "proxy_pass switches to https and the path gets proxy_ssl_server_name on. Without it a server on 443 answers \"400 The plain HTTP request was sent to HTTPS port\".",
    tlsName: "proxy_ssl_name",
    tlsNameHint:
      "Name sent in the handshake (SNI). The nginx default is the host from proxy_pass, i.e. the pool name — nobody issued a certificate for that. Empty — the host of the pool's only server is used.",
    hostHeader: "Host",
    hostHeaderHint:
      "Host header sent to the protected server. Overrides the one set on the path: the pool knows the external name, the path only knows the name we were reached by. Empty — with TLS the handshake name is sent, otherwise Host stays up to the path.",
    sectionBalance: "Balancing",
    sectionBalanceHint: "How the pool picks a server for a request.",
    sectionKeepalive: "Keepalive",
    sectionKeepaliveHint: "Connection pool to the upstream servers. Empty — nginx opens a connection per request.",
    hostHint: "Server name or address; printed as server host:port.",
    portHint: "Server port; the second half of server host:port.",
    weightHint: "weight=: share of requests relative to the other servers. Default 1.",
    maxFailsHint: "max_fails=: consecutive failures that take the server out of rotation. Empty — nginx default (1).",
    failTimeoutHint: "fail_timeout=: failure-counting window and pause after removal. Empty — nginx default (10 s).",
    backupHint: "backup: receives requests only when the primary servers are down.",
    downHint: "down: server taken out of rotation by hand.",
    resolve: "resolve",
    resolveHint:
      "resolve: the name is resolved at run time, not when the configuration loads. nginx starts even if the name is not in DNS yet and follows address changes. Needs a resolver in the http settings.",
    flagsHint:
      "Tail of the server line: backup — a spare server, down — out of rotation, resolve — the name is resolved at run time.",
    peersEmpty: "No servers yet — add the first one with “+”.",
    addPeer: "Add server",
    editPeer: "Pool server",
    peerLineTitle: "Line",
  },
  routeSettings: {
    general: "Main settings",
    protect: "Protection settings",
    protectHint:
      "Which inspectors to call on this node. A phase set is replaced whole: the request line does not touch the response.",
    openProtect: "Inspector settings",
    request: "Request inspectors",
    requestHint: "waf_inspect request: waves run one after another",
    response: "Response inspectors",
    responseHint: "waf_inspect response: the reply is held until the verdict",
    frame: "Frame inspectors",
    frameHint:
      "waf_inspect frame: WebSocket messages after the upgrade in both directions, each held until the verdict; a deny closes the connection with a Close frame. Each call picks its side: client, application or both",
    stream: "Side",
    streamHint:
      "Whose frames to ask about: from the client (frame:c2s), from the application (frame:s2c) or both sides with one line (frame). Empty — client.",
    streamValue: {
      c2s: "client",
      s2c: "application",
      both: "both sides",
    },
    local: "Lists and limits",
    localHint:
      "What is decided before the bus: allow and block by dataset, rate limits. The outcome is final — no inspector is called.",
    listKind: "List",
    inspector: "Inspector",
    inspectorHint:
      "A declared name (settings → Inspectors). The list holds only those whose process claims this phase; under the name — the profile and the subject. A row only switches between profiles of the same process: a different kind goes on its own row.",
    order: "Order",
    orderHint:
      "Inspectors are polled top to bottom. The same number means one wave: those inspectors run in parallel, and the next wave starts once the previous one has answered.\n\nDrag a row by its grip to reorder it. The number is a button: it merges the row into the previous wave or lifts it into its own.",
    drag: "drag",
    waveFirst: "Wave 0 — the first one, nothing to merge with",
    waveJoin: "Wave {n}, its own. Merge with the previous — they will run in parallel",
    waveSplit: "Wave {n}, parallel with the previous. Lift into its own wave",
    timeout: "Timeout",
    timeoutHint: "How long to wait for this inspector. Empty — the whole phase budget (waf_deadline).",
    mode: "Mode",
    modeHint:
      "**active** — the verdict counts.\n**passive** — the inspector is polled, but its verdict decides nothing: the answer only reaches the log.\n**vote** — only points are taken from the inspector: a score lands in the sum as sent, a deny counts as 100 points, and it decides nothing on its own — no denial, no redirect, no traffic edits. How many such votes make a denial is the route threshold waf_score_deny. Neighbours see it, it may ask and control, and it is awaited like an active one.\n**off** — the row is written, but the inspector is not asked until a neighbour changes the mode with a request: any inspector asked on this route may.\n\nThe mode says how the answer counts, not whether to call: that is the call condition, and it is settled first. It fails — the inspector is not asked in any mode.",
    keep: "Keep",
    keepHint:
      "keep=on: the inspector leaves the transaction open after it answers — the response phase of the same inspector will come back for it. Without it the transaction is dropped with the answer.\n\nOnly in a pair with Resume on the same name in the response phase: one without the other is rejected by the controller and by nginx -t — nobody to keep for, or nothing to keep.",
    resume: "Resume",
    resumeHint:
      "What to do with the state the request phase kept (Keep). Resuming plays phases 3–4 on the same transaction, with the real request context and inbound score.\n\n**off** — start over from the request context in the exchange.\n**prefer** — resume; no state (instance died, ttl expired, registry was full) — replay quietly.\n**require** — resume; no state — deny (MODSEC_RESUME_LOST) and log an error: for rules that need the request body or the real score a replay is no check at all.\n\nNeeds Keep on the same name in the request phase.",
    timeoutInherit: "phase budget",
    addInspector: "Add inspector",
    addDialogHint:
      "The kind is picked here, once. From then on the row only switches between profiles of the same process; a different kind goes on its own row.",
    addPick: "name, profile, or subject",
    addAllUsed: "Every inspector of this phase is already listed",
    addNoNames: "No declared name claims this phase",
    keepNone:
      "This name's process serves the request only: the response phase never comes back for the state, there is nobody to hold it for.",
    resumeNone:
      "This name's process serves the response only: it has no request phase, there is nothing to resume.",
    emptyList:
      "The list is empty — add an inspector. An empty list is not saved: the path falls back to the server set, and to keep the phase from running pick none.",
    emptyListServer:
      "The list is empty — add an inspector. An empty list is not saved: the phase simply does not run, same as none.",
    emptyNoNames:
      "No declared name claims this phase — declare an inspector in settings (the Inspectors section) or pick none.",
    noneList:
      "none — the phase does not run: no inspector is called on this route, capture is untouched.",
    inheritedEmpty:
      "The server set no list — the phase runs without a poll. To call inspectors here only, pick 'set'.",
    wrongPhaseTitle: "Wrong phase",
    wrongPhase:
      "{names}: the process does not claim this phase. The line is printed, but nobody answers — the phase waits out its deadline.",
    unknownNameTitle: "Not declared",
    unknownName:
      "{names}: not among the names declared in http {} — declare them in settings (the Inspectors section). A document with such names does not compile (undeclared_inspector).",
    call: "Conditions and mode control",
    callTitle: "Call — {name}",
    condsBlock: "When to call",
    condsAlert:
      "With no conditions the inspector is called on every request. A condition matches a value against a dataset: all conditions hold — the call works; one of them fails — the row is not published at all and the wave goes on without it: nobody waits for it, it adds nothing to the score and does not move the deadline.\n\nThe condition is settled before the mode and outranks it: a row it drops is raised neither by 'active' nor by a neighbour's request — there is no longer anyone to call. The reverse does not hold: a condition that matched is permission to call, not an order, and 'off' overrides it.\n\nAn empty value counts as 'not in the dataset': no cookie means it is not among the trusted ones either. A condition without a dataset is not saved.",
    condsSummaryAny: "on every request",
    condsSummary: "conditions: {count}",
    kind: {
      inherit: "inherit",
      all: "all",
      none: "none",
      override: "set",
    },
    modeValue: {
      active: "active",
      passive: "passive",
      vote: "vote",
      off: "off",
      ignore: "ignore",
    },
    resumeValue: {
      off: "off",
      prefer: "prefer",
      require: "require",
    },
    keepValue: {
      off: "drop",
      on: "keep",
    },
  },
  servers: {
    copyOff: "No copy: a server comes with ports, certificates, and routes.",
    deleteWithPaths: "“{name}” will be gone together with its {count} route(s). This cannot be undone.",
    title: "Virtual servers",
    blurb:
      "server {} blocks of this space. Hostname is server_name. Paths live inside a server. Ports are a catalog you bind here.",
    names: "hostname",
    namesHint: "server_name hosts. Enter adds a name. The first one is the label.",
    listens: "listen",
    listensHint: "Catalog ports this server stands on",
    listenColHint:
      "Address, port and listen flags — http2, proxy_protocol. They are edited in the port catalog; only the binding lives here.",
    tlsHint: "The catalog port ssl flag. A TLS port needs a server certificate.",
    needPorts: "Create ports in the catalog first.",
    addListen: "Port",
    addListenTitle: "Bind port",
    listenEmpty: "No ports bound yet.",
    sslOnly: "This server already has TLS — only ssl ports can be added.",
    plainOnly: "This server already has a plain listen — TLS ports cannot be mixed in.",
    defaultTakenBy:
      "default is already on “{name}”. It cannot be enabled here — turn it off there first.",
    defaultServer: "default",
    defaultServerHint:
      "default_server: this server answers on the port when no server_name matched. Only one server per port can hold it.",
    tls: "TLS",
    certs: "Certificates",
    certsHint: "Catalog certificates, one per role",
    certKindHint:
      "The directive the certificate is printed under in server {}: ssl_certificate — the server TLS pair, ssl_client_certificate — the mTLS client root, ssl_trusted_certificate — the chain used for verification.",
    addCertTitle: "Bind certificate",
    certEmpty: "No certificates bound yet.",
    needCerts: "Upload a certificate to the catalog first.",
    noCertsForKind:
      "No certificate of a suitable type for this role. Upload one on the Certificates page.",
    needServerCert: "This server has a TLS listen — bind a server certificate.",
    mtlsNoCrl:
      "The mTLS root has no revocation list: revoked client certificates will still pass verification. Upload a CRL on the Certificates page.",
    certKindsFull: "Every certificate kind is already bound.",
    unbindCert: "unbind certificate",
    unbindAria: "unbind port",
    locations: "Paths",
    openPaths: "Paths of this server",
    traffic: "Traffic · 10 s",
    noListens: "not listening",
    empty: "No servers yet — create the first one.",
    newTitle: "New server",
    editTitle: "Server",
    offTitle: "Server is off",
    offHint:
      "The server {} block is not printed: the host does not answer and its paths are dead. Edits below are saved, but apply only once it is on again.",
    wafHint:
      "Sparse overlay on the space waf. An empty field inherits, it does not turn the key off.",
    rawNginx: "server {} body",
    rawWarn: "Paths and overlays of this server are not printed: the template gets this text.",
    rawHelp: "The template gets this text instead of the generated server {}.",
  },
  paths: {
    copyTitle: "Copy route",
    copyPath: "Copy path",
    copyPathHint: "The path is unique within a server together with the match kind.",
    deleteAsk: "Route {path} of server {server} will be gone. This cannot be undone.",
    builtinHint:
      "Server root: created with the server, cannot be deleted, match and path are locked. Requests that hit no other path land here, so every journal record has a route. Handler and settings are editable.",
    title: "Paths",
    blurb:
      "location inside a server. Match is nginx-style; order matters for regex. waf and nginx overlays are sparse: no key means inherit from the server.",
    server: "Server",
    allServers: "All servers",
    match: "match",
    matchHint: "prefix, exact (=), regex (~), regex_i (~*), named (@).",
    path: "Path",
    pathHint: "URI or named location name.",
    handler: "Handler",
    handlerHint: "proxy to a server pool, static files, return a status, or named.",
    protocol: "Protocol",
    protocolHint:
      "http — request and response. websocket — handshake and frames: the upgrade preset, an hour of read timeout and waf_inspect response none are printed automatically; frame inspectors are assigned only here.",
    protocols: { http: "http", websocket: "websocket" },
    handlerSection: "Handler",
    handlerSectionHint: "Where the request goes: a server pool, files, a status code.",
    drag: "drag — order matters for regex paths",
    dragLocked: "Pick a server and clear the other filters to change the order.",
    cut: "Cut",
    cutBar: "Cut “{path}” — pick the row to paste it before.",
    pasteBefore: "Paste before",
    pasteEnd: "To the end",
    empty: "No paths yet — create the first one.",
    needServer: "Create a server first — a path cannot exist without a server {}.",
    newTitle: "New path",
    editTitle: "Path",
    offTitle: "Path is off",
    offHint:
      "The location {} block is not printed: the request goes to the next matching path or to the server root. Edits below are saved, but apply only once it is on again.",
    upstreamId: "Server pool",
    upstreamHint:
      "A pool in this space. Empty skips the named proxy_pass. Catalog is under Protected servers.",
    upstreamNone: "none",
    needUpstream: "Create a pool under Protected servers first.",
    upstreamUri: "URI suffix",
    upstreamUriHint: "proxy_pass tail, e.g. /. Empty forwards the URI as-is.",
    returnStatus: "return status",
    returnStatusHint: "Status 100–599 for the return handler.",
    returnPage: "Page",
    returnPageHint:
      "The named path that serves the response: printed as error_page <code> =<code> @path.",
    returnPageNone: "no page",
    needPage:
      "This server has no named paths. A page is served by a match=named path: ssi on, root pages:, try_files.",
    returnUrl: "Target",
    returnUrlHint: "Address for Location. For 3xx the second return argument is a URL, not a body.",
    wafHint: "Overlay on the server and the space. An empty array clears parent checks.",
    rawNginx: "location {} body",
    rawWarn:
      "Handler, upstream and overlays of this path are not printed: the template gets this text.",
    rawHelp: "The template gets this text instead of the generated location {}.",
  },
  inherit: {
    tag: {
      http: "http",
      server: "server",
      module: "default",
      request: "as request",
    },
    from: {
      http: "from http",
      server: "from server",
      module: "default",
      request: "as request",
    },
    fromHint: {
      http: "No key here — the value from http {} applies.",
      server: "No key here — the value from server {} applies.",
      module: "No key at any level — the module default applies.",
      request: "This phase has no waf_deadline line at any level — it takes the request phase budget.",
    },
    unset: "unset",
    own: "here",
    was: "was",
    wasHint: "The value that would apply without this line.",
    off: "cleared",
    override: "Override here",
    revert: "Back to inherited",
    turnOff: "Clear what is allowed above",
  },
  registry: {
    name: "Name and subject",
    profile: "Profile",
    audit: "Inspector audit",
    breaker: "Circuit breaker",
    empty: "No inspector is declared. A name absent from the registry cannot be called on a route.",
    add: "Add an inspector",
    addTitle: "New inspector",
    addNoCatalog: "The catalog is empty: process records arrive with the delivery, they are not created by hand.",
    line: "Config line",
    lineEmpty: "No process picked — nothing to declare.",
    declared: "already declared",
    remove: "Remove from the registry",
    settings: "Inspector settings",
    notInCatalog: "process {process} is not in the catalog",
    unknown:
      "No process behind: {names}. The declaration has no catalog record — the compiler will not accept it.",
    declareBlurb:
      "The name is what a route calls. A second name on the same process with a different profile — that is how a lenient allow_ip and a strict strict_ip are made; the subject, phases and conf stay with the process, the declaration references them.",
    nameHint: "A letter, then letters, digits, `_` and `-`. Up to 64 names per contour.",
    nameBad: "Not a name: a letter first, then letters, digits, `_` and `-`, up to 64 characters.",
    nameUsed: "That name is already in the registry — a contour has no two alike.",
    editBlurb:
      "The declaration: the name routes call an inspector by, and the process behind that name. Subject, phases and inspector.conf belong to the process and are edited in the catalog.",
    process: "Process",
    processHint: "the catalog record this name listens as",
    processPick: "pick a process…",
    processGone: "not in the catalog",
    profileDeclareHint: "the process rule set for this name; empty — default",

    noProfileSource: "no profiles for this inspector yet",

    ownProfile: "own name",
    auditSubject: "Subject",
    auditBlurb: "Where the inspector sends its parse details.",
    auditSubjectHint: "The subject the record goes to. Empty — the one the module derives from the name.",
    auditDefaultSummary: "default subject",
    auditHint:
      "Empty — the default subject, waf.audit.inspector.<name>: the module derives it, and every name gets its own, because details are read by name. A subject of your own routes details to a separate consumer — a stream with its own retention and access — or keeps the subject alive across a rename.",
    auditOffHint: "Off: the inspector writes no details at all.",
    auditOffTag: "audit off",
    auditOnTag: "audit: {subject}",
    breakerBlurb: "When to stop asking an inspector that starts timing out.",
    breakerHint:
      "An inspector that starts timing out is briefly left alone: the share of timeouts over the window passed the threshold. While the breaker is open it is probed once per probe period. An empty field is the directive default.",
    breakerSummary: "open at {pct}% timeouts over {window}, probe every {probe}",
    breakerShort: "{pct}% over {window}, probe {probe}",
    breakerOff: "off: a silent inspector is always asked",
    breakerOffTag: "breaker off",
    breakerThreshold: "Threshold",
    breakerWindow: "Window",
    breakerProbe: "Probe",
    breakerPctBad: "The threshold is a whole number from 1 to 100 percent. Empty is the directive default.",
    breakerTimeBad: "Window and probe are nginx time: `10s`, `500ms`, `1m`. Empty is the directive default.",
    profilePick: "default — inspector profile",
    vars: "Request fields",
    varsBlurb: "Which request fields travel in the message to this name.",
    varsHint:
      "The vars section in the message to this name: the module's standard set and the contour's waf_var. Unticked fields do not travel. The audit record carries every field regardless.",
    varsOffHint: "Off: no vars section — the inspector reads headers from the exchange.",
    varsNone:
      "No field is ticked, and a vars section is never empty: tick fields or switch the block off.",
    varsAllSummary: "all fields",
    varsAll: "all fields — including ones declared later",
    varsBuiltin: "module standard set",
    varsOwn: "contour waf_var",
    varsAllTag: "fields: all",
    varsTag: "fields: {fields}",
    editTitle: "Inspector · {name}",
    renameWarn:
      "The name moves in the registry too. waf_inspect calls on servers and paths keep the old name — fix them, or the compiler rejects the config.",
  },
  waves: {
    budget: "Phase budget: {budget} ms of",
    ms: "ms",
    edit: "Set the phase budget",
    editHint: "Phase budget in milliseconds: a whole number from 1 to {max}.",
    over: "over waf_deadline",
  },
  catalog: {
    pick: "pick…",
    empty: "catalog is empty",
    unknown: "not in catalog",
  },
  logs: {
    off: "access_log off",
    condition: "if=$…",
    add: "Add a sink",
    addTitle: "New sink — access_log",
    addHint:
      "Where the request lines of this block go. The agent socket is the only road into the Logs page; a file is visible on the node only.",
    addDup: "That sink is already in the list — the line would write to it twice.",
    addNothing: "No address set — nothing to write.",
    emitted: "Line in the file",
    kind: "List",
    errorHint: "Where the block writes nginx errors and from which level. A key replaces the parent entirely.",
    accessHint:
      "Where the block writes request lines. Several rows are allowed: on the edge that is the agent socket plus a file.",
    target: "Address",
    targetHelp:
      "For the agent socket — the tag: it becomes the “service” column in Logs. The socket itself is /var/run/waf/log.sock, the agent’s log_sock. For a file — the path on the node.",
    destHelp:
      "The agent socket is the only road into the Logs page. A file is visible on the node only. “Custom” is a foreign syslog and shows up when one is already set.",
    level: "Level",
    levelHelp: "error_log threshold. nginx default — error.",
    format: "Format",
    formatHelp: "A log_format from the contour catalog. Empty — combined.",
    formatDefault: "combined",
    conditionLabel: "Condition",
    conditionHelp: "if=$variable: the line is written when the value is neither empty nor 0.",
    inheritNotice: "No sinks here — lines go wherever the parent says.",
    inheritOffNotice: "access_log off is inherited ({from}) — this block writes no request lines.",
    defaultNotice: "No key — nginx writes access_log by its own default.",
    offNotice: "access_log off — this block writes no request lines.",
    emptyNotice: "The list is empty — add a sink. An empty list is not stored.",
    dest: {
      label: "Sink",
      unset: "not set",
      socket: "agent socket",
      file: "file",
      other: "custom",
    },
  },
  vars: {
    pick: "variable…",
    known: "Ready-made",
    parsers: "Parsed by name",
  },
  cond: {
    title: "Conditions — {name}",
    alert:
      "A condition matches a value against a dataset: `if <value> in|not in <dataset>`. There can be several, and the line works only when all of them hold; with no conditions it works on every request.\n\nAn empty value counts as 'not in the dataset': no cookie means it is not among the trusted ones either. A condition without a dataset is not saved.",
    value: "Value",
    dataset: "Dataset",
    add: "Add condition",
    addTitle: "New condition",
    valueHint: "What to compare: an nginx variable, a header, a cookie or a request argument",
    op: "Comparison",
    opHint: "in — the value is in the dataset, not in — it is not. An empty value counts as not in the dataset",
    datasetHint: "A list declared in the config: the value is compared against it",
    empty: "No conditions — the line works on every request.",
    pickList: "pick a dataset",
    notDeclared: "not declared in the config",
    lineTitle: "Line",
  },
  local: {
    dynamicList: "dynamic",
    staticList: "local",
    ttlMissingTitle: "Ban has no expiry",
    ttlMissingText:
      "Set {list} has no ttl= of its own and the rule does not set one. nginx -t on the node rejects such a line, and the whole template with it.",
    listMismatchTitle: "Key will not fit the set",
    listMismatchText:
      "{list} takes addresses ({type}) while key {key} arrives as a request string: the ban has nowhere to write.",
    mismatchTitle: "Set and value never match",
    mismatchText:
      "{dataset} holds addresses ({type}) while {value} arrives as a request string. The line compiles and passes nginx -t, but never matches.",
    meansNever: "Never matches: {dataset} compares addresses, this is a string.",
    notDeclaredTag: "gone",
    checks: "Lists",
    checksHint: "waf_local_check: a set against a value, first match wins",
    actionHint:
      "allow skips inspection entirely: own probes, partners, service addresses.\nwave skips the rest of the local layer (blocks and rates) but still goes to the inspectors.\nblock denies before the bus: no inspector is called.",
    responseHint:
      "An empty page means the route's waf_deny_response_default. Only block has one: an allow row has nothing to hand the client.",
    orderHint:
      "A block stands above an allow. The first matching row top down decides, so a key present in both datasets ends up denied. If that is not intended, drag the allow higher.",
    drag: "Drag",
    rates: "Rate limits",
    ratesHint: "waf_local_rate: a bucket per key, after every check",
    rateHint:
      "What happens once the bucket overflows. count=waves counts bus round trips, not requests. action=pass only counts, without denying. list= auto-bans the key into the dataset overlay and tells the neighbours; an empty ttl= takes the dataset's own.",
    outcome: "Outcome",
    decision: "Decision",
    page: "Page",
    speed: "Rate",
    reserve: "Burst",
    rateFieldHint:
      "Bucket leak rate: requests per second or per minute. The directive has no hourly unit -- 300 per hour is 5 per minute.",
    rateUnit: { s: "sec", m: "min" },
    burstHint: "Headroom above the rate. 0 is a bucket without headroom, the directive's default.",
    rateConfigure: "Configure the limit",
    rateSettings: "Limit on {key}",
    countHint: "waves counts bus round trips, requests counts requests, frames counts WebSocket frames (websocket locations only; the handshake is not counted).",
    rateActionHint: "block denies, pass only counts.",
    rateResponseHint: "An empty page means the route's waf_deny_response_default.",
    listHint: "Auto-bans the key into the dataset overlay and tells the neighbours.",
    ttlHint: "Empty takes the dataset's own term.",
    passNoPage: "A counting limit has no page: nothing is handed to the client.",
    passNoBan: "The module rejects action=pass together with an auto-ban.",
    noBan: "no auto-ban",
    hashLabel: "hash=md5",
    hashHint:
      "The bucket is kept per md5 of the key, not the key itself: a shm node is 32 bytes however long the key is. Without it a key over 255 bytes (a JWT in a cookie) silently skips the rule. The auto-ban receives the raw key: whether the dataset hashes it is up to the dataset.",
    hashedList: "md5",
    hashedListHint:
      "The dataset stores md5 of its values: checks and auto-bans hash the value themselves, and the set holds no value in clear text.",
    sumHash: "md5 key",
    lineTitle: "Config line",
    sumWaves: "by waves",
    sumFrames: "by frames",
    sumPass: "counts only",
    sumPage: "page {name}",
    sumPageDefault: "denies, default page",
    sumBan: "bans into {list}",
    sumBanTtl: "bans into {list} for {ttl}",
    kind: "Rules",
    dataset: "Dataset",
    value: "Value",
    key: "Key",
    addCheck: "Add rule",
    addCond: "Add condition",
    conds: "Conditions",
    addRate: "Add limit",
    emptyChecks: "No rules: no dataset passes anyone through or bans anyone.",
    emptyRates: "No limits.",
    pickList: "pick a dataset",
    notDeclared: "not declared in the config",
    defaultPage: "default",
    ttlNeeded: "ttl needed",
    noShmTitle: "No waf_shm_zone",
    noShm:
      "The local layer lives in shared memory: without the zone the node rejects the config (nginx -t) instead of silently skipping the checks. The zone is declared once per contour.",
    off: "Empty means a `waf_local_check none` line: the server's checks are dropped on this location.",
    offRate:
      "Empty means a `waf_local_rate none` line: the server's limits are dropped on this location.",
    parentEmptyChecks: "The server set no checks.",
    parentEmptyRates: "The server set no limits.",
    keyHint:
      "The value the bucket is kept per: `waf_local_rate <key>`. One value, not a set: the module rejects `$waf_request_args.*` — a request would land in several counters at once.",
    datasetHint:
      "A dataset declared in the config by a `waf_local_dataset` slot. Its membership is edited under Data.",
    valueHint:
      "What is compared against the dataset. The dataset type decides what is offered: only an address compares against cidr.",
    checkWhere: "Look up in dataset",
    checkWhat: "Value from the request",
    someValue: "the value",
    someList: "the dataset",
    meansTitle: "What this means",
    meansHint:
      "The rule fires on a hit only: it has no inverse check. \"Everything except the dataset\" is either an `if <value> not in <dataset>` condition (the conditions icon in the row) or an allow row placed above the block one.",
    meansHit: "{value} is in {dataset} — {outcome}.",
    meansMiss:
      "{value} is not in {dataset} — the row stays silent: the next row decides, and if none match, the limits and the inspectors do.",
    outAllow: "straight past inspection: no local blocks, no limits, no waves",
    outWave: "the rest of the local layer is skipped, off to the inspectors",
    outBlock: "denied before the bus, inspectors are not called; page {page}",
    outBlockDefault:
      "denied before the bus, inspectors are not called; the route's default page",
    addCheckTitle: "New rule",
    addRateTitle: "New limit",
    orderTitle: "Order decides",
    badRowTitle: "The row will not build",
    noDataset:
      "The rule has no dataset. The dataset name is the directive's first word: without it the node rejects the config.",
    badRate:
      "The key is required, and rate= is written as <n>r/s or <n>r/m — the unit is part of the value, not a default. Check: {keys}",
    dupRuleTitle: "One counter for two rows",
    dupRule:
      "The module tells counters apart by key, rate=, burst= and count=; neither the place of declaration nor the if conditions are part of that signature. Rows with the same signature count into one bucket: a request matching both is charged twice, and the limit fires twice as early as written. Check: {keys}",
    banNoTtlTitle: "Auto-ban without a term",
    banNoTtl:
      "Neither the rule nor the dataset itself has a ttl=, and nginx -t rejects such a config. Datasets: {lists}",
    passNoPageCheck: "Only block has a page: an allow row has nothing to hand the client.",
  },
  httpCat: {
    copyFrom: "initial membership…",
    copyFromHint:
      "Copy live entries from an existing set of the same type. Empty means the list starts empty.",
    lists: "Lists",
    listsHint:
      "Which datasets from Data are declared as waf_local_dataset slots. The dataset with its membership lives under Data; here you decide whether it reaches the template.",
    listsEmpty: "No dataset is declared. Local checks would have nothing to reference.",
    declareList: "Declare a dataset in the config…",
    newList: "Create a new dataset under Data",
    undeclare: "Remove from the config (the dataset stays under Data)",
    listMode: "Mode",
    listModeHint:
      "Where the set came from, not a setting of this row. active — the controller fills the membership over the bus, no nginx reload; such a set is taken from Data. internal — entries live inside the config and change with it; such a set is created right here.",
    listNameHint:
      "The name in the directive: `waf_local_dataset <name>`. Local checks on a route refer to the set by it.",
    listTypeHint:
      "The element type is set under Data and never changes afterwards: a string cannot be matched against an address.",
    listLimitHint:
      "`limit=` — entry cap of the shm slot. Below the membership size the extra entries never reach the slot.",
    listTtlHint:
      "`ttl=` is printed for active only: the lifetime of a live entry that carries no ttl of its own. internal has no such directive — `nginx -t` rejects it.",
    listHashHint:
      "`hash=md5` — the dataset stores md5 of its values: checks and auto-bans hash the value themselves. Set under Data on an empty string list.",
    listSizeHint: "How many entries the set holds now. Membership itself is edited under Data.",
    denyCode: "Code",
    denyPagePick: "pick a page…",
    denyMessagePlaceholder: "blocked by policy",
    denyParamsAll: "params: all",
    denyParamsHint:
      "What this record hands to the client page: a list opens only the chosen ones, empty means everything. Gates the $waf_deny_* variables; logs and audit are unaffected.",
    denyParamUnused: "the page never prints it",
    denyParamsSilenced:
      "The page prints {list}, and the list withholds them: the client will never see those blocks.",
    denyParamsDiag:
      "The page prints diagnostics ({list}) — params= does not gate them: those variables belong to the logs and the audit, and the client sees them whatever the list says.",
    denyParam: {
      ray: "Event ID (ray)",
      addr: "client IP",
      scope: "what is closed (scope)",
      subject: "named as (subject)",
      retry: "retry after (retry)",
    },
    storeCreate: "Create an exchange",
    storeWhere: "Exchange",
    storeWhereHint:
      "Driver and address are not editable: the same redis is declared twice more — by the agent (agent.conf) and by the inspectors (REDIS_URL). The controller prints them from its own environment (CONTROLLER_REDIS_URL); editing one of the three would silently split them, and the module would put objects where nobody reads.",
    storeUrlUnset: "The controller runs without CONTROLLER_REDIS_URL: no exchange address, the config will not build.",
    storeTtlHint: "Object lifetime during the wave. Default 30s.",
    storeRetainHint: "Until the agent takes archive and preview. Below ttl= — nginx -t.",
    storeMaxHint: "Value cap. A larger waf_body_limit — nginx -t.",
    storeMore: "More parameters",
    storeMoreHint: "Connection pool, timeouts, database number.",
    storeDialog: "Exchange: more parameters",
    storeAddTitle: "New exchange",
    storeLine: "Config line",
    storePoolHint: "Connections per worker, 1…64. Default 4.",
    storeConnectHint: "Connecting to redis. Default 200ms.",
    storeOpHint:
      "GET/SET inside the request budget. An exchange answering slower must turn the body into unavailable rather than eat the inspectors' deadline. Default 100ms.",
    storeReconnectHint: "Pause between reconnect attempts. Default 500ms.",
    storeDbHint: "Redis database number. Empty means 0.",
    formatNameHint: "This is the name access_log uses.",
    formatAdd: "Add format",
    formatAddTitle: "New log format",
    formatNameBad: "Name: Latin letters, digits, _ and -; a letter comes first.",
    formatNameTaken: "A format with this name already exists.",
    formatQuote: "A single quote cannot be written in a format: such a format does not reach the config.",
    formatsEmpty: "No custom formats.",
    modeActive: "Membership arrives from the controller over the bus. No nginx reload.",
    modeInternal: "Entries live in the config. A change means an nginx reload.",
    listSize: "Entries",
    deny: "Deny responses",
    denyHint:
      "waf_deny_response: what the contour answers a blocked client. Name, status and body; referenced by waf_deny_response_default, the score threshold and local checks.",
    denyBody: "Body",
    denyBodyHint:
      "What reaches the client: for http a page (`page=@name`, the named location nginx routes to via error_page); for grpc and websocket the `message=` text. The page text itself is edited under Data.",
    denyTypeHint:
      "How the record answers: http returns a status and a page, grpc and websocket their own code and a short text. Both the code and the body follow the type.",
    denyParamsOff: "params= is http only: grpc and websocket have no page to open anything to.",
    denyNameHint: "This is the name a route references.",
    denyStatusHint: "4xx/5xx only: 2xx and 3xx turn a block into a silent pass.",
    denyPageHint:
      "the named location nginx routes to via error_page. The module never fetches the page itself: it returns the status and the record name. The deny text belongs to the page itself, edited under Data.",
    denyMessageHint: "Short text instead of a page. The main option for grpc and websocket.",
    denyAdd: "Add a response",
    denyAddTitle: "New deny response",
    denyInUse: "The record is in use — remove the references first:",
    denyLine: "Config line",
    fromPages: "from the pages catalog",
    editPages: "Edit page content",
    advanced: "Advanced",
    advancedHint: "The object exchange and log formats: one or two records per contour.",
    store: "Object exchange",
    storeHint:
      "waf_store: where body, headers and query string are put so an inspector can read them and the agent can archive them. One per contour.",
    storeMany: "More than one exchange — the config will not build.",
    storeNone: "No exchange. Capture and archive will not load: there is nowhere to put the object.",
    formats: "Log formats",
    formatsHint: "log_format — nginx access-log formats; access_log refers to them.",
    formatBodyHint: "Directive body without quotes. A single quote inside cannot be expressed.",
    name: "Name",
    body: "Body",
    empty: "Empty",
  },
  tail: {
    object: "Object",
    objectHint:
      "**A row is one object of the phase**: `headers`, `args` — the query string, `body`. The columns are what happens to that object: Capture — what inspectors see, To record — what lands in the audit record, To archive — what the agent takes to S3, Deliver — which version the recipient gets.\n\nClicking a row opens the object window: every axis is edited together there, next to the lines that go into the config. A row's object never changes — for another one, delete the row and add the object you need.",
    objects: {
      headers: "headers",
      args: "query args",
      body: "body",
    },
    objectHints: {
      headers:
        "`headers` — headers as name–value pairs in the order received; a repeated name stays as separate pairs. On the response phase — the response headers of the protected server. Names are matched case-insensitively.",
      args:
        "`args` — the query string after `?`, as received, not decoded. Name lists work on parameter names. The response phase has no query string.",
      body:
        "`body` — the request body, or on the response phase the response body of the protected server. The body is one opaque run of bytes: it has no name lists and no masks, only a size.",
    },
    state: {
      inherit: "inherited",
      set: "own",
      off: "none",
    },
    addObject: "Add object",
    addAllUsed: "All objects of this phase are already in the table",
    empty:
      "Nothing is captured. Inspectors get request metadata only — method, address, path, client — and neither headers nor the body reach the record or the archive. Add an object with the “+” next to the phase.",
    emptyJournal:
      "The phase has no inspectors, so there are no Capture and Deliver columns: there is nobody to send the object to. The record and the archive work anyway — they take the object from the traffic itself. Add an object with the “+” next to the phase; the columns appear once the phase has inspectors.",
    editObject: "Edit object",
    stateLabel: "Set by",
    addTitle: "New object — {phase}",
    objectTitle: "{object} — {phase}",
    objectDialogHint:
      "Every axis of the object is edited together: what to capture for inspectors, what to show in the audit record, what to take to the archive and which version to deliver. A ticked axis applies; unticked, the object does not travel along it.\n\nBelow are the warnings (what `nginx -t` on the node would say, or what only the archive contents would show) and the lines that go into this level's config. Only touched axes are written; the rest keep inheriting.",
    objectDialogHintJournal:
      "The phase has no inspectors: nobody to capture or deliver for, so there are no Capture and Deliver axes here. The audit record and the archive remain — they take the object from the traffic itself.\n\nBelow are the warnings and the lines that go into this level's config; only touched axes are written.",
    capture: "Capture",
    captureShort: "what inspectors see and what goes into the exchange",
    preview: "To record",
    previewShort: "what lands in the audit record and is searched",
    archive: "To archive",
    archiveShort: "what the agent takes to S3 after the verdict",
    send: "Deliver",
    sendShort: "which version the recipient gets — as received or edited",
    axisOff: "not carried",
    captureHint:
      "`waf_capture` — **what inspectors see**. A captured object is put into the exchange (Redis) once, before the first wave, and inspectors read it from there; an object that is not captured reaches inspectors as `null`.\n\n**Slice** limits the size, **Mask** replaces a name's value with sha256, **Do not capture** drops the name altogether — all before the exchange, so inspectors never see the original.\n\nA body in the capture makes the first wave wait for the body to be read. The response body capture also sets how much of the response the module holds until the verdict.\n\nThe record and the archive with lists “as captured” show names the way inspectors saw them; their sizes are their own.",
    previewHint:
      "`waf_preview` — **a slice of the object inside the audit record**. The record goes to the log, and the slice lets you search requests across all traffic. The module cuts the slice itself from what it holds in memory — nobody fetches it from the exchange.\n\n**Budget** — the slice size in bytes of written JSON, required. Headers and args take a second value, the **pair cap**: `30k/2k`.\n\n**Name lists** — as captured or own; own lists replace the capture lists instead of adding to them. On the request phase the slice may be wider than the capture, a response body only within it.\n\nWhich requests are recorded at all is up to the route's sample; denials are always recorded.",
    archiveHint:
      "`waf_archive` — **what the agent takes to S3 after the verdict**: each kind of object into its own bucket. The audit record keeps a link, and the incident card opens the object by it.\n\n**Size** — how many bytes to keep, empty for the whole object. **Keep** and **Outcome** — the lifetime in the archive and on which outcomes to archive. **Name lists** — as captured or own, independent of the capture.\n\nWhere the bytes come from, the module decides itself: if the copy in the exchange is enough — the size is no wider than the capture and own lists open nothing it hid — the agent takes it; otherwise the module hands the original to the agent with the record. The exchange is never written twice, and the request does not wait for it.",
    slice: "Slice",
    sliceHint:
      "How many bytes of the object to capture: `64k`, `1m`. Empty — the whole object.\n\n**Headers** are cut at a whole pair, the size counted over the JSON of the pairs. **The query string** and **the body** are cut to a byte prefix.\n\nA size above `waf_body_limit` or `client_max_body_size` fails `nginx -t`. Headers and the query string are further bounded by nginx's header buffers. A body over `waf_body_limit` is handled by the limit policy: deny, cut, or pass without checking the body.",
    sendHint:
      "`waf_send` — **which version of the object the recipient gets**: the upstream on the request phase, the client on the response phase, the other side on frames.\n\n**Edited** (the module default) — the version an inspector put. Edits from different waves chain up and the last one goes; nobody edited — the original goes.\n\n**Original** — as received; an edit, if there was one, is not applied and shows in the record as `applied:false`.\n\nAn edited body needs a whole capture: a body wider than the slice cannot be replaced with a piece — that is a failed lift, and the phase policy for an unavailable object (`waf_exception … body`) decides: deny or the original. Headers and the query string are edited by name over the original, and the slice does not get in the way.",
    sendOriginal: "original",
    sendOriginalHint:
      "as received; an inspector edit is not applied and lands in the record with applied:false",
    sendStore: "edited",
    sendStoreHint: "the last version inspectors put; nobody edited — the original goes",
    sendPick: "Version",
    sendDefaultMark: "(default)",
    sendEffective: "in effect: {value} ({from})",
    sendPickHint:
      "**Original** — the body as received, inspector edits are not applied.\n\n**Edited** — the body from the object an inspector put with the rewrite section; several edits across waves — the last one goes, none — the original goes.\n\nThe body capture must be whole: a body within the slice is delivered edited, a body wider than the slice is a failed lift. A failed lift (partial capture, missing key, size or hash mismatch) is decided by the phase policy for an unavailable object: deny or the original.",
    sendPickHeadersHint:
      "**Original** — headers as received, inspector `set` and `unset` are not applied.\n\n**Edited** — edits land over the original by name and stack wave after wave, a later edit of a name wins. A name nobody touched goes as received — even if the capture cut or masked it: capture masks do not apply to the recipient.",
    sendPickArgsHint:
      "**Original** — the query string as received, inspector `set` and `unset` are not applied.\n\n**Edited** — edits land over the original by name: `set` changes a value or appends a pair, `unset` removes every pair with that name, a later edit wins. Capture masks and the slice do not apply to the recipient, and the string never travels through the exchange.",
    sendPickFrameHint:
      "**Original** — the frame as received.\n\n**Edited** — the frame payload from the inspector's object (the last one if there were several); the frame is rebuilt with the original opcode and fin flag.\n\nThe frame capture must be whole: a frame wider than the slice is a failed lift, and the direction's policy for an unavailable object decides: close the connection or deliver the original.",
    budget: "Budget",
    budgetHint:
      "How many bytes of the object go into the audit record — the size is required. It counts **written JSON**: quotes and escapes take room too, so less text fits than the number says.\n\nHeaders and args are written as whole pairs while the budget lasts. The body is a prefix; bytes that do not form UTF-8 are replaced with “�”.\n\nThe archive keeps the object whole and for long; the record exists for search, and big budgets bloat every record. The three budgets together are bounded by the size of one message to the agent — more than that fails `nginx -t`.",
    budgetNone: "required",
    pairCap: "Pair cap",
    pairCapHint:
      "The second value of a headers or args slice: `30k/2k` — at most 2k per name–value pair inside the budget. Empty — pairs are not cut.\n\nA value longer than the cap is cut at a character boundary, and the pair is marked as cut. A pair whose name alone takes more than half of the cap is skipped entirely and counted in `headers_preview_dropped` or `args_preview_dropped`.",
    pairCapNone: "none",
    archiveSize: "Size",
    archiveSizeHint:
      "How many bytes of the object to take to S3. Empty — the whole object.\n\n**Headers and args** are cut at a whole pair, **the body** to a prefix; a cut object is marked incomplete in the record.\n\nOn the **request phase** any size within `waf_body_limit` and `client_max_body_size`: whatever is wider than the capture, the module hands to the agent with the record. On the **response phase** the body is held only at capture size — wider is not allowed. On **frames** — up to the side's `waf_body_limit`, wider than the capture is fine.",
    archiveSizeJournalHint:
      "How many bytes of the object to take to S3. Empty — the whole object, but no more than the phase's `waf_body_limit`.\n\nThe phase has no inspectors: the object is taken from the traffic itself and reaches the agent with the record.",
    source: "Source",
    sourceHint:
      "Which version of the body the record shows.\n\n**As received** — the body before any rewrite. **As delivered** — what the recipient got after an inspector rewrite; the record marks it with `body_preview_source: sent`. No rewrite happened — the record shows what was received.\n\nHeaders and args have no second version: the name lists decide what of them is shown.",
    sourceCapture: "as captured",
    sourceCaptureHint: "the capture slice and masks; the route's own lists apply on top",
    sourceOriginal: "original",
    sourceOriginalHint:
      "before the capture masks; only the route's own lists apply, none — no masks",
    recordReceived: "as received",
    recordReceivedHint: "the body before any inspector rewrite",
    recordSent: "as delivered",
    recordSentHint:
      "the body sent to the recipient after a rewrite; the archive keeps the received one",
    lists: "Name lists",
    listsHint:
      "Which names land here and in what form.\n\n**As captured** — the same Mask and Do not capture as the capture: exactly what inspectors saw.\n\n**Own** — independent lists: they replace the capture lists instead of adding to them. A name the capture hid that own lists neither mask nor drop lands here in the clear — the window warns about that separately. The module takes the original for that by itself.",
    listsCapture: "as captured",
    listsCaptureHint: "the capture's Mask and Do not capture — what inspectors saw",
    listsStandard: "standard",
    listsStandardHint: "the phase masks and denials set by waf_capture lines",
    listsJournalHint:
      "Which names land here and in what form.\n\n**Standard** — the phase masks and denials set by `waf_capture` lines on this level or above. The phase has no inspectors, so these lists apply to the record and the archive only.\n\n**Own** — independent lists: they replace the standard ones instead of adding to them.",
    listsOwn: "own",
    listsOwnHint: "your own choice of names and masks instead of the capture lists",
    namesMode: "Which names",
    namesModeHint:
      "**All except the named** — every name lands, the named ones are dropped. Good for removing a few known secrets.\n\n**Only the named** — only the named names land, the rest are dropped, including ones you do not know about in advance. Good for responses and strict routes.\n\nMasks apply separately in both modes: a masked name stays, its value replaced with sha256.",
    namesExcept: "all except the named",
    namesExceptHint: "the named names are dropped, the rest land",
    namesOnly: "only the named",
    namesOnlyHint: "only the named and the masked names land",
    namesExceptShort: "except",
    namesOnlyShort: "only",
    namesDrop: "Drop",
    namesDropHint:
      "Names that will not be here at all — neither the name nor the value. Case-insensitive. Empty — nothing is dropped.",
    namesKeep: "Keep only",
    namesKeepHint:
      "The only names that land here; every other name is dropped. Masked names need not be listed here — they stay by themselves. Case-insensitive.",
    mask: "Mask",
    ownMaskHint:
      "The name stays, the value is replaced with its sha256. Hashes let you match requests — say, the same token from different addresses — without revealing the value. A name the capture already hashed is not hashed again. Empty — no masks.",
    captureMaskHint:
      "The name stays, the value is replaced with its sha256 — before the exchange, so inspectors see only the hash. Hashes let you match requests (the same token from different addresses) without revealing the value.\n\nThe record and the archive with lists “as captured” get the same hash. Names are matched case-insensitively.",
    deny: "Do not capture",
    captureDenyHint:
      "The name is dropped together with its value before the exchange: inspectors never learn it was there.\n\nThe record and the archive with lists “as captured” do not get it either; with own lists they do, unless those lists drop it themselves. Names are matched case-insensitively.",
    addName: "add name…",
    whole: "whole",
    no: "no",
    clearedHere: "cleared here",
    namesMark: "own names",
    ownEmpty: "own lists empty — no masks",
    ttl: "Keep",
    ttlForever: "forever",
    ttlHint:
      "How long the object lives in the archive: `30d`, `12h`. Empty — forever.\n\nThe lifetime travels as the object tag `waf-retain-ttl`, and the bucket's lifecycle rule for that tag deletes the object. **Every lifetime needs its own rule in the bucket**: without one the object is never deleted and stays forever.",
    when: "Outcome",
    whenHint:
      "On which request outcomes the object goes to the archive.\n\nNothing pressed — on any outcome, redirect included. **Deny** — only when the request was denied. **Allow** — only when it was let through. Both — everything but redirect.\n\nThe outcome is the whole route's: a deny on the response phase counts as a deny for request objects too, so their record waits for the end of the response.",
    whenSummaryAny: "any outcome, redirect included",
    whenSummaryDeny: "deny only",
    whenSummaryAllow: "allow only",
    whenSummaryBoth: "everything but redirect",
    alert: {
      capture:
        "The capture is the only copy of the object inspectors see. The slice cuts the object, Mask and Do not capture apply before the exchange, and the original is out of inspectors' reach. The record and the archive with lists “as captured” show names the same way; their sizes are their own.",
      sourceOriginal:
        "The original is taken before the capture masks, and only the route's own lists for the record or the archive apply. With no own lists the object goes without any masks, and a header the capture masked ends up in the clear.",
      sourceSent:
        "The record gets the body sent to the recipient after a rewrite. The received body stays only in the archive, if body archiving is on.",
      namesOnlyEmpty:
        "In “only the named” mode, name at least one name: an empty list would mean “nothing”, yet the config reads it as “everything”.",
      ownListsOpen:
        "The capture hides {names} from inspectors, and own lists neither mask nor drop them — they land here in the clear. If that is not intended, add them to Mask or Drop.",
      ownListsOpenJournal:
        "The phase's standard lists hide {names}, and own lists neither mask nor drop them — they land here in the clear.",
      auditOverride:
        "An inspector can ask for this object to be recorded through the action channel — with its own budget and source, even when recording is off here and the request missed the sample. What stands here is the route's default, not a ban.",
      archiveOverride:
        "An inspector can ask for this object to be archived through the action channel — with its own size, lifetime and source, and on any outcome. What stands here is the route's default, not a ban.",
      sendOriginal:
        "The recipient gets the object as it arrived: inspector edits are not applied — the same on the request, the response and frames. An edit, if there was one, lands in the record with applied:false.",
      sendStoreBody:
        "The recipient gets the inspector's version if one was put; several edits — the last one goes, none — the original goes. The capture must be whole for that: a body wider than the slice is a failed lift, and the phase policy for an unavailable object decides what happens (deny by default).",
      sendStoreNames:
        "Inspector edits land over the original by name and stack wave after wave, a later edit of a name wins. A name nobody touched goes as received, even if the capture cut or masked it.",
    },
    problem: {
      overBodyLimit: "{size} is above waf_body_limit ({limit}) — nginx -t will reject it",
      overClientMax: "{size} is above client_max_body_size ({limit}) — nginx -t will reject it",
      notInCapture:
        "the body is not captured, and after the request phase the module holds only what it captures — turn on Capture for the body",
      widerThanCapture:
        "{size} is wider than the capture ({capture}): after the request phase the body is held only at capture size — widen the capture or lower the size",
      budgetRequired: "the record needs a size — set the budget",
      ownListsOpen: "own lists open what the capture hides: {names}",
      sendNotCaptured:
        "nothing to deliver edited: the object is not captured — nginx -t will reject it",
      sendPrefix:
        "slice {size}: an object wider than the slice is not delivered edited — that is a failed lift decided by the phase policy for an unavailable object; an object within the slice is delivered",
    },
    emitted: "In the file at this level",
    emittedNone: "No lines of its own — what is above applies.",
    fromAbove: "applies from above",
    nothingAbove: "nothing is set above",
    phase: {
      request: "request",
      response: "response",
      "frame:c2s": "client frames",
      "frame:s2c": "application frames",
    },
    phaseHint: {
      request:
        "**Request phase** — the headers, query string and body the client sent. Module default: capture headers and the query string whole, do not capture the body.\n\nThe module has the whole body until the request ends, so the record and the archive may be wider than the capture. Without inspectors the phase keeps a journal: the record and the archive take the object from the request itself.",
      response:
        "**Response phase** — the headers and body of the protected server's response. Default: capture nothing.\n\nThe response body capture sets how much of the response the module holds until the verdict and makes it ask the upstream for an uncompressed response — inspectors do not read a compressed body. The body in the record and the archive is no wider than the capture; headers can be any size.\n\nWithout inspectors the response is not held: the client gets it at once, and the record and the archive get a copy of the start of the body.",
      "frame:c2s":
        "**Client frames** — the payload of WebSocket frames from the client to the application. Default: capture nothing.\n\nFrames have no masks, and the whole frame sits in a buffer until the verdict, so the record and the archive may be wider than the capture — up to the side's `waf_body_limit`. Which frames reach the journal and the archive is set by the frame record policy on the right; without inspectors only “record all” writes.",
      "frame:s2c":
        "**Application frames** — the payload of WebSocket frames from the application to the client. Default: capture nothing.\n\nFrames have no masks, and the whole frame sits in a buffer until the verdict, so the record and the archive may be wider than the capture — up to the side's `waf_body_limit`. Which frames reach the journal and the archive is set by the frame record policy on the right; without inspectors only “record all” writes.",
    },
    frameAudit: "Frame records",
    frameAuditHint:
      "`waf_audit_frames` — **which frames reach the journal**, and with them the frame slice (To record) and archive.\n\n**Record denials** — only a frame that was denied, rewritten or scored; without inspectors there are none. **Record all** — every frame of both sides (or every n-th); the frame goes to the recipient without waiting for the record. **No records** — neither frames nor the session summary.\n\nThe session summary is written when the connection closes, with any value but “no records”. One policy per path, for both sides.",
    frameAuditOptions: {
      inherit: "inherits: {value}",
      off: "no records",
      deny: "record denials",
      all: "record all",
    },
    frameAuditEvery: "every {n}th",
    frameAuditEveryOne: "every",
    frameAuditWarn: {
      off:
        "with no frame records the frame slice and archive do not go out, and there is no session summary",
      deny:
        "no frame inspectors, and “record denials” records only denied, rewritten or scored frames — this side has none; choose “record all”",
    },
    phaseEmpty: {
      request: "nothing is captured",
      response:
        "response is not captured — response-phase inspectors see only the status and metadata",
      "frame:c2s":
        "frames are not captured — frame inspectors see only the opcode and the handshake URI",
      "frame:s2c":
        "application frames are not captured — inspectors see only the opcode and the handshake URI",
    },
    phaseEmptyJournal:
      "no inspectors — nothing to capture for; the record and the archive take the object from the traffic itself",
  },
};
