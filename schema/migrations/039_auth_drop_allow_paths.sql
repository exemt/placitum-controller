-- Список путей мимо калитки убран из профиля.
--
-- Он решал ту же задачу, что и маршрут: путь, на котором инспектор не объявлен,
-- до шины не доходит вовсе, а `allow_paths` заставлял спрашивать калитку и
-- отвечать allow. Дороже, и второе место, где решают, что публично.
--
-- Пути API остаются: маршрутом их не заменить -- путь остаётся закрытым,
-- меняется только форма отказа (401 вместо редиректа на форму).

update auth_profiles
   set doc = doc #- '{gate,allowPaths}'
 where doc -> 'gate' ? 'allowPaths';
