-- Встроенные страницы отказа говорят с клиентом, а не с разработчиком.
--
-- В текстах жили внутренние слова контура -- «калитка», «клиренс», «защитный
-- контур» -- и объяснения устройства: почему на не-навигацию отвечают кодом, а
-- не редиректом, и как складывается счёт признаков. Человеку в браузере это
-- не говорит ничего: он не знает, что у нас калитка, и решить свою задачу
-- («войти», «подождать», «позвонить в поддержку») ему это не помогает. Тексты
-- приведены к обычному виду страницы отказа: заголовок, одна фраза о том, что
-- случилось, публичная причина, что делать, и опознавательные данные внизу.
--
-- Правка текстовая, как в 066: тела сеются в каждое пространство (062), их у
-- оператора бывает десяток, а перечислять их заново значило бы завести второе
-- место, где они лежат, и разойтись с `nginx/pages/*.html` при первой правке.
-- Те же пары применены к исходникам образа.
--
-- Трогаются только встроенные тела (`datasets.builtin`): копия, снятая
-- оператором кнопкой «Скопировать», -- уже его текст, и переписывать его
-- миграцией нельзя. Отбор по типу содержимого: в этой же таблице лежат
-- картинки, и `convert_from` на них упал бы.
do $$
declare
    pair record;
begin
    for pair in
        select * from (values
            ('  <p>Ограничение действует на вашу сессию, а не на адрес.</p>',
             '  <p>Ограничение действует на вашу сессию.</p>'),
            ('<p class="lead">Запрос остановлен на защитном контуре и до приложения не дошёл.</p>',
             '<p class="lead">Запрос отклонён системой защиты и до приложения не дошёл.</p>'),
            ('  <p>Запрос отклонён политикой безопасности.</p>',
             '  <p>Доступ к этому ресурсу закрыт настройками безопасности.</p>'),
            ('<p class="foot">Если доступ нужен по работе, передайте в поддержку Event ID — по нему запрос находится в журнале.</p>',
             '<p class="foot">Если доступ вам нужен, обратитесь в поддержку и укажите Event ID.</p>'),
            ('<p class="lead">Ресурс закрыт вторым фактором. Запрос дошёл до калитки, но она не нашла подтверждённой сессии.</p>',
             '<p class="lead">Этот ресурс доступен только после входа.</p>'),
            ('  <p>Для этого ресурса нужен второй фактор.</p>',
             '  <p>Вход не выполнен или сессия истекла.</p>'),
            ('<p class="foot">Запросу, который не является обычной навигацией, калитка отвечает 401, а не редиректом: редирект отобрал бы у POST тело, а у fetch — ответ.</p>',
             '<p class="foot">Если вход выполнен, а доступ всё равно закрыт, обратитесь в поддержку и укажите Event ID.</p>'),
            ('<p class="lead">Ресурс просит пройти проверку. Запрос дошёл до калитки, но клиренса у него нет.</p>',
             '<p class="lead">Прежде чем открыть ресурс, нужно пройти проверку.</p>'),
            ('  <p>Для этого ресурса нужна проверка на человека.</p>',
             '  <p>Проверка не пройдена или её результат устарел.</p>'),
            ('  <p>Откройте заново страницу, с которой вы сюда попали: проверке нужен свежий билет.</p>',
             '  <p>Откройте страницу заново — проверка начнётся автоматически.</p>'),
            ('<p class="foot">Запросу, который не является обычной навигацией, калитка отвечает 403, а не редиректом: редирект отобрал бы у POST тело, а у fetch — ответ.</p>',
             '<p class="foot">Если проверка не проходит, повторите попытку позже или обратитесь в поддержку и укажите Event ID.</p>'),
            ('<p class="lead">Защитный контур не смог обработать запрос. Это сбой на нашей стороне — с вашим запросом всё в порядке.</p>',
             '<p class="lead">Запрос не удалось обработать. Это сбой на нашей стороне — с вашим запросом всё в порядке.</p>'),
            ('  <p>Внутренняя ошибка контура защиты.</p>',
             '  <p>Внутренняя ошибка сервиса.</p>'),
            ('<p class="foot">Повторите попытку через минуту. Если не проходит и дальше, передайте в поддержку Event ID.</p>',
             '<p class="foot">Повторите попытку через минуту. Если ошибка повторяется, обратитесь в поддержку и укажите Event ID.</p>'),
            ('<title>Запрос не разобран</title>',
             '<title>Некорректный запрос</title>'),
            ('<h1>Запрос не разобран</h1>',
             '<h1>Некорректный запрос</h1>'),
            ('<p class="lead">Контур не смог разобрать запрос: он не соответствует формату HTTP или контракту сервиса.</p>',
             '<p class="lead">Запрос не удалось разобрать: он не соответствует ожидаемому формату.</p>'),
            ('  <p>Запрос составлен неверно и проверку не проходил.</p>',
             '  <p>Формат запроса нарушен.</p>'),
            ('<p class="foot">Проверьте метод, заголовки и тело запроса. До приложения он не дошёл.</p>',
             '<p class="foot">Проверьте адрес, метод и данные запроса и повторите попытку.</p>'),
            ('<p class="lead">С вашего адреса приходит больше запросов, чем разрешено. Приложение здесь ни при чём: ограничение стоит на контуре.</p>',
             '<p class="lead">С вашего адреса приходит больше запросов, чем разрешено.</p>'),
            ('<p class="foot">Снизьте частоту и повторите. Автоматическим клиентам стоит добавить паузу между запросами.</p>',
             '<p class="foot">Подождите и повторите попытку. Автоматическим клиентам стоит снизить частоту запросов.</p>'),
            ('<p class="lead">Запрос набрал слишком много признаков атаки. По отдельности ни один из них не решает, но вместе они дали отказ.</p>',
             '<p class="lead">Запрос похож на атаку и до приложения не дошёл.</p>'),
            ('  <p>Сочетание признаков в запросе превысило допустимый порог.</p>',
             '  <p>В запросе нашлись признаки атаки.</p>'),
            ('<p class="foot">Похоже на ошибку? Передайте в поддержку Event ID — по нему видно, какие именно признаки сработали.</p>',
             '<p class="foot">Если это ошибка, обратитесь в поддержку и укажите Event ID.</p>'),

            /* auth_forbidden (070): страницы в образе нет, тело живёт только в базе. */
            ('<p class="lead">Вход выполнен, но этот раздел открыт не всем: у вашей учётной записи нет нужной группы.</p>',
             '<p class="lead">Вход выполнен, но прав на этот раздел у вашей учётной записи нет.</p>'),
            ('  <p>Повторный вход ничего не изменит: группы приезжают вместе с учётной записью. Допуск выдаёт тот, кто отвечает за ресурс.</p>',
             '  <p>Доступ выдаёт администратор ресурса.</p>'),
            ('<p class="foot">На форму входа калитка не уводит: вход уже выполнен, и второй заход выдал бы ту же сессию с теми же группами.</p>',
             '<p class="foot">Если доступ вам нужен, обратитесь к администратору ресурса и укажите Event ID.</p>')
        ) as p(was, now_is)
    loop
        update dataset_contents c
           set body = convert_to(
                        replace(convert_from(c.body, 'UTF8'), pair.was, pair.now_is),
                        'UTF8'
                      ),
               updated_at = now()
          from datasets d
          join content_types t on t.id = d.content_type_id
         where d.id = c.dataset_id
           and d.builtin
           and d.kind = 'content'
           and t.name = 'html'
           and position(convert_to(pair.was, 'UTF8') in c.body) > 0;
    end loop;
end $$;
